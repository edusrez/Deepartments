# Deepartments — runtime store map (verified 2026-09-05, LANE W-3)

Canonical locations of the org state stores and the department report trees,
**verified live** on 2026-09-05 (builder-32, LANE W-3 — fb-64 verification +
fb-136 report-path topology). This is the TOPOLOGY correlato of the fb-140
convention (VERIFICATION-LADDER §6 — always cite ABSOLUTE report paths): it
answers "which store is canonical for what" so a cited path always resolves.

## 1. The stores (verified probes: inodes, mtimes, existence)

| Store | Path | Contents | Status |
|---|---|---|---|
| LIVE stateDir | `/.deepartments` (inode 904961) | `feedback.jsonl`, `posts.json`, `calendar.json`, `deliveries.jsonl`, `journals/`, `hosts.json`, `health-heartbeat.json`, `restart-registry.jsonl`, `interrupt-state.json`, … | LIVE — the runtime stateDir: config `stateDir: .deepartments` is RELATIVE (`packages/dshd-core/cordis.patch.yml:43`) and resolves against the daemon `WorkingDirectory=/` → the EFFECTIVE stateDir is `/.deepartments`. NO departments/reports tree under it (glob `departments/**` → none). |
| Departments workspace tree | `/root/.deepartments/departments/<dept>/` (inode 818256) | per-dept workspaces + their `reports/` trees | LIVE — the configured `workspacePath` tree (`cordis.patch.yml:55,79,100`). Only the worker's OWN deptCwd is an allowed dept_exec root under it. |
| stale top-level state | `/root/.deepartments/{health-heartbeat,posts,config-presets,…}.json*` | pre-flight/smokes leftovers — `health-heartbeat.json` 09-03 16:45, `posts.json` 09-03 17:01; **no** `deliveries.jsonl`/`feedback.jsonl`/`calendar.json` | STALE — footprint of a former CLI/pre-flight run (fb-134). NOT the stateDir. |
| legacy repo reports | `/home/esuarez/projects/deepartments/.dsh/reports/<agent>/` | pre-department reports (2026-08-16..08-22 era) + **LIVE quality store** | LEGACY for RD/IPD (WORK-REGISTER D-6: no-tocar); **LIVE for quality** (D-Q6 deliberate divergence, see §2). |
| repo-relative leftover | `/home/esuarez/projects/deepartments/.deepartments/` | `boot-crash.json` (09-05 00:38), `capacity-gate-state.json` | STALE — generated by CLI invocations run with the repo as cwd (fb-134 class; the relative `stateDir` resolves against THAT cwd). |

## 2. The report map — ruta → store per department (TODAY)

| Dept | Canonical report store | Verified example |
|---|---|---|
| research | `/root/.deepartments/departments/research/reports/{researcher,daily-news,reviewer}/` | `…/daily-news/2026-09-04.md` ✓ (glob hit), `…/researcher/2026-09-04-fb134-store-separation.md` ✓ |
| internal-programming | `/root/.deepartments/departments/internal-programming/reports/{builder,reviewer,explore-deep}/` | `…/builder/2026-09-05-…` ✓ (547 report files) |
| quality | `/home/esuarez/projects/deepartments/.dsh/reports/quality/` — D-Q6 deliberate divergence from the department-workspace `reports/` dir (documented in `docs/specs/007-quality-department.md`, `presets/departments/quality/*`) | `…/2026-09-05-quality-daily.md` ✓ (write 09-05 11:04) |

**Corollary for citations (fb-136/fb-120 class):** a glob on `*.deepartments*`
may return SHORT/relative-looking paths (e.g. `.deepartments/…`, `reports/…`)
whose absolute form depends on the probe cwd. Deriving `/.deepartments/…` from
them is WRONG — the reports tree lives under `/root/.deepartments/departments/
<dept>/reports/`, never under the stateDir. Always cite the absolute canonical
path of §2 (fb-140).

## 3. fb-64 verification — execRoots/stateDir (verdict: NO live drift)

**Code (repo HEAD 9308e3d):** the dept_exec allowed-roots computation DOES
include the runtime stateDir — `deptExecAllowedRootsInline` adds `stateDir`
verbatim (`packages/dshd-orchestration/src/tools.ts:1393`, computation
1390-1416; service-first wrapper 1423-1431 falls back to it). The deny guard
(`src/invoke.ts:2074-2145`) scans every `/`-leading token against that set,
realpath-canonicalized (HOTFIX 0.2.2-1 split; R5 fb-138/142 hints intact).

**Live probes (builder-32, 2026-09-05):**

| Probe | Result |
|---|---|
| `ls /.deepartments/feedback.jsonl` + `tail -n 1` | **ALLOWED** (exit 0) — fb-64's core use case works |
| `ls /.deepartments/calendar.json` | **ALLOWED** |
| `stat` on stateDir files | **ALLOWED** |
| `dept_zstd_read` on `/opt/dsh/.dsh-dev/sessions/…/session.jsonl.zstd` | **ALLOWED** (3 lines decoded) — session zstd use case works |
| `ls /home/esuarez/projects/deepartments/packages/*` | **ALLOWED** (repo root) |
| `ls /root/.deepartments/…` (ANY absolute token under it) | **DENIED** — BY DESIGN: `/root/.deepartments` is NOT the stateDir and is NOT an allowed root; only the worker's own deptCwd (`…/departments/internal-programming`) is |

**Verdict:** the declared stateDir include == runtime allow. The persistent
`/root/.deepartments` deny is EXPECTED (that tree is the workspace tree + stale
state; cross-workspace siblings and the tree root are denied by design). No
code drift persists; the residual was a doc NAMING drift (docs/tests calling
`/root/.deepartments` "the stateDir"), already corrected in
`presets/departments/internal-programming/ARCHITECTURE.md` (execution-scope
section, commit b05d75a option C). Feedback record fb-64 → verified, no
follow-up lane needed on the allow-list itself.