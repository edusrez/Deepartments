# ROADMAP — Deepartments phases

Roadmap of the Deepartments project (`dsh-deepartments`): phases with their
status, content and exit criterion, and the phase 2 kickoff tasks. Decisions
and mapping in [concept.md](concept.md); the idea in [IDEA.md](IDEA.md).

## Phases

| Phase | Status | Content | Exit criterion |
|---|---|---|---|
| **0. Research** | ✅ **COMPLETE** (2026-08-16) | Research of the DSH plugin API: mapping cells resolved; feasibility confirmed (rc.6, native mechanisms for posts/activations/witness); decisions 1-2. | Report delivered; feasibility verdict: **positive**; mapping with no pending items blocking the MVP. |
| **1. Concept** | ✅ **COMPLETE** (2026-08-16) | v1 draft + debate; 10 v1 questions → 9 binding decisions + naming proposal; the concept (docs/concept.md). | Decisions taken; concept approved; phase 2 authorized. |
| **2. MVP** | 🚧 **IN PROGRESS** | **Milestone 1 (first, reordered 2026-08-16): board room + research department** — coordination room, research department and coordinator post in the plugin config; addressed envelopes + agenda; `dept_invoke` owner flow. First milestone (board room + research department) done 2026-08-16; next = programming-department dogfooding. **Milestone 2 (former MVP):** one department (programming) with roles absorbed from the previous setup; persistent posts (continuable + witness); CEO → Asistente → head → parallel builders → reviewer → verification → commit flow; sleep/wake cycle managed by the plugin; dogfooding on the repo itself; headless. — Superseded by the 2026-08-19 aggressive restructure A-G (board-as-bus, permanent lean department heads, host direct channel, nap/sleep lifecycle), which now implements this milestone's success criteria (a post falling asleep midway and resuming without loss) under the new model. — **2026-08-31: MATURITY MILESTONE** — bundle publicado a npm (`0.1.0`), modularización Cordis casi completa (D5+P1 + hito 3 decoupling: applyInvoke → fábricas de orquestación, tools SB1-3 ✅, SB4/presets/boot en curso), canary post-boot client-graph live. La Fase 2 queda operativa y estable; cierre del hito 3 = último gran paso funcional. | MVP success criteria (concept.md §4): end-to-end assignment with a post that falls asleep midway and resumes without loss; cycle managed without manual glue. |
| **3. Rooms and activations** | Pending | Multiple rooms with reception and visits; scheduled (`dsh-schedule`) and reactive (events/MCP) activations; assignments between posts; editable governance policy; **client plugin UI — total observer** (decision 2); archiving/querying dead rooms. | Two departments coordinating via head → Asistente; a scheduled rhythm running without human intervention; a dead room archived and queryable; a governance exception applied; the UI shows the organization's state. |
| **4. Self-observation, quality, self-modification** | Pending | Quality group + dream/post-session interview; proactive contextualizer; operational channeled escalation; self-modification of structure and implementation with safeguards and recovery (default structure). | The organization proposes a structure or implementation change, the CEO approves it, it is applied with quality gates, and the way back (default structure) works. |

## Phase 2 kickoff

Concrete tasks to start development, in order, each with its "done"
criterion. (The `dsh-plugin-dev` skill is created first — Recommendation 1
of the research — so no builder needs to re-research.)

| # | Task | "Done" criterion |
|---|---|---|
| ✅ 1 | **Seed the repo** (docs + skill first): `README.md`, `docs/IDEA.md`, `docs/concept.md`, `docs/ROADMAP.md`, `AGENTS.md` and `.dsh/skills/dsh-plugin-dev/SKILL.md` (verbatim text of the concept annex) + initial commit. | `git status` clean in `/home/esuarez/projects/deepartments`; a builder session in the repo loads the `dsh-plugin-dev` skill from the catalog (root `<project>/.dsh/skills`, rank 100). — DONE 2026-08-16 (docs, AGENTS.md and the dsh-plugin-dev skill committed; repo clean). |
| ✅ 2 | **Development profile**: create `deepartments-dev` (headless template, independent port) for development and smoke without touching the web profile. | `dsh --profile deepartments-dev --dump-config` composes without errors; the headless boot starts on its own port. — DONE 2026-08-16: dev profile `deepartments-dev` (web GUI clone of `web`, port 3090, isolated DSH_HOME `/opt/dsh/.dsh-dev`, systemd `dsh-deepartments-dev`, Tailscale https://laagencia.taildb5a7a.ts.net:8445) + headless twin profile `deepartments-dev-headless` for automated smoke. |
| ✅ 3 | **Bundle scaffold**: `package.json` (name `dsh-deepartments`, 0.1.0-rc.1, `dsh.bundle`, rc peerDeps), `cordis.patch.yml` (`deepartments` row), minimal `src/index.ts` (`name`/`inject`/`apply` with a log effect), tsconfig NodeNext, build to `lib/`. | `dsh plugin --profile deepartments-dev add /home/esuarez/projects/deepartments` installs and `dsh --profile deepartments-dev --dump-config` shows the `# == dsh-deepartments` layer. — DONE 2026-08-16: bundle scaffolded (package.json rc.1, cordis.patch.yml, src/index.ts, tsconfig NodeNext), installed in both dev profiles, TIERED verification green (layer `# == dsh-deepartments` + real headless smoke answering `pong`). |
| ✅ 4 | **Board room architecture in the plugin**: coordination room + research department + coordinator post defined in `cordis.patch.yml`; the plugin instantiates the room state (log, cursors, agenda) at boot. | `--dump-config` shows the layer with room config; headless smoke shows the room instantiated; the room board persists in `<stateDir>/rooms/<roomId>/board.jsonl` across restarts (decision 17). — DONE 2026-08-16: board room architecture (org config, room-state projection, boot instantiation) + durable board.jsonl persistence (decision 17). |
| ✅ 5 | **`dept_invoke`**: the Asistente forks into the board room, converses with the research coordinator (addressed envelopes, agenda), and merges back via the board delta + relevo witness. | Headless smoke: the Asistente's `dept_invoke` spawns a fork that posts addressed messages to the board room and converses with the research coordinator; the fork merges back (board delta + relevo witness) and the Asistente's session receives the result. — DONE 2026-08-16: dept_invoke + board toolset (dept_room_read/write, dept_witness_write) + plugin wake relay + coordinator post bootstrap; verified in the dev GUI (long-lived profile). |
| ✅ 6 | **Milestone smoke** (success criterion of decision 10): owner invokes the Asistente → board room → short conversation with the research coordinator → back to the owner. | Decision 10's success criterion: owner invokes the Asistente → board room → short conversation with the research coordinator → back to the owner's office chat; the room state persists across sessions. — DONE 2026-08-16 — MILESTONE ACHIEVED: the owner invoked the Asistente on the dev GUI (deepartments-dev); the fork conversed with the research coordinator on the board (6 addressed messages); the coordinator delivered a full research report; the fork closed with its schema-constrained relevo witness; the Asistente returned to the owner with the result (7 turns). Room state persisted across a service restart. |
| 7 | **`dept_post_create`** (moved): the coordinator creates department agents (researcher posts) inside its department room. | Headless smoke: the coordinator creates a researcher post (asleep) in its department room and wakes it with a followup; the `deepartments/post-created` event appears in the session log. |
| 8 | **Programming-department dogfooding** (former MVP, moved): CEO → Asistente → head → N parallel builders → reviewer → verification → commit; a post falls asleep halfway and resumes without loss. | Complete assignment through CEO → Asistente → head → N parallel builders → reviewer → verification → commit, with a post asleep halfway and resumed without loss; includes the witness skeleton (schema-constrained frontmatter, `agent/turn-stopping` sleep listener, post projection) folded from the former task 5. |

> The former 'witness skeleton' task (witness convention + post projection + sleep listener) is folded into task 8 (milestone 2); `dept_invoke` (task 5) already carries the 1:1 relevo witness for supersession.

## Current status

- **2026-09-02** — **MISIÓN TOTAL MODULARITY (0.2.x) COMPLETA — termómetro 8/8 (estructura cerrada)** — lanes: 0.2.1 (P6 binder→Service, `81ef5cd`) · 0.2.2 (P1+P4 dshd-orchestration, `9cda995`) · **fix fb-55** (`5210682` — execRoots service shape restaurado, deploy canary 02:22Z) · **0.2.3a** (`e8222af` — single-source org vía `dshd-core-min` [mirror colapsado, parity 5/5] + R4 agentOptions org-driven + jobs→spawn service-first + TEST P7 real) · **0.2.3b** (`dc9f79a` — gui-split [mount en dshd-gui, rutas byte-iguales] + register legacy RE-HOME verbatim con **re-freeze CUT-4 `fe319e0f`→`5b548545`** + re-plumb W8-c + test de regresión jobs→spawn commiteado) · **0.2.3c** (`48cea9f` — dshd-webfetch a plugin completo, override de perfil preservada; CIERRE de misión) — suite raíz **720/698/0/22 EXACTA**; P4 SÍ (policy-substitution + 5210682) · P6 SÍ · P7 SÍ (single-source + gui + webfetch + P7-tests) · P1 avanza (bundle net-negative) · P5/P8 parcial. **RESIDUAL CERRADO: DI-BY-SERVICES (73f60d9, deploy canary 11:17Z) — muerte del binder register (holders baseline service-first, P1 0 ctx.provide, md5 CUT-4 5b548545 intacto, suite 724/702/0/22 EXACTA) → termómetro 8/8, dept-as-plugin DESBLOQUEADO**. Deploy canary 06:14Z — 0.2.3a/b/c + fb-55 + **dshmarket 1.40.0** + dsh-smooth-stream 0.4.3 live en dev. (detalle: docs/WORK-REGISTER.md)

- **2026-09-01** — **HITO 3 CERRADO — MODULARIZACIÓN DECOUPLING COMPLETA (5/5 fábricas) + JORNADA POOLER/FLOTA** (cadena completa): (a) **HITO 3 CERRADO** (`f28c719` — zona 7 boot 678 LOCs, BootSurface 39 miembros, md5 82761e5d, LOCK boot-factory 3/3, suite **651/629/0/22 EXACTA**): las 5 fábricas de orquestación (delivery/spawn/tools/presets/boot) + PASO 1/SP2/FB-33/SP3/SB1-4; applyInvoke coordinador final; 0 ctx.provide (P1). La Fase 2 queda con su último gran paso funcional cerrado. (b) **CRISIS DEL POOLER RESUELTA**: **fb-39** (fall-through 401/429 — el request nunca falla habiendo key sana; commit bb22b20 en dsh-key-pooler; implementado por el host como excepción F5 [el IPD no despertaba por el propio pooler; subagentes transitorios son read-only]) + **pooler v2** (`3d7e538` — sweep proactivo 300s + smoke-gated rotation + zombie fix [clase-401 solo revive con 200 real; incidente oc-6] + sanitización 400-[1210] ×/“”; 101/101) + **DS-fallback** live. (c) **EXPERIMENTO glm-5.3-flash PROBADO Y REVERTIDO**: switch desplegado (id verificado en catálogo opencode-go por el RD; allow-list del provider en settings.yaml) → **inusable ~30 tok/s → flota DS V4 Flash MAX para TODA la arquitectura** (`a2d0f7a` + guard alineado `b9e51c2`); glm queda en catálogo para reprobar con otra key/optimización. (d) **CABEZAS ROTADAS** con memo fresco (limpieza de contexto previa al switch, owner). (e) **GAP DE OBSERVABILIDAD DETECTADO** (el owner detectó arquitectura inactiva: una misión entregada al IPH quedó en cola stale sin iniciar y nadie avisó a tiempo) → **M-5 watchdog 'misión-sin-inicio'** despachado al IPD (Fase 4 kickoff). (f) **DECISIÓN DE FASE**: tras el hito 3, **FASE 4 (auto-observación/calidad/auto-modificación) recomendada y en kickoff** (el watchdog M-5 es su primera pieza; la org ya opera autónoma — la auto-observación es el siguiente salto). Fase 3 (Rooms/activations) queda en backlog.

- **2026-08-31** — **RELEASE 0.1.0 + MODULARIZACIÓN HITO 3 (DECOUPLING) AVANZADO** (cadena completa): (a) **`dsh-deepartments@0.1.0` PUBLICADA en npm** (tag latest, 2026-08-29T21:52Z por edusrez; primera estable post-P1; caveat RD pendiente — instalabilidad standalone `workspace:*` + dshd-core 404 → publicar el resto o diferir). (b) **HITO 3 (DECOUPLING)**: PASO 1 ✅ (`3762e05` — baseline + 3 locks [export-parity/binder-contract/smoke-boot real] + RPC guiChannel→dshd-gui + daemons→dshd-health/jobs + relleno 4 buckets) · sub-paso 2 ✅ (`c45b150` — factory delivery.ts 1515 LOCs) · FB-33 ✅ (`58a20d3` — F10 crash-safe snapshot/restore desde `git show HEAD`) · sub-paso 3 ✅ (`b1235b7` — factory spawn.ts 432 LOCs) · **TOOLS SB1-3 ✅** (`e074770`/`ef5eb2a`/`5ee10e7` — factory tools.ts acumulativa a 3430 LOCs con Boot-Wiring; suite **645/623/0/22 EXACTA**) · SB4 (tools) → presets 907 → boot 678 PENDIENTES. (c) **CANARY POST-BOOT client-graph validation** live (`dc6669c` — lección de la regresión GUI del P1: el probe HTTP-200 solo no ejercitaba el boot del cliente). (d) **CRISIS DE CAPACIDAD RESUELTA**: ola de ~8 muertes por `401` billing-agotado en ~9h (auditoría QD q-i-10) + **bug de pick/fall-through del pooler** (se atascaba en una key sin saldo sin caer a las sanas) → fix (bloqueo de keys insaludables + reset del state, fb-13) + **fb-39 filed** (gate de billing 401/credits en pacing/pooler). La org opera con **4 keys sanas** (oc-8/9/11/12); top-up ws10 = PENDIENTE-OWNER recomendado (no bloquea). 

- **2026-08-29** — **HITO P1 de la MODULARIZACIÓN REAL — worktree (builder-9,
  pendiente commit del Asistente tras reviewer PASS)**: los 6 packages-LIB
  (dshd-feedback/gui/health/jobs/pooler/quality) llegan a superficie plugin
  Cordis REAL (patrón name/inject/apply de dshd-core/dshd-webfetch) — sin
  tocar applyInvoke ni los 20 bridges same-module (eso es el hito DECOUPLING).
  (a) CADA package: fila cordis.patch.yml propia + servicio `deepartments.*`
  con las deps INYECTADAS vía el Binder FASE 2.6 (`deepartments.org` = shared
  config source + `deepartments.binder` buckets; dep ausente al USE →
  FAIL-LOUD R1): feedback = el store (self-open desde stateDir, bucket
  `feedback.store` para el store ya-abierto del bundle) · quality = gate +
  directive EMITTER funcional HOY vía los buckets existentes
  (deliver.deliverPost + wakepack.messagesStoreReady + catalog) · jobs =
  scheduler tick (bucket `jobs`: runJob/notifyHead/departmentFor*) · pooler =
  boot check (bucket `pooler`: configuredProviders/appendPostError; retry
  window por su propia config, defaults de código) · health = daemon tick
  (ALERT delivery funcional HOY vía deliverHost + store; scans closure-bound
  vía bucket `health`) · gui = dispatcher/handler bound (bucket
  `gui.endpointDeps`). (b) **peers** en package.json de los 6 (+cordis; el
  grafo entre packages dshd-gui→dshd-jobs, dshd-health→dshd-core+dshd-quality
  — el flag del audit — deps workspace conservadas, R6) + metadatos
  `dsh.bundle.patch`/files para el formato plugin. (c) **composición dev**:
  las 6 capas `# == dshd-*` compuestas en el perfil dev (deepartments-dev),
  dump-config con las secciones dshd-core/deepartments BYTE-IDÉNTICAS al
  baseline (postsRetention core-only, pacing/quality bundle-only,
  poolerBaseURL espejo). (d) **bundle componible en MODO MÍNIMO** (set mínimo
  de filas: los 8 packages + deepartments — verificado en perfil temporal) y
  **plugin add OK** (flujo `dsh plugin add` + reconcile + dump-config,
  scopeteado en perfil temporal del dev HOME). Ladder: suite **619/597/0/22
  EXACTA** (0 skips nuevos), builds raíz + 8 packages + client exit 0, git
  diff --check limpio, P1 smoke (aplica los 6 plugins + verifica los servicios)
  PASS, stable /opt/dsh/.dsh NO tocado.

- **2026-08-29** — **HITO D5 de la MODULARIZACIÓN REAL — worktree (builder-8,
  pendiente commit del Asistente tras reviewer PASS)**: las 3 superficies
  casi-formales del bundle + fold de flags baratos. (a) **patch-row
  deepartments**: fila formal verificada (cordis.patch.yml:3-5) y **doble
  fuente de verdad de org resuelta SIN cambio de comportamiento (R6)** —
  dshd-core = SHARED CONFIG SOURCE, org del bundle = FALLBACK MIRROR
  (one-sided keys documentadas: postsRetention core-only, pacing/quality
  bundle-only) + **parity test nuevo** test/org-config-parity.test.js que fija
  stateDir/departments/poolerBaseURL idénticos. (b) **deepartments-subagent →
  secretary**: `tool-secretary` PROMOVIDO a fila Cordis formal ÚNICA del repo
  (presets/deepartments-head/agent.cordis.yml — SURFACE-FORMAL comment + nota
  en subagent.ts), matiz *TEMPORARY smoke patch* del twin headless retirado
  (perfil dev; R6 nada retirado, la tool sigue cargable vía
  dsh-deepartments/subagent). (c) **deepartments-client**: dshd-gui = OWNER
  de la superficie (build/normalize ÚNICO en el package; el ./client del
  bundle se PRESERVA como espejo byte-idéntico vía scripts/mirror-client.mjs;
  root tsdown.config.ts + scripts/normalize-client-banner.mjs FOLDED);
  README.en/zh + client-watcher header + desc dshd-gui alineados. (d) **flag
  stale**: comentario «dshd-core NOT composed today» corregido (sí se compone
  — compose-first en dsh.profile.bundles de los perfiles dev). Ladder: builds
  exit 0, suite completa verde (sin skips nuevos), git diff --check limpio,
  dump-config capa `# == dsh-deepartments` INTACTA (sin capas nuevas), stable
  /opt/dsh/.dsh NO tocado.

- **2026-08-16** — phases 0 and 1 complete; phase 2 ready to start; task 1
  (seed the repo) in progress.
- **2026-08-16** — tasks 1, 2 and 3 complete (dev topology: isolated DSH_HOME
  `/opt/dsh/.dsh-dev`; GUI profile `deepartments-dev` on port 3090/Tailscale
  8445; headless twin `deepartments-dev-headless` for smoke); task 4
  (`dept_post_create`) ready to start.
- **2026-08-16** — owner decision: subagent dispatch must never block. New
  plugin entry `dsh-deepartments/subagent` provides the four delegation tools
  always-async (no `run_in_background`; settlement notices continue dependent
  chains); installed in the stable profile (restart pending owner's go).
- **2026-08-16** — decisions 10-16 recorded (concept.md); roadmap reordered:
  research milestone (board room + research department) first, programming
  dogfooding next; research report
  `.dsh/reports/researcher/2026-08-16-interagent-communication.md` validates
  the board model; rename of the main agent to **Asistente** applied (docs +
  preset + skill).
- **2026-08-16** — Milestone 1 (decision 10) ACHIEVED on the dev GUI: owner →
  Asistente → board room → research coordinator conversation → relevo witness
  → result delivered back to the owner; board state survives restarts
  (decision 17). Known limitation: the one-shot headless CLI kills
  continuable children at exit — milestone smokes must run on the long-lived
  GUI profile. Follow-ups queued: durable read cursors, respawn of posts
  whose parent is no longer live, dept_room_read truncation of long messages,
  then task 7 (dept_post_create).
- **2026-08-16** — LLM provider migrated from `deepseek-official` to
  `opencode-go` (from `@earendil-works/pi-ai`, registered by
  `@deepseek-ai/dsh-llm-pi-ai`) in both homes (stable `/opt/dsh/.dsh`, dev
  `/opt/dsh/.dsh-dev`) and the plugin coordinator post (cordis.patch.yml),
  keeping the same DeepSeek models `deepseek-v4-pro` (Pro) and
  `deepseek-v4-flash` (Flash). Key insight: the harness `apiKeyEnv` field is a
  **credential-ref** resolved against `$DSH_HOME/.credentials.yaml`, not a
  physical env var name. TIERED ladder green (build, plugin add, dump-config,
  headless smoke self-identifies pro/flash); reviewer PASS; committed
  `ed907ef`. Follow-ups remain queued: durable read cursors, respawn of
  orphaned posts, dept_room_read truncation, then task 7 (dept_post_create).
- **2026-08-17** — **J-Space evaluated and SKIPPED** (owner decision 2026-08-17):
  the third-party "cognitive enhancement" skills suite
  (J-Space-Cognition-Suite-V3.6) + its self-published benchmark claiming
  DeepSeek V4 capability gains will **not** be adopted as a default. Why
  rejected: no demonstrated benefit (an independent A/B in the plugin's own
  issue tracker showed no correctness gain at ~3× tokens and +17–36% time); a
  headline benchmark (TB2.1) was not reproducible and a reviewer suggested
  some entries were "fake boosts" — single-run, vendor-reported, not
  reproducible; it duplicates our existing AGENTS.md/workflow discipline; and
  open-code-go compatibility is unanswered. Reference:
  `.dsh/reports/researcher/2026-08-17-jspace-evaluation.md` (report path is
  gitignored; filename cited here). If ever re-considered: pilot only on a
  throwaway dev agent with an A/B measuring correctness + tokens + time.

- **2026-08-18** — **`dshmarket@1.13.1`** adopted as the plugin store on both
  DSH instances (owner decision; research report
  `.dsh/reports/researcher/2026-08-18-dsh-plugin-store-selection.md`):
  stable profile `web` (`/opt/dsh/.dsh`, port 3080) and dev GUI profile
  `deepartments-dev` (`/opt/dsh/.dsh-dev`, port 3090), installed via
  `dsh plugin add dshmarket@1.13.1` (exact pin, auto-appended to
  `dsh.profile.bundles`). Chosen as the ecosystem-convergence winner over the
  awesome-dsh-plugin catalog: 951★, ~25k npm downloads/day, recommended by
  the awesome list, default preset of two DSH desktop apps, peerDeps matching
  our rc.7 exactly. `allowRestart: false` set in both profiles'
  `cordis.patch.yml` because both instances run under systemd
  (`dsh.service` / `dsh-deepartments-dev.service`) — the market's one-click
  restart is disabled. Reviewer PASS; both services restarted;
  `/dsh-market/status` reports `version 1.13.1` and `restart:false` on
  3080 and 3090. Headless twin `deepartments-dev-headless` intentionally not
  touched (market is web-GUI-only). Follow-ups: builder
  `2026-08-18-dshmarket-allowrestart` reported but did not write its report
  (process gap); re-evaluate store choice if DSH ships an official
  marketplace.

- **2026-08-18** — **`dsh-smart-restart`** (edusrez/dsh-smart-restart, npm + GitHub, MIT)
  built, dogfooded and installed on both instances (stable `web` 3080, dev
  `deepartments-dev` 3090). After any DSH restart it wakes the session that was
  active: via the `smart_restart` tool (explicit — records the caller session +
  reason, restarts the systemd unit detached) or, for a plain `systemctl
  restart` made while an agent was active, via a SIGTERM/SIGINT shutdown hook
  that persists the last-active session and pins the post-restart notice to it
  within a configurable grace (`shutdownGraceMs`, default 10 min). Live-verified
  end-to-end multiple times (auto-wakes without the owner prompting). A critical
  issue was caught in an isolated smoke before release: a SIGTERM handler that
  does not re-raise the signal suppresses Node's default termination and hangs
  systemd restarts to TimeoutStopSec/SIGKILL (fixed — handler now re-raises).
  Versions 0.1.0 → 0.2.0 → 0.3.0 published (npm + tagged GitHub); installed as
  `dsh-smart-restart@0.3.0` in both profiles with per-profile `restartUnit`
  (`dsh.service` / `dsh-deepartments-dev.service`). Research reports:
  `.dsh/reports/researcher/2026-08-18-dsh-boot-notify-plugins.md`,
  `.dsh/reports/explore-deep/2026-08-18-dsh-boot-notify-apis.md`.

- **2026-08-18** — **Bilingual Chinese README** (owner decision 2026-08-18):
  `README.zh.md` (Simplified Chinese) added to the three DSH repos
  (`edusrez/Deepartments`, `edusrez/dsh-smart-restart`,
  `edusrez/dsh-tool-web-enhanced`), following the official deepseek-harness
  convention — English `README.md` stays canonical/default, the zh file is a
  full mirror, and each `README.md` opens with `English | [中文](README.zh.md)`.
  `deepartments/AGENTS.md` gained an explicit i18n carve-out sanctioning the
  README.zh.md pair (English still canonical). Reviewer PASS on both batches;
  all three repos pushed (deepartments also published its 24-commit backlog).

- **2026-08-19** — **`dept_invoke` generalized**: `dept_invoke` gained an
  optional `to` parameter so an assignment can be addressed to ARBITRARY
  board members (a sibling fork post id `asistente-fork-<id>` or a department
  head), replacing the hardcoded coordinator-only addressing. Coordinator
  ensure/wake preserved as the default (no-`to`) path only. Fork prompt
  rewritten mission-driven and room-generic (room + assignment + named
  addressees), keeping the resident-post close (concludes with a report to
  its principal; the post stays resumable/registered in posts.json and is
  re-woken by the relay). Return shape unchanged. Verification: `pnpm build`
  clean, `node --test test/invoke.test.js` 5/5, full `node --test` 19/19,
  TIERED ladder green (plugin add + dump-config layer + headless smoke on the
  twin). Reviewer PASS. Committed as `05abad1`. Purpose: enable the
  cross-session resident-post test (two GUI sessions sharing the board room,
  fork↔fork addressing, settlement-delivered reports).
- **2026-08-19** — **Twin-profile env fix**: the `deepartments-dev-headless`
  twin linked `dsh-smart-restart` via its dev path, whose repo had its
  declared dep `@deepseek-ai/dsh-home-paths` missing from node_modules
  (caused `ERR_MODULE_NOT_FOUND` in the headless smoke). Fixed by `pnpm
  install` in `/home/esuarez/projects/dsh-smart-restart`. Headless smoke now
  green.
- **2026-08-19** — **Next**: the owner will signal when ready to restart the
  `deepartments-dev` GUI service to load the new build, then run the
  cross-session live test (session 1 fork A resident in the board room →
  session 2 fork B → message to fork A → wake relay → settlement report back
  to session 1). Technical validation: the explore report
  `.dsh/reports/explore-deep/2026-08-19-continuable-settle-and-cross-parent-wake.md`
  (NO-BLOCKER verdict).
- **2026-08-19** — **`dept_room_who` board roster tool**: added a read-only
  roster tool to the continuable-child board toolset in `src/invoke.ts`:
  `dept_room_who(room)` lists the room's static org members plus every
  registered post in that room from the live `byPost` registry, with per-post
  `parentLive` (parent agent resident in ctx.agents; false when the agents
  service is absent). It lets a coordinator/fork enumerate the authoritative
  occupants of a room instead of inferring presence from stale board history.
  Root cause this fixes (observed live in the first cross-session test):
  research-head addressed a ghost post `asistente-fork-e7cd9ad5...` left over
  from the 2026-08-16 milestone and visible in the ancient board.jsonl, which
  was NOT in posts.json and thus not wakeable. Verification: `pnpm build`
  clean, invoke tests 6/6, full suite 20/20, TIERED ladder green (plugin add,
  dump-config layer, headless smoke pong). Reviewer PASS. Committed as
  `2aa6561`.
- **Silent-truncation fix + full-message fetch (2026-08-19):** live
  cross-session log analysis (4 reviewers, consolidated at
  `.dsh/reports/scribe/2026-08-19-cross-session-log-analysis-consolidated.md`)
  found the wake relay was silently slicing every addressed message to 200
  chars (invoke.ts:228) with no marker, while `dept_room_read` sliced to 240
  and capped the digest at 10 lines — which caused two forks to accuse each
  other of fabricating truncation, and left no way to recover full text
  except dumping the whole shared `board.jsonl`. Fix (owner choice, option
  A): the wake relay is now pointer-only (message id + from, never body);
  `dept_room_read` supports optional `messageId` full untruncated fetch
  (never advances the cursor), `limit` (default 20) and `offset` pagination,
  with TOC previews `- <id> | from -> to | <preview>` and an explicit `…`
  at 140 chars; the 10-line `digestDelta` cap was removed. Empty reads keep
  the exact `No board messages addressed to you.`. Verification: `pnpm
  build` clean, invoke tests 8/8, full suite 22/22, TIERED ladder green
  (plugin add, dump-config layer, headless smoke pong). Reviewer PASS.
  Committed as `530049a`. The same analysis also surfaced following
  candidates (documented, not yet scheduled): identity/roster validation
  before addressing, secret hygiene on the shared board, persistent
  per-member cursors, minor guards (self-writes, inject canonical postId,
  resolve `anyParentId()` ambiguity).
- **2026-08-19** — **Fork identity + mission-as-official-context**: after
  the live cross-session test exposed role-confusion (a fork receiving its
  mission as a user-role start prompt read it as an injection and refused),
  the explore report
  `.dsh/reports/explore-deep/2026-08-19-fork-identity-context-delivery.md`
  established that rc.7 has no `role:'system'` channel: "official context"
  is a user-role message with a distinguished `source` (settlement
  `{kind:'subagent-settled', form:'notice'}`; report `{kind:'subagent-report',
  form:'relay'}`; the plugin's wake relay already used `{kind:'coordinator',
  form:'relay'}`), and `request.prompt` is always delivered as plain user.
  Fix (owner design): (1) new child tool `dept_whereami` answering spatial
  identity — `kind:'post'` (postId/roomId/members/posts+parentLive) for a
  registered board post vs `kind:'host'` (the Asistente in its private
  room), fixing the `'unknown'` fallback; (2) `dept_invoke` deploys with a
  neutral role prompt and delivers the mission as OFFICIAL context via
  `subagents.followup` with source `{kind:'coordinator', form:'relay'}`
  (deployment snapshot: where you are, who was present on entry, your
  mission), never writing the mission to the board (secret hygiene);
  `messageId` removed from the return; (3) hardening: a failed deliver
  followup no longer leaves a silent orphan — it rolls back the
  just-registered post (awaiting the last registry write to avoid a
  lost-update race) and rejects with an actionable error. Verification:
  `pnpm build` clean, invoke tests 10/10, full suite 24/24, TIERED ladder
  green (plugin add, dump-config layer, headless smoke pong). Two reviewers
  PASS. Committed as `bd518cc`. Next (pending live re-test): a fork
  receives its deployment context and mission via the official channel,
  multi-session password test.
- **2026-08-19** — **Dept_invoke now deploys a spatial-only clone — "the fork
  is you, divided"**: per the owner model, `dept_invoke` no longer carries a
  mission. It only deploys a continuable clone (fork) to the board room with
  a SPATIAL deployment context and injects a spatial notice into the copy
  that stays with the owner. Removed: the `assignment`/`threadId`/`to`
  parameters and the entire coordinator-ensure path (department lookup,
  spawn-provider coordinator, coordinatorInFlight) — `dept_invoke` only
  carries `room` and only ever materializes the fork. Kept: post registry,
  wake relay (kind 'coordinator'/form 'relay'), board toolset
  (dept_room_read/write/witness/who/whereami), registerContinuableSetup,
  rollback-on-followup-failure hardening, and the new non-fatal parent
  context injection (via Agent.inject with a non-user plugin source —
  renders CONTEXT). The fork start prompt is minimal identity framing; the
  deployment followup is spatial-only (official-context prefix, kind
  'post'/postId/room, presence snapshot, dept_whereami pointer) with NO
  mission text — the clone is an identical copy of the Asistente and knows
  what to do, only where it is. Added `@deepseek-ai/dsh-llm` to
  peerDependencies (rc channel, rule 8). Background mechanics (verified):
  the LLM never sees a message's source — the only model-visible "official"
  marker is a textual prefix (per
  `.dsh/reports/explore-deep/2026-08-19-context-input-delivery.md`);
  `request.prompt` is unavoidably a user-role row in rc.7; deployment via
  `subagents.followup` with `{kind:'coordinator', form:'relay'}` already
  renders as CONTEXT in the UI and survives end-to-end. Verification:
  `pnpm build` clean, invoke tests 11/11, full suite 25/25, `pnpm peers
  check` clean (single-instance dsh-llm), TIERED ladder green (plugin add,
  dump-config layer, headless smoke pong). Two reviewers PASS. Committed as
  `45233da`. Follow-up note: `CoordinatorConfig` schema remains declared in
  `src/org.ts` (unused by dept_invoke now; harmless — optional separate
  schema-pruning). The skill now fixes the model: "## The fork is you,
  divided" (WHERE not WHO; no role-confusion resistance; spatial identity
  via dept_whereami). Next (pending): live multi-session password test with
  the spatial clone (no mission).
- **2026-08-19** — **Aggressive restructure A-G (2026-08-19):** after an
  11-auditor critical log review (reports
  `.dsh/reports/reviewer/2026-08-19-logaudit-*.md`, consolidated
  `.dsh/reports/scribe/2026-08-19-cross-session-log-analysis-consolidated.md`)
  found ~271M tokens spent 95% on cache re-reads of inherited context,
  unbounded confirmation ping-pong, ghost posts, in-memory cursor replays,
  and self-asserted identity, the owner approved retiring the fork model
  entirely and restructuring to the "board-as-bus, departments = company"
  model. Commits: `46215d0` (A: host bus — retire dept_invoke/fork, global
  board tools for the host, hosts.json registry, raw host wake kind:'board',
  no anyParentId), `22b5af4` (B: permanent department heads — spawn fresh +
  toolFilter {allow:[]} lean + persona role + spatial deploy +
  dept_post_retire), `27e51ed` (C: wake-relay guard — ack:true flag +
  ack-loop suppression N>=3/T=120s + cursor dedup), `3c8be93` (D: persistent
  cursors.json high-water, ready single-once, fork-ghost sweep), `6fe60cf`
  (E: roster host liveness, whereami self-registration, sensitive/
  senderVerified trust signal), `83b4999` (F: O(1) board writes via per-room
  seq counter, boot compaction with mandatory cursor reset, dev-state
  clean), `72a86c4` (G: head lifecycle — dept_nap siesta keeps context,
  dept_memo_write journal, dept_sleep dormir resets context and respawns
  fresh with the journal in its first-turn start prompt; reviewer FAIL on
  respawn state-ordering fixed with deliver-before-commit). Each batch:
  builder → reviewer → TIERED ladder → commit. Final: build green, 51/51
  tests, `ready` single-once (1 record/room), dev state clean, GUI restarted
  on the new bus. Next: live test of the direct bus
  (Asistente↔heads↔Asistentes) and the nap/sleep lifecycle.

- **2026-08-19** — **Batch G follow-up: the slept head's journal rides the
  spawn START PROMPT (first-turn memory)**: the respawn path delivered the
  journal as a SECOND turn (a post-start `subagents.followup`), so a
  re-materialized head could answer its first wake before seeing its memory.
  Fix: `respawnAsleepPost` builds the spawn start prompt as the neutral
  `headPrompt(postId)` plus a sleep-resume section carrying the FULL journal
  text, and passes it to `startContinuable` — the real manager submits that
  prompt as the child's first inbox turn and resolves only after acceptance,
  so the start IS the delivery and still precedes the registry commit
  (read → build prompt → start → commit; on any failure the OLD child +
  sleepEpoch stay set and the next wake retries the respawn). The post-start
  journal followup is removed entirely; later wakes are pointer-only relays
  that never re-carry journal text (tests assert this, secret-phrase
  included). The fork-era lesson stands: the plain-user start prompt is
  framed as the head's OWN authored memory (neutral head identity + persona
  = role), never as a mission. Tests: the two Batch G lifecycle tests now
  spy `startContinuable` (success: journal + secret phrase in `inbox[0]`
  with source kind 'user', no separate followup; failure: rejected first
  start leaves the registry untouched, retry succeeds). Verification: build
  green, 35/35 invoke tests, 51/51 full suite, TIERED ladder green.
  Committed as `c39de68`.

- **2026-08-19** — **Live test battery PASSED (2026-08-19):** the restructured bus was exercised end-to-end in the live dev GUI: (1) direct host↔head channel — the Asistente host posted an assignment to research-head on the board (no fork), the wake relay woke the spawn head, the head replied, and the relay RAW-woke the host with the kind:'board' source; (2) spatial identity — dept_whereami returns kind host (address host-session-<uuid>) for the Asistente and kind post for the head; (3) nap/sleep lifecycle — the head wrote its journal via dept_memo_write, slept via dept_sleep (durable sleepEpoch), re-materialized as a FRESH incarnation (new childId, previousChildId chain) with the journal in its FIRST-turn start prompt (fix c39de68, found by the incarnation's own honest report that the pre-fix journal arrived as a second turn), and recovered a secret phrase that existed ONLY in the journal (0 occurrences on the board); (4) trust signal — the head saw [sensitive — sender verified: yes]; (5) ack-loop guard — terminal acks did not ping-pong; (6) TWO HOSTS LATERAL — the owner's second session registered host-session-8f1009e1, each host raw-woke the other bidirectionally (m-board-14/15) and both saw a consistent roster (2 live hosts). All autonomous tests were verified with independent evidence (registry files, session logs), not the agents' word. Commits: c39de68 (journal-first-turn fix) + deed386 (docs). Next: departmental multi-head/multi-post scenarios and the programming-department dogfooding on the new bus.

- **2026-08-20** — **Wake optimization (W1 + W2)**: audited and hardened the
  deterministic wake machinery for host and head agents. **W1** (commit
  `cfaffe9`, "fix(org): wake machinery — atomic journal write,
  dept_room_who global schema, host-sleep ordering (Batch W1)") shipped three
  fixes from the wake audit: (A3) the GLOBAL `dept_room_who` hosts output
  schema now declares `sleeping` (it was crashing with
  `additionalProperties:false` after host sleep — root cause was copy-paste
  drift from the head own-layer copy when Batch 7 landed); (A1) `writeJournal`
  is now atomic (tmp file + rename); (A5) host `dept_sleep` persists
  `sleepEpoch` only AFTER the in-place surface-reset append, closing the crash
  window (Session.append is synchronous, verified by reviewer). Regression
  test added; 97/97 tests and the TIERED ladder green; reviewer PASS; audit +
  fixes + builder reports `.dsh/reports/reviewer/2026-08-20-wake-audit.md`,
  `.dsh/reports/reviewer/2026-08-20-wake-fixes-review.md`,
  `.dsh/reports/builder/2026-08-20-wake-fixes.md` (all gitignored; filenames
  cited). **W2** (commit `109272b`, "feat(org): wake identity+cursor block in
  journals + version deepartments-workflow skill in repo (Batch W2)") adds a
  journal identity+cursor block (`wake_counter` monotonic, `last_wake`,
  optional `current_step`) plus a Wake-routine footer per journal; both
  `dept_memo_write` tools gained an optional `currentStep` parameter; 98/98
  tests and the TIERED ladder green; reviewer PASS (`w2-journal-review`). The
  lean wake routine ("Wake routine (injected wake)") — identity →
  read journal → board delta → health check → decide, loading
  AGENTS/ROADMAP-tail lazily — is encoded in the `deepartments-workflow`
  skill. Owner microdecisions 2026-08-20: audit+fix+optimize the wake
  (reviewer examined the wake sequence live; researcher investigated SOTA —
  `.dsh/reports/researcher/2026-08-20-agent-wake-best-practices.md`); scope
  "Completa" (lean routine + journal identity/cursor block + health check);
  the routine is encoded in BOTH the skill and the journal; the skill is
  versioned in the repo under `.dsh/skills/deepartments-workflow/` and both
  dev + stable presets symlink to it (backups preserved as
  `deepartments-workflow.bak-20260816/` siblings). Both DSH homes (dev
  `/opt/dsh/.dsh-dev` and stable `/opt/dsh/.dsh`) verified on DSH 0.1.0-rc.8 —
  AGENTS.md CLI pin updated (rule 8).
- **2026-08-20** — **Main agents sidebar**: the left DSH sidebar's workspaces region is replaced by a list of main agents (host row always first + department heads), shipped as a CLIENT bundle inside `dsh-deepartments` (fully reversible; shadows `sidebar.workspaces` at priority -1). Server: new `/deepartments` loopback RPC (`agents`/`list`) via `ctx.connection.rpc` (resolved optionally via `ctx.get('connection')`, skipped in headless profiles); pure status computation in `src/agents.ts` (precedence sleeping → completed-notice(unread addressed-to-host) → working(running) → idle); optional `title` display field on coordinators; new **Internal Programming** department (room `programming`, post `programming-head`, board member) and research dept renamed to Research Department. Client: `dsh.client` (web) + `exports["./client"]` + tsdown `build:client` wrapped by `scripts/normalize-client-banner.mjs`; host row "Assistant — User's Office" with DSH default status dots (ongoing/warning/done), head rows with lifecycle dots (spinner working, green completed-notice, gray idle, gray moon sleeping) fed by 5s+focus RPC polling; clicking the host row opens the current session (`ctx.sessions.open`) or starts a new one when none; the New Session button is hidden via CSS targeting `.hHd-Xa_newSession` ONLY — the shared aria-label selector was removed because the brand button bears the same aria-label and the logo disappeared. Commits: `9ead681` (feature), `bb7b734` (export `./package.json`), `bd82d67` (version client source — `/client/` gitignore anchored). Verification: `pnpm build`, `node --test` 63/63, build:client envelope checks, plugin add, dump-config `# == dsh-deepartments` layer with programming, headless smoke (`room ready: programming`), reviewer PASS. Dev GUI restarted twice with owner approval; `/plugins/dsh-deepartments/client.js` serves (HTTP 200) and the `/deepartments` RPC route is mounted.

- **2026-08-20** — **Deepartments Settings tab + trusted-host rework**: owner wanted a Settings tab with a slider to enable/disable the Deepartments UI, but the initial DSH settingsScope is loopback-only (owner on Tailscale → switch disabled). Reworked to persist toggle in `<stateDir>/ui.json` served over `/deepartments` RPC `ui/config` / `ui/config/set` with authority `trusted-host` (declared Tailscale hosts, loopback still allowed) so it works from any origin; Deleted `src/settings.ts` and `dsh-settings` deps. Client now drives a live gate polling `ui/config` and a segmented Enabled/Disabled selector (DSH options style). Commits: `30901ea` then `2c4ba9c` (rework).

- **2026-08-20** — **Agents-only sidebar + multiple Assistants**: sidebar made agents-only (removed Sessions section). Assistant sessions now appear as separate agents (Assistant, Assistant 2, ...) in creation order oldest-first, only after first message (blank filter), each clickable to open its session, active highlighted, collapsed dots per assistant. Subagents hidden (`origin !== 'subagent'`) and archived Assistants hidden via `archivedSessionIds` from `workspace.list` (rpc). Ordering fix: original Assistant stays on top, new below; restored ⋯ hover menu per Assistant with Archive agent via `workspaces.archiveSession`. Commits: `bdb972f`, `02f1c5b`, `37915b0`, `bf6e4e9`, `f768971`, `de367da`.

- **2026-08-20** — **Dept nap removal**: `dept_nap` was a no-op conclude marker; DSH wake relay / settlement injection wakes heads regardless, so the tool was redundant. Removed both host-plane and child-layer definitions, rewrote head spawn prompt, cleaned lifecycle comments, deleted Batch G nap test, renamed head status `napping`→`idle` across `src/agents.ts` and `src/client/index.tsx`. Commit `8c2c3fe`. 62/62 tests.

- **2026-08-20** — **Batch C (committed `f2454a1`)**: `feat(org) wake pack injected FRESH at agent/pre-step (message arrival) — Batch C timing` — the Deepartments wake-context pack moved from a frozen dept_sleep-surface append to a fresh agent/pre-step injection (reusing `assembleWakePack`/`buildWakePackMessage`, no dsh-core change); the pack now reflects live board-delta/git/roster/cursor at the moment the owner's message arrives, alongside the standard DSH injections (`@deepseek-ai/dsh-system-prompts` + skill-catalog). `dept_sleep` host surface reduced to a single journal node. Never-slept host degrades gracefully (no throw). P1 sentinel `pack-v1: present` + P2 KPI line (`wake_counter` + top-1 open item) added; journal footer deduped to a one-line pointer. 109/109 tests; reviewer PASS (arch GO, stale comments fixed). Loaded into dev + smart-restart 23:10; live re-validation GREEN (`pack-v1: present` + KPI visible at next wake).
- **2026-08-20** — **Batch B (committed `3ddec57`)**: `feat(org) head+worker wake_counter ordinal parity` — heads AND workers now bump `wake_counter` at their own-layer `dept_sleep` seed boundary (new `bumpPostSleepCounter`, atomic tmp+rename); `writeJournal` post/worker route unified to host-style `Math.max(prevCounter,1)` so the +1 happens only at sleep seed (was advancing on every write). 106/106 tests; reviewer PASS.
- **2026-08-20** — **Docs + config (committed `a48b2c1`)**: AGENTS.md + ROADMAP wake-routine label renamed 'Wake routine (deterministic orientation)' → 'Wake routine (injected wake)' (owner-approved); Session ritual START rewritten to injected-wake wording byte-identical to the canonical skill text. Plus audit finding C1 fixed in the dev host persona (rule 8 → injected wake; config outside repo, mirrored to stable at deploy time). Context-injection audit PASS (coherent composite; skill-catalog only injects the 1-line summary — no full-body duplication; routine text deduped to one source). Note on the audit: C1 HIGH resolved in the dev persona; C2 LOW-MED is dsh-core/out-of-scope; P1/P2 folded into Batch C.

- **2026-08-20** — **Dev-only trial LLM route rolled back**: a trial LLM provider route that a previous session had extended to the department heads and the delegation tools was reverted from the hot path. The trial model rejected the harness's reasoning-effort mapping (the `none` wire value is unsupported) and returned HTTP 400 before generating any token, so the rollout never ran a single real turn. Department heads are back to `deepseek-v4-flash` on `opencode-go` (commit `e38f339`); the delegation rows are back to `deepseek-v4-flash` in the dev preset. The trial route stays declared only in the dev harness home (outside this repo), off-by-default. Reviewer PASS recorded in the gitignored `.dsh/reports/reviewer/` (2026-08-20).
- **2026-08-20** — **W3 (committed `ba5c24c`)**: `fix(org) host wake_counter advances at the dept_sleep seed boundary (Batch W3)` — ordinal now correct (wake 2 saw 1 before; the one-behind gap closed); builder+reviewer PASS; tests 98/98.
- **2026-08-20** — **W4 (this unit)**: `feat(org) wake context pack` — Deepartments context injection at host wake: identity/journal path/board delta/condensed roster/git/system state/full workflow skill injected pre-thought (owner doctrine: no lazy for what will always run); + `dept_wake_snapshot` on-demand; wake routine rewritten. Live re-validation GREEN at the 2026-08-20 23:14 host wake (host-session-cf5225e4…, seeded journal `wake_counter: 4` on disk, KPI `wake_counter 4` present in the injected pack) and confirmed on stable via the headless twin smoke (`pack-v1: present`).
- **2026-08-20** — **STABLE deployed (Batch 6 + W1..W4 + B + C)**: stable instance `dsh.service` (3080, DSH_HOME=/opt/dsh/.dsh, profile `web`, bundle `dsh-deepartments` link:repo) reloaded — `pnpm build` tsc clean, dump-config shows `# == dsh-deepartments` layer, `systemctl restart dsh.service` active (HTTP 200, marker.json updated); tests 109/109. C1 persona full-sync dev→stable (backup `.bak-20260820`; injected-wake START rule 8 now live in stable). Stable headless twin `/opt/dsh/.dsh/profiles/headless-deepartments/` created (mirror of dev twin) for automated CLI smoke. Re-validation on stable: twin smoke post-restart → `pack-v1: present` + identity + `kpi (unavailable)` (never-slept degrade by design, Batch C); real host wake seeded with `wake_counter 4` + KPI present (GREEN). Reviewer wake-compare PARTIAL → effectively GREEN (flagged anomalies were the never-slept SUBAGENT session's own pack, not the host wake); out-of-scope follow-up: optional KPI fallback to adjacent seeded journal for never-slept agents.
- **2026-08-21** — **Batch S1: Session Memory Archive** (committed `7418b69`): append-only per-member entry history under the journals tree (one archive file per member, `<memberId>.md`, unique per-write delimiter) + one-cycle automatic session log per wake (one file per `<memberId>-<wake-counter>` cycle; in-process `sessions.flush` + `sessionPersistence.readRaw` via OPTIONAL `ctx.get`, exact `seq > boundarySeq` slice with time fallback, bounded caps MAX_TOOL_ARGS 800 / MAX_TOOL_RESULT 2000 / MAX_TEXT 2000 / 512 KB, stub+warn never-throw) + per-member mutable `journals/index.json` (atomic tmp+rename, last-write-wins) captured from the writeJournal/bump* choke point; journal citations `archive_seq`/`session_log` marker lines (KPI regex lines untouched); `boundarySeq` (`session.seq`) persisted at both dept_sleep sites; inject array stays `['tools','sessions','sessionProjections']` with services resolved optionally (owner-ratified deviation); spec materialized as `docs/specs/001-session-memory-archive.md`. RAG wired (config, outside repo — T2/T2b): a `journals` embeddings database added to dev+stable personae AND both GUI profile patches (tool-web-enhanced `rag.databases = [reports, journals]`, embeddings remote via `apiKeyEnv DEEPINFRA_TOKEN` at runtime → zero-401 smoke when the token is exported from the systemd unit env; backups `.bak-20260820-t2`/`.bak-20260821`). 5 new tests (→114), reviewer PASS. Headless observation: CLI one-shot calling dept_sleep does not terminate cleanly (related to the T4 subagent guard).
- **2026-08-21** — **T4: role-focused Deepartments injection for subagents** (committed `4b1713d`): `subagent`/`subagent_fork` accept an optional `role` param (builder|reviewer|researcher|scribe|explore, default generic) carried via an in-process `roleRegistry` (dsh-session strips non-whitelisted meta fields, so `meta.role` is not an option; cold-resumed children fall back to generic). The agent/pre-step injector now branches on `session.header.meta.origin === 'subagent'` → injects ONE slim role contract block (new `src/role-orient.ts`) INSTEAD of the full host wake pack (~4.6–4.9k tokens saved per subagent; host + registered posts keep the full packet unchanged). Global `dept_sleep` REFUSES subagent-origin callers (guard before the host branch). SKILL.md dispatch templates rewritten to the light form (objective+files+spec+verification; "your role contract is injected"). Flaky test 74 (Batch 7 host dept_sleep) hardened with waitFor. `node --test` **117/117** (114 + 3 new), build clean; reviewer PASS. Two non-blocking follow-ups: evict `roleRegistry` at child settlement; comment-accuracy nit in role-orient.ts.
- **2026-08-21** — **Wake-5 session hardening (4 commits, all reviewer PASS; tests 120/120)**: closes the T4 follow-ups, fixes the pre-existing test-93 regression and the S1 live session-log capture, and amends the wake routine from the wake-5 audit. (1) **T4 follow-ups closed** (commit `bbdd2b5`): `roleRegistry` now evicted at child SETTLEMENT via the dsh `subagent/end` lifecycle listener (module-scope, single registration; guards non-string ids, never throws) + a `forgetRole` helper; comment-accuracy nit fixed (SKILL dispatch templates = handoff contract, ROLE_CONTRACTS = injected discipline rules — complementary, not byte-identical). (2) **Pre-existing test-93 regression fixed** (commit `aab6e5c`): the S1 ROADMAP entry was reworded so the wake-packed ROADMAP tail carries no literal session-log/archive path patterns — test stays strict, 118/118. (3) **Wake-routine amendment** (commit `34e8a5e`, wake-5 audit): hard REPLY-FIRST rule (first wake output = owner-facing greeting + ≤5-line plan + explicit "what do you want this session?" ask before ANY tool call), permission gate (plan is PROPOSED, not authorized — no subagent dispatch, codebase exploration, or item start until the owner answers), grounding cap (≤1–2 reads of a journal-referenced report; zero src/checkout exploration before go-ahead) — mirrored in the HOST_WAKE_ROUTINE_TEXT / HOST_WAKE_NEXT_STEP constants and the SKILL.md checklist (rule 0). (4) **S1 session-log live-capture fixed** (commit `e38163e`): root cause — `captureSessionLog` extracted `flush`/`readRaw` and invoked them UNBOUND, losing `this` inside the real dsh-session implementation → every live session log degraded to the stub (`Cannot read properties of undefined (reading 'liveEntryFor')`); fixed with bound method calls; +2 tests (→120/120).
- **2026-08-21** — **Wake-6 environment upgrade + S1 live verification** (tests 120/120; docs-only entry): (a) **Environment upgraded**: dsh core `0.1.0-rc.8` → `0.1.1-rc.1` (npm global reinstall of `@deepseek-ai/dsh`, published 2026-08-21 — brings the Bubblewrap security fix, vision-exp support and subagent improvements) and the `dshmarket` plugin `1.16.2` → `1.17.1` in the `web`/stable and `deepartments-dev` profiles (via `dsh plugin add dshmarket@1.17.1`); pre-upgrade backups under `/opt/dsh/backups` (tarballs `*-20260821-1117`). Verified: `--dump-config` clean in both profiles (`# == dsh-deepartments` layer present), smart-restart clean, GUIs 3080/3090 respond. (b) **S1 verified LIVE**: first REAL capture of the session log — the wake-5 cycle materialized with a full transcript (159 KB) under the ordinal bumped at rebirth, exactly as the open item predicted; the `wake_counter 6` ordinal matches triple (journal = wake pack = session log). The dept_sleep stub stays flagged as a P2 follow-up (explore in progress). (c) **Ecosystem research (2026-08-21)**: `dsh-store@0.5.2` is a NON-official community plugin and NOT adopted (we stay on dshmarket); `deepseek-v4-flash` is DeepSeek's latest flash model (checkpoint DeepSeek-V4-Flash-0731, no successor) and is available on opencode-go with the same ID as the direct API — our config is already the correct one; the experimental multimodal variant `deepseek-v4-flash-vision-exp` is only reachable via the direct API (not curated on opencode-go). No RAG/embeddings changes in the core.
- **2026-08-21** — **Wake-9 environment upgrade: dsh 0.1.1-rc.1 → 0.1.1-rc.2 (dev only)** (tests 122/122; docs-only entry): (a) **Core upgraded**: `@deepseek-ai/dsh@0.1.1-rc.2` installed via npm global reinstall (456 packages changed; rc.2 is the npm `latest`, published 2026-08-21 12:42Z, ~6h after rc.1) — the delta is an image/attachment/vision pipeline rework (DeepSeek adapter uploads images via the Files API first with file-id reuse, inline-base64 fallback, deterministic canonical attachment encoding, `read_image` gains sourceWidth/sourceHeight); researcher verified NO breaking changes across config/profile loading, agent-default-model/provider settings, subagent tool-calling, session-log, smart-restart and plugin manifest — no migration steps; dshmarket stays at 1.17.1 (already newest). (b) **Fix B re-applied to rc.2**: the orphan `role:'tool'` sweep was ported to the new `serializeMessages`/`serializeMessagesWithImages` codebase (Set + collect + guard; in the image path the guard wraps the ENTIRE result body including `pendingToolImages`) — 13/13 simulation asserts, reviewer PASS, clean sequences byte-identical; pristine pre-edit backup and the rc.1 patched file preserved under `/opt/dsh/backups` (the fix lives in the global npm install and is lost on every upgrade). (c) **Verification ladder**: plugin add up to date + `--dump-config` shows the `dsh-deepartments` layer; real headless smoke in the headless twin (web_fetch tool-call round-trip against the strict direct API) → 200, no 400s. (d) **Restart + post-boot**: dev service restarted 13:37:18 UTC via smart-restart (boot on rc.2, GUI 3090 HTTP 200), and the live session log confirms the resumed session still runs `deepseek-official` / `deepseek-v4-flash-vision-exp` (direct API, reasoningEffort max). (e) **Observation (P2)**: rc.2 changes the `read_image` coordinate semantics for downscaled images (now uses the reported scale multiplier instead of assuming 1:1) — relevant to our vision-exp screenshot workflows; stable profile untouched (stays on rc.1).
- **2026-08-21** — **Wake-9 live verification** (tests 122/122; docs-only entry): (a) **S1 session-log capture REAL**: the wake-8 cycle materialized under the ordinal bumped to 9 with a full 51 479 B transcript (12:41:46→12:57:28 UTC, frontmatter wake_counter 9) — no stub; at wake-9 birth the file carries the previous-cycle seed, exactly as the wake-8 file did with the wake-7 cycle at its verification. (b) **Runtime model continuity**: the dev service process (port 3090) started 12:42:32 UTC and has NOT restarted since wake 8, so the runtime descriptor remains `deepseek-official` / `deepseek-v4-flash-vision-exp` (direct API, reasoningEffort max) by continuity from the wake-8 live verification (turns 163+); no smart-restart notice in wake 9. (c) **Capture finalization at sleep confirmed**: `-8.md` now holds the wake-8 own cycle (51 130 B, 12:41:46→12:57:25 UTC) — the ordinal-bump capture rewrites the file at sleep with the current cycle, consistent with the -6/-7/-8 pattern. (d) **No new findings**: the P2 queue is unchanged; the wake-9 invariant open item is now closed.
- **2026-08-21** — **Wake-8 live verification** (tests 122/122; docs-only entry): (a) **Runtime model descriptor LIVE**: the dev service restarted (12:42:34 UTC); from the third post-reboot turn onward every LLM request of the resumed session carries `deepseek-official` / `deepseek-v4-flash-vision-exp` (direct API, reasoningEffort max) — confirmed in the live session log; caveat: the first two post-reboot turns still used the cached pre-restart config (`opencode-go` / `deepseek-v4-flash`), the new default took effect from the next turn. (b) **T4 slim-injection fix (commit `279ce10`) LIVE**: the first wake-8 subagent (an explore verification probe) received the slim `## Deepartments context` block (~750 chars — role identity + role contract + reporting) instead of the previous full 25 313-char host wake pack — no journal/ROADMAP/board_cursor/wake_counter markers present. (c) **dept_sleep subagent-origin guard LIVE**: a subagent attempting `dept_sleep` is refused with the expected loud rejection ("dept_sleep is refused for a transient delegated subagent — a subagent cannot sleep; its task ends with the settlement notice"). (d) **S1 session-log capture REAL**: the wake-7 cycle materialized under the ordinal bumped to 8 with a full 123 432 B transcript (11:36:10→12:41:46 UTC) — no stub. (e) **End-to-end flip**: the verification probe itself ran on vision-exp with real tool calling against the strict direct API — no 400s — the flip is functional end-to-end.
- **2026-08-21** — **Wake-7: dev-profile default-model flip to vision-exp on the direct API + root-cause fixes A/B** (tests 122/122; docs-only entry): (a) **Model flip (owner-approved, outside the repo)**: the dev profile default model is now `deepseek-v4-flash-vision-exp` via the DIRECT DeepSeek API — provider `deepseek-official`, reasoningEffort `max` kept, headless twin pinned the same; dev-only, the stable profile is untouched. (b) **Root-cause Fix A (commit `7333ea5`, in repo)**: `dept_sleep` now defers the surface replace so the host close no longer leaves an orphaned `role:'tool'` message on wake — 121/121 tests (+1 regression test), reviewer PASS. (c) **Fix B (outside the repo, re-apply after each dsh upgrade)**: the orphan sweep in the DeepSeek LLM provider's message serialization (plain and image-variant paths) lives outside the repo and must be re-applied after every provider/dsh upgrade. Diagnosis: the orphaned `role:'tool'` generated by the host dept_sleep close trips the upstream strict validator as a direct-API 400; the pre-flip strict-API smoke PASS (workflow agent on vision-exp with tool calling) confirms the fix end-to-end. (d) **T4 subagent-injection discriminator dead-code fixed** (commit `279ce10`): the live owner audit found the role discriminator read `header.meta?.origin`, but dsh-session flattens meta to top-level `header.origin` — so EVERY subagent received the full host wake pack instead of the slim role block (real session-log capture: one 25 313-char pack, zero slim blocks) and the subagent dept_sleep guard never fired; the fix reads the flat field, with a real fixture and a regression test through the real Loader — 122/122, reviewer PASS. This session closes with a dev service restart; wake 8 must verify live BOTH the runtime model descriptor and the slim subagent injection (the next wake materializes on the new model).
- **2026-08-21** — **Wake-11: corrupt session log root-caused + fixed + recovered** (owner-reported dev error "Failed to load history: history unavailable for session session-cf5225e4-4261-4eba-bb34-733864243587: corrupt Zstandard session log: complete frame contains a torn JSONL record"): (a) **Diagnosis** (explore: `.dsh/reports/explore-deep/2026-08-21-corrupt-session-log-diagnosis.md`, confidence HIGH): NOT a torn tail — the artifact was 581 complete zstd frames ending at EOF with a complete last record; the failure is a SINGLE MID-LOG seq discontinuity at decompressed line 6 (the cleanup's minimal artifact events 0..3, then a jump to seq 405164) — raised by the scanner's commit-continuity check (`dsh-session-persistence-jsonl lib/index.js:984`); the seam was born 15:39:23.36 UTC, 1 s after the cleanup backup, and both service stops were graceful ("Deactivated successfully") — so it is OUR wake-10 web-GUI cleanup (`runSleepCleanup` truncate step on a session ALREADY materialized/live 24 s earlier; only the child-archive step had a live guard), not an external crash. (b) **Recovery (owner-approved Option A, full history)**: forensic copy of the corrupt artifact (184,279 B → `archive/…-corrupt-20260821-180114.jsonl.zstd`); restored the verified pre-cleanup backup (14,198,147 B, 41,161 frames, 54,620 lines, 0 seq gaps, ends seq 403490) over the live artifact; dev service restarted; boot clean and append-only growth resumed (first 54,620 lines md5 == backup; last record seq 403493 `session/end-seed`); wake-11 tail (seqs 403491–416663, turns 193–194) intentionally dropped per owner decision — preserved in the wake-11 journal + subagents archive tar. (c) **Root-cause fix (commit `196eb75`)**: `runSleepCleanup` now returns a skipped report BEFORE any mutation when `opts.isLive(hostSessionId) === true` (mirrors the archive-step guard), `truncateSessionArtifact` gets a defensive direct-call guard, and `runPendingWebUiCleanups` clears `webUiCleanupPending` only behind the new pure gate `shouldClearCleanupPending` (skipped-live or failed truncate → flag KEPT, retry next boot); 136/136 tests (+3 regression: live-skip holds artifact byte-identical, direct-call refusal, gate truth table), reviewer PASS (`.dsh/reports/reviewer/2026-08-21-fix-cleanup-live-guard.md`); fix LIVE in dev (plugin re-added, restart 18:13:26 UTC — clean boot, 0 error matches). Remaining caveat: the boot-before-materialize ordering assumption still exists (hook runs after resume relay in the fast path) — mitigated by the skip-and-retry semantics, next real test is the next sleep→boot cycle of a live host session.
  **Post-fix LIVE validation & P1 closure (owner-confirmed, 2026-08-21 ~19:00 UTC)**: after the fix restart (18:13:26, bundle with `196eb75`), the dev GUI reloads cleanly — owner visual confirmation: full restored history visible, no `corrupt Zstandard` error (expected with Option A recovery; the journal-only look is NOT the current state, it is deferred). Server-side evidence: live artifact healthy append-only (14 297 355 B, 54 910 lines, last record seq 408360 at 18:31, prefix md5 matches the restored backup); `/opt/dsh/.dsh-dev/archive/` holds the complete incident evidence set — `session-…-corrupt-20260821-180114.jsonl.zstd` (184 279 B forensic copy), `session-…-pre-cleanup-20260821-153922.jsonl.zstd` (14 198 147 B verified restoration source, 0 seq gaps), `subagents-…-2026-08-21T15-39-25-701Z.tar.gz` (38 557 158 B); `hosts.json` flag cleared (the 15:39:22 cleanup run completed fully: truncate+backup+projcache-reset+subagent archive; 397→260 session dirs = 137 archived); scan found NO remaining host-child subagent dirs (the 4 extra `cf5225e4` mentions in other dirs are content-only, no `parentSession`/`origin` schema). **P1 CLOSED.** The visual-cleanup outcome (journal-only chat, zeroed stats, empty subagent list) is re-scoped to the next real sleep→boot cycle: the boot hook will run `runSleepCleanup` with the live-guard (`196eb75`) — if the host session is already materialized it SKIPS before any mutation and KEEPS `webUiCleanupPending` for the next boot (skip-and-retry, no corruption possible); expected: journal-only at the first post-sleep boot where the host is not yet live. Wake-12 validates that cycle.
- **2026-08-21** — **Wake-10 session** (three units): (a) **Fix B folded**
  (commit `f9ffdcd`): `patches/dsh-llm-deepseek-orphan-sweep.patch` +
  `scripts/reapply-dsh-patches.sh` (fingerprint-gated: PASS/NOT-APPLIED/drift-FAIL,
  idempotent, stable-home guard) + `patches/README.md`; verified 11/11 + ladder
  (smoke OK); reviewer PASS. Correction surfaced: dev and stable SHARE the single
  global CLI rc.2 — stable's process still holds rc.1 in memory (untouched); a
  future stable restart boots rc.2. (b) **Context-injection analysis**: wake-8 vs
  wake-10 boot diff → root cause is commit `7333ea5` (Fix A surface-deferral), NOT
  rc.2 (runtime byte-identical for injection packages): the DSH snapshot +
  skill-catalog re-emit only when their previous node leaves the surface, so they
  arrive at the owner's 2nd message instead of the 1st; all 19 wake-pack sections
  present at both boots; owner decision: leave as-is, documented (report
  .dsh/reports/explore-deep/2026-08-21-wake-context-injection-diff.md). (c)
  **Web-GUI cleanup at sleep** (Option A plugin-side, owner-approved: reset total,
  log guardada): dept_sleep sets durable `webUiCleanupPending` (hosts.json); first
  boot after sleep truncates the host session artifact to journal-only (WITH
  full-artifact backup to <stateHome>/archive/), resets session_projcache rows,
  archives+deletes child subagent session dirs (idempotent, atomic, best-effort);
  129/129 + new backup test; reviewer PASS (deviation sanctioned: truncation moved
  out of dept_sleep — seed-contiguity proof dsh-session lib/index.js:1381 +
  persistence:835). Caveat: boot-before-materialize ordering verified by
  construction — first LIVE validation is the wake-11 sleep→restart.

- **2026-08-22** — **Wake-13: dev crash-loop root-caused + recovered + repair-script framing fix** (owner-reported "no puedo conectarme al dsh dev"): (a) **Incident**: `dsh-deepartments-dev` en crash-loop desde 2026-08-21 20:06 UTC (restart counter 4840, ~9 s/ciclo) — cada boot moría en el init del workspace con `corrupt Zstandard session log: first frame is not exactly one header line` (dsh-session-persistence-jsonl lib:742 `assertZstdHeaderFrame`); causa raíz: ejecución de `scripts/repair-session-renumber.py` (commit `37be8b3` original) sobre el artifact del host `session-cf5225e4…` (a las 20:06:23, backup `.pre-repair-20260821-200623.jsonl.zstd` de 730 KB) — su `compress_file` re-comprimía TODO el log reordenado en UN único frame zstd, violando el invariante dsh (primer frame = exactamente la línea header); el gate del script (sha round-trip del stream) no cubría el invariante → artifact de 279 KB inusable y servicio imposible de arrancar. (b) **Diagnóstico**: validación autoritativa de los ~250 artifacts del dev home con un port 1:1 del escáner real (`scanZstdFrames` primer frame + `SessionLogScanner`/`decodeStorageRecord`, regla `seq == índice expandido`): solo 1 corrupto (el reparado); el backup pre-cleanup 18:45:38 (14 642 459 B) OK — 430 438 eventos, termina en `turn/end` del turn 198; el pre-repair 730 KB FAIL (seam `expected 4, got 430449` — truncado por el cleanup del sleep; por eso se intentó reparar). (c) **Recuperación (precedente wake-11 Option A)**: restaurado el backup full 18:45:38 como artifact live; forense del corrupto en `/opt/dsh/.dsh-dev/archive/…-broken-repair-20260821-200623.jsonl.zstd`; dev UP verificado (HTTP 200 en :3090, NRestarts=0, tailscale serve 8445→3090 intacto, boot completo pasa el workspace init que valida todos los artifacts). (d) **Fix durable (commit `36a0cf5`)**: `compress_log_file` (frame 1 = header re-escrito solo; body en frames ~512 KiB independientes con checksum — módulo `write_checksum=True` con fallback TypeError documentado; CLI `zstd --check` porque `--checksum` es rechazado por zstd 1.5.5) + gate `verify_artifact_framing` ANTES del `os.replace` (port de `assertZstdHeaderFrame` + `scanZstdFrames` + re-scan de continuidad estricta sobre el artifact comprimido final; falla = RepairError rc 1, artifact intacto); `renumber`/`expand_record`/`verify_strict` sin cambio de semántica; `scripts/README-REPAIR.md` + sección "Output format (zstd framing)"; **12/12 tests nuevos** en `scripts/test-repair-renumber.py` (todo en tempdirs), incl. el caso del incidente (artifact de 1 frame → el gate lo rechaza con el mensaje exacto del boot) y red-con-stash comprobado por el reviewer; reviewer PASS (2 nits no bloqueantes anotados: bounds-guard del frame-descriptor para artifact de 4 bytes → IndexError en vez de RepairError; `--check` incondicional en la ruta CLI, fails-safe). (e) **Pendiente opcional**: recuperar los turns 199–211 (seqs 430449+) desde la copia `.pre-repair` de 730 KB con el script corregido — decisión del owner.
   **Pendiente RESUELTA (decisión owner, 2026-08-22)**: los turns 199–211 se dejan COMO EVIDENCIA — preservados en la copia `.pre-repair` (730 KB), en los forenses de `/opt/dsh/.dsh-dev/archive/` y en el capture del journal de sesión 12; recuperables en cualquier momento con el script corregido (`36a0cf5`, framing conforme + gate verify) en una ventana planificada, si el owner lo pide.

- **2026-08-22** — **Wake-12 continuation: clean-chat UX (sidebar Asistente) + dsh-smart-restart v0.4.0/0.4.1 canary + awesome-dsh-plugin submission**: (a) **Clean-chat UX path (owner decision)**: instead of the truncation/cleanup machinery, the owner's play — the sidebar shows the workspace's blank/new session as `Asistente` (commit `caf15e6`, plugin-side `src/client/index.tsx`: `!s.blank` un-hidden; blank rows labeled with the host RPC name; non-blank labels unchanged; 140/140, reviewer PASS; live in dev after restart 10:03:02). Explore-verified: blank sessions have NO artifact (lazy persistence — file only on first append), no auto-close/GC, tab close disposes nothing; the old session stays fully intact (log preserved). On first message the blank becomes the new host (wake pack injection unchanged — owner-approved, old host stays dormant; no host-identity conflict). (b) **dsh-smart-restart v0.4.0** (`9890d83`, 57/57, reviewer PASS, npm-published): optional canary gate on `smart_restart` — `canary` param (per-call, default from config `canary: false`); `runCanary` derives binary/profile from the systemd unit ExecStart (generic), builds a temp patch overlay (this plugin disabled + `canaryStateDirOverrides` rows' stateDir → tmp — verified against the dsh-deepartments bundle row id `deepartments`; dump-config composition tested twice), pre-flight `--dump-config`, ephemeral boot on an auto-picked port, HTTP liveness probe (45 s), process-group kill + temp cleanup; FAIL → restart ABORTED (no pending-notice persist, no spawn) + live caller alert (plugin-source notice); derivation impossible → `skipped` (never blocks — generic installs safe). Genericity audit (Q8): zero env-specific runtime code, only `restartUnit` needed, tests env-free. Dev profile configured (`canaryStateDirOverrides: {deepartments: ''}` + `.bak-20260822-canary` backup) and **LIVE SMOKE PASS** (2026-08-22, 10:45 UTC): canary→`Canary: passed — restarting…`, real restart, zero residue (no temp dirs, no stray processes, marker clean). (c) **v0.4.1** (`c9fc515`, 59/59, reviewer PASS, npm-published): single-line canary-failure render, quoted patch `stateDir` paths, hoisted merged `smart-restart` row block (pre-existing mixed-overrides bug found by the builder + fixed + regression test). Repo bar for the list: LICENSE (MIT), GitHub Actions CI workflow, 10 commits, `dsh-plugin` topic set via API. (d) **awesome-dsh-plugin submission**: PR [#2726](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin/pull/2726) — `data/plugins/edusrez__dsh-smart-restart.yml` (category `notify`; description verified line-by-line against v0.4.1 source; READMEs regenerated — 1838 entries, entry at line 1490; `generate-readme.mjs --check` passes; awesome-lint 0 warnings on the entry). Fork `edusrez/awesome-dsh-plugin` + branch push + topic via the GitHub token at `/root/.config/gh.token` (fine-grained, cannot open PRs on upstream — `Pull requests: write` missing; owner opened PR #2726 with the official template, all boxes verifiably ticked). (e) **Other**: Deepartments repo pushed (`origin/main` = `caf15e6`; 54 commits pushed, 8 of them this session's work); wake-13 turns 199–211 kept as evidence (commit `416ac1a`); the sidebar change replaces the journal-only cleanup goal (machinery stays as defense-in-depth with the 196eb75/1fd4543 guards).

- **2026-08-22** — **Wake-13/14 — host-session rotation FIRST LIVE TEST: split-brain incident + 7-failure post-mortem + deterministic host-selection fix** (owner report "Parece que ha funcionado, ¿no?" → actually PARTIAL; continuation of the prior-wake entries "clean-chat sidebar UX" — the wake-12 continuation entry — and the "U1/U2/U3" units, sidebar removal / server-side rotation / client watcher): (a) **Incident**: the wake-12→13 rotation live test (host `dept_sleep` at 14:52:02 UTC) — the rotation STATE MACHINE itself worked end-to-end: journal re-keyed to `host-session-1122cd45…` (wake_counter 13), S2.5 archive, S2.7 evidence copy, contiguous cycle log seq 430436→604029, old artifact byte-intact. BUT the owner's next message (14:52:33) landed in the OLD blank tab `ba01f1a4…` (created 08-21 11:36, deepartments preset) instead of the rotated session `1122cd45…`, which never materialized — its artifact holds 4 seed events and 0 turns (nobody ever typed there). (b) **Root-cause chain — 7 failures** (full evidence `.dsh/reports/explore-deep/2026-08-22-rotation-postmortem.md`): (1) a stray live entry `host-1a4af1ea…` (dead 09:34 session) survived everything — nothing enforces the single-live-host cardinality (the loader validates field types, not cardinality); (2) `buildHostStatusPayload` picked the FIRST non-retired entry in Map order (invoke.ts:879-893; hosts wired as a single-use `hosts.values()` iterator at invoke.ts:4639) → the `/deepartments` RPC reported `hostSessionId=1a4af1ea…`, never `1122cd45…`; (3) the U3 watcher therefore could never open `1122cd45…` — it actively opened the stale `1a4af1ea…` (shouldOpenHostSession satisfied, index.tsx:124); (4) no routing into `1122cd45…` (blank + not-current ⇒ invisible under the sidebar rule; archiving cf5225e4 cleared the selection) — the owner typed in the open dormant tab; (5) `ensureHost` re-registered a SECOND live host — `ba01f1a4…` became a bare live entry at 14:53:34 via its own dept_whereami (only RETIRED re-registration is refused, not new-registration-while-a-live-host-exists) = the durable split-brain; (6) the wake pack was injected into the WRONG host — `ba01f1a4…`'s first pre-step after registration (pre-step 8, 14:53:34.287, after 7 pack-less steps) delivered the full pack DEGRADED (`wake_counter (unavailable)`, no journal — forensic `.dsh/reports/explore-deep/2026-08-22-this-wake-log-forensic.md`); (7) consequences compound forward: the roster lists three hosts, board wakes addressed to "the host" are ambiguous, and a future dept_sleep from `ba01f1a4…` would throw "requires a saved journal". (c) **The gate is NOT a regression**: the injection-timing comparison across wakes (`.dsh/reports/explore-deep/2026-08-22-injection-timing-across-wakes.md`) proves the pack-at-pre-step-1 norm holds for the REGISTERED host pre- and post-`bfe50ff` (the 12:35/13:53/14:50 boots all delivered pack at step 1 with a real KPI `wake_counter 12`); "pack only after self-registration, degraded" is the INTENDED post-gate semantic for never-registered tabs (owner-approved) — pre-gate, the same unregistered tab would have received the polluting pack at step 1. (d) **Fixes applied**: (1) code — deterministic live-host selection `pickLiveHostEntry` (successor `previousSessionId` → single-live → first-in-order + `ambiguous` warn) plus re-iterable hosts wiring (the single-use `hosts.values()` Map-iterator exhaustion caught by the reviewer: CALL2 `hostSessionId:null`; fresh `[Symbol.iterator]` wire at invoke.ts:4712) — commit `21d148b`, build clean, 165/165 tests, reviewer PASS ×2 (re-review `.dsh/reports/reviewer/2026-08-22-host-status-live-selection-fix-re-review.md`); (2) state — split-brain cleanup of `/.deepartments/hosts.json`: stale `host-1a4af1ea…` + ghost `host-session-1122cd45…` retired with `rotatedTo=host-session-ba01f1a4…` (their fields preserved), the pre-fix file backed up at `hosts.json.bak-20260822-splitbrain-cleanup`, exactly ONE live host restored (`ba01f1a4…`); builder report `.dsh/reports/builder/2026-08-22-hosts-splitbrain-cleanup.md`. (e) **Owner decisions**: recovery path = formalize `ba01f1a4…` as the host (dept_memo_write → dept_sleep → rotates into a fresh session C); code-fix scope = minimum. (f) **Open P2 follow-ups** (fair wording): ensureHost new-registration guard while a live host exists (post-mortem #5); loader single-live cardinality; roster of retired hosts (R-Q3) / orphaned members (R-Q5); a note on the re-keyed journal wake_counter lineage; next live rotation test = the sleep following this entry (verify pack at pre-step 1 on the rotated session AND the U3 watcher opens it).

- **2026-08-22** — **Wake-14/15 — 2º live rotation test: SEGUNDO TEST PASS + resume-live race fix chain (FIX 1/1b/1b.1)**: (a) **Incident** (owner report "al intentar hacer la rotación"): el test 2 (host `ba01f1a4…` dept_sleep 16:19:52 UTC) rotó bien (journal re-keyed a `host-session-6e49895c…` wake_counter 2, hosts.json v2 único live + retired/rotatedTo, S2.5 archive + S2.7 evidencia) PERO la sesión C nunca despertó: el GUI mostró `resume failed for session "session-6e49895c…": Error: cannot prepare session … while it is live (internal)` — artifact C = 5 eventos de seed, 0 turns. (b) **Root cause (8º failure mode, spec-level)**: S2 creaba la sesión con el store LIVE (`ctx.get('sessions').create` — attached-but-agentless): el resume es contractual-cold y el live-guard de `dsh-session-persistence` (lib/index.js:852) rechaza cualquier id presente en `ctx.sessions`; el harness nunca produce ese estado (su create RPC empareja agente+sesión en la misma llamada). Evidencia completa `.dsh/reports/explore-deep/2026-08-22-rotation-resume-live-race.md`. (c) **Fixes (owner-approved)**: (1) **`d397354`** — S2 cold-seed vía seam `sessionPersistence` (`create(meta)`+`append(seed)`; artifact frío, sin attach en memoria; `session/end-seed` lo re-añade el ctor en el primer resume y `toHeaderLine` mantiene el header byte-equal; fallback legacy preservado) — 165/165, reviewer PASS; (2) **`296ac24`** — S2.2 **workspace attach** (canonical parity con apiproxy:2539; FATAL sin resolución → legacy fallback: nunca un host registrado-invisible) + **boot repair hook** (único host non-retired, idempotente) — 171/171, reviewer PASS; (3) **`20198f1`** — el hook de boot corría antes de que el fiber proveedor del workspaceRegistry llegara a estado 2 (cordis strict-get `ctx.get` → undefined: única salida silenciosa) → non-strict get + retry 250 ms ≤10 s — 172/172, reviewer PASS; 3 restarts con canary PASS. (d) **Recuperación + cierre del test 2**: tras el 3er restart el repair adjuntó `6e49895c…` al workspace root (`storages/workspace.json` prepend 17:34:27) → fila en el sidebar; el owner abrió la sesión y escribió "Hola" → artifact: **wake pack `pack-v1` presente en PRE-STEP 1 con KPI real (`wake_counter 2`, journal path pre-resuelto, identidad host-session-6e49895c…)** ANTES del primer `request/header` (147 eventos, turn completo) — **TEST 2 PASS** (verificación del follow-up (f) del post-mortem wake-13/14; el watcher U3 no fue el camino — el owner abrió la fila manualmente, baseline-first-observation — aceptable). (e) **P2 follow-ups abiertos del post-mortem siguen**: ensureHost new-registration guard mientras exista un host live (nº5), cardinalidad single-live del loader, roster de hosts retired (R-Q3) / miembros huérfanos (R-Q5), nota de lineage del wake_counter re-keyed.

- **2026-08-22** — **U4 — host-session title "Asistente" (plugin-side `session/title` pin, zero DSH-core changes)**: host sessions now display the pinned title **"Asistente"** in the sidebar. DSH sessions have no `name` field — the row label IS the title projection, folded last-wins from `session/title` log events (design: `.dsh/reports/explore-deep/2026-08-22-dsh-session-naming.md` — Option A). (a) **Rotation cold seed** (`buildRotationSeed`, session-rotation.ts): a 5th event `{type:'session/title', seq:4, data:{title:'Asistente', messageSeqs:[], source:{kind:'user'}}}` — the exact `rename()` shape (`session/title` is in the persistence catalog, so cold artifacts load everywhere); durable from first materialization; blank rows keep the client-side "New Session" label (no synthetic turn events seeded). (b) **Registration path** (invoke.ts): `ensureHost` pins the same event on the live host session ONLY when the log holds no user-kind title yet — the owner's manual rename always wins; an existing Asistente pin is never double-pinned; automatic LLM (`source.provider`)/fallback titles stay overridable. Covers the first UI-created host session and every resume (rotated sessions already carry the pin in the seed). (c) **Verify**: `pnpm build` clean, full suite green (173+ tests — the new `pinHostSessionTitle` unit test included; exact count in the builder report), seed-shape assertions updated in rotation.test.js + invoke.test.js, reviewer PASS. (d) **Note (flag, not fixed)**: `docs/specs/002-host-session-rotation.md` still describes the 4-event seed; the seed is now 5 events (spec-drift — doc update out of the builder's file scope).

- **2026-08-22** — **Wake-15/16 — Relevo arreglado: split-brain live (2ª vez) post-mortem-2 + fixes single-live + promoción de la sesión activa + U4 verificado LIVE + dsh-smart-restart listado (PR #2726)**: (a) **Incidente** (owner report "veo dos sesiones, esta y otra que es root que pone un context injection de deepartments"): tras el dept_sleep de la sesión C (20:40:18-54 UTC), la rotación creó bien el sucesor `1cc4b33c…` (journal re-keyed, hosts.json v2, archive, seed 4-eventos CON journal-as-notice) PERO: el GUI mostró la vista "new chat" (el cliente NO auto-crea: solo clear en archived-sessions, dsh-client-runtime client.js:10065 — la sesión `c23fa91b…` nació 4 s después, 20:40:58, vía click del owner o startInitialSelection) → el owner escribió "Hola." en `c23fa91b…` (20:41:12) → `ensureHost` sin guard la registró como 2º host LIVE (fallo nº5 del post-mortem wake-13, P2 abierto) → **split-brain durable**; el watcher U3 abrió el sucesor a las 20:43:45 (2:51 de retraso: sesión COLD sin frame session-added + store no refrescado hasta generación de conexión; el watcher pre-chequea ids y no abría); el pack de `c23fa91b` fue degradado (KPI unavailable, journal inexistente — la ruta pre-resuelta apuntaba a journals/host-session-c23fa91b….md sin archivo); el sucesor mostraba la fila "**root**" (sin evento session/title — runtime pre-U4: el servicio arrancó 17:34 y el U4 se commiteó 20:21 → el pin de título no se aplicó a la rotación). (b) **Fixes (commit-eados)**: `3cf147a` U3 watcher refresh-on-missing (`shouldRefreshForHost` + `sessions.refresh()` → open ≤5-10 s; reviewer PASS); `6e032a1` ensureHost **single-live guard** (registro NUEVO con otra live → warn+refuse+devuelve liveHostId; sesión plain; dept_whereami honesto), **refresh MERGE** (conserva previousSessionId/sleepEpoch/boundarySeq/deferredJournalSeed — antes REPLACE que borraba la metadata del sucesor) y **loader cardinalidad WARN** (nunca throw); `c8063a7` **dept_whereami honest render** (una sesión rechazada recibía el texto "you are the Asistente host (not a board head)" pese a que el value.message decía "NOT a registered host — the live host es X" — el render ignoraba message; hallazgo vía smoke headless). 180/180 tras cada lote, reviewer PASS ×3. (c) **Recuperación/cirugía** (`hosts.json.bak-20260822-relay-promotion`, journal re-key 1cc4b33c→c23fa91b, archivo de fila "root" en workspace.json — ojo: el storage real es /opt/dsh/.dsh-dev/storages/workspace.json, no /.deepartments/): `1cc4b33c` retirada con rotatedTo→`c23fa91b` (que hereda sleepEpoch/boundarySeq/previousSessionId, forma canónica successor) — respaldos byte-idénticos, builder PASS; luego **restart con canary** (service 17:34→21:26) que cargó bundle nuevo + registry saneado → **verificación post-boot**: pack con KPI REAL (wake_counter 3, journal presente), UN solo host live, merge empírico (los campos sobreviven al refresh de ensureHost), **pin U4 disparado** (session/title 'Asistente' source user — la fila ahora dice Asistente), guard/loader empíricamente funcionales. (d) **dsh-smart-restart listado**: peers `^0.1.0-rc.7 || ^0.1.1-rc.0` (el harness es 0.1.1-rc.2; antes ERESOLVE), v0.5.1 publicada, push `1e0539e`, **PR #2726** a awesome-dsh-plugin actualizado a categoría `dev` + READMEs regenerados + informe fuera de scope (`9e331417`; CI verde; 3 archivos; reviewer PASS con FAIL intermedio corregido). dsh-ai.org espeja la lista (sin formulario propio). Research: `.dsh/reports/researcher/2026-08-22-dsh-plugin-listing.md`. (e) **Open items**: merge del PR #2726 por el mantenedor (+ verificar dsh-ai.org/api/plugins?q=smart-restart post-merge; nit: título "(notify category)" por actualizar; description.zh opcional); flake pre-existente `invoke.test.js:2427` 'a worker is lean' (race room-liveness worker first-message; pista: esperar room readiness en bootWithHead/create, mirror waitForRooms; no es regresión del lote); nota lineage wake_counter re-keyed (P2 heredado); R-Q3/R-Q5 (roster retired/orphaned) siguen.

- **2026-08-22** — **Wake-16/17 — Limpieza board-only del org (R-Q3 y R-Q5 CERRADOS) + fix de raíz del roomId latch del host**: (a) **Diagnóstico** ("¿se muestran agentes inactivos en las rooms?"): el roster vivo de board solo mostraba al host registrado; los huérfanos reales eran los posts `research-head`/`programming-head` (rooms muertas desde el 19-20 ago; sin sesión DSH real — solo presets legacy) → **R-Q5 CERRADO**; **R-Q3 CERRADO** empíricamente (los 7 hosts retirados de la cadena cf5225e4→…→c23fa91b NO se listan en el roster — archivo sano). (b) **Cleanup (owner, alcance B)**: `dept_post_retire` ×2 (nota de retiro + unregister; posts.json={}); `cordis.patch.yml` a **board-only** (rooms research/programming + bloque `departments` eliminados; board con members solo [asistente]) — builder PASS + reviewer PASS (FAIL intermedio: `.gitignore` no ignoraba `.deepartments.bak-*` → patrón añadido, re-check PASS) — **commit `0859823`**; **estadoDir vivo identificado: `/.deepartments`** (servicio cwd=/; `/root/.deepartments` y repo `.deepartments` eran copias stale de invocaciones CLI con otro cwd → archivadas `*.bak-legacy-20260822`; OJO: el CLI desde esas rutas regenera esos stateDirs). (c) **Bug descubierto — roomId latch**: al retirar un post, `retirePost` (invoke.ts:3741) resolvía el remitente con `memberIdFor(caller, entry.roomId)` → `ensureHost` REFRESH re-derivaba el `roomId` del host a la sala del post retirado → el host vivo quedó registrado en `programming` (sala moribunda); la rotación lo propagaba (dept_sleep pasa existing.roomId). Trazado: `.dsh/reports/explore-deep/2026-08-22-host-roomid-flip.md`. (d) **Fixes**: heal runtime (read a board → refresh; roomId=board restaurado); purga de residuo (rooms/{research,programming} → `*.bak-legacy` + prune de cursors huérfanas con backup previo); **fix de raíz `e69529c`** — ensureHost REFRESH conserva `existing.roomId` (`{...existing, hostId, sessionId}`; roomId solo en CREATE), +2 tests discriminantes (read/retire mantienen roomId) — **182/182**, reviewer PASS. (e) **Verificación post-boot** (2 restarts canary, 21:57 y 22:08): board = [asistente] + host db9680d0 live; roomId board persistido; posts {}; rooms solo board (+.bak). (f) **Open items tras esta sesión**: PR #2726 (externo, sigue); flake pre-existente invoke.test.js:2427; registro fantasma — `dept_room_who("research")` responde vacío (sin miembros/hosts; probablemente registros de sala persistidos; revisar en el próximo tema de rooms); CLI regenera stateDirs según cwd (nota operativa).

- **2026-08-22** — **Wake-16/17 (cont.) — research-head Fase 1: el head EXISTE (room research + post + preset + sesión ‘Research Head’ en sidebar + ciclos)**: (a) **Decisiones owner**: room `research` recreada (espejo del diseño original; board vuelve a listar research-head como miembro); ciclo persistente+dept_sleep (sin rotación); preset legacy reutilizado/actualizado; título ‘Research Head’; permiso de dormir DIFERIDO a la fase 2 (comunicación). (b) **Piezas** (la maquinaria de heads estaba viva — `.dsh/reports/explore-deep/2026-08-22-head-machinery.md`): config room+department con `sessionTitle: Research Head` (`CoordinatorConfig.sessionTitle?` en org.ts, default ''), `pinSessionTitle` generalizada (U4 hosts delega, semántica intacta), `attachHeadSession` en ensureHead/wakePost (seam canónica, no-FATAL WARN+give up), `dept_room_who` valida room (unknown room — cierra el ‘registro fantasma’: NO existía registro, era falta de validación — `.dsh/reports/explore-deep/2026-08-22-ghost-room-records.md`); 185/185 → **`31c9d49`** (reviewer PASS; FAIL intermedio: schema sessionTitle requerido → `.default('')`). (c) **Bug del sidebar descubierto**: el plugin fijaba `meta.cwd = repoRoot` (dir del bundle) y `attachSession` exige cwd==workspace.path (`/root`) → el head (y en su día la era vieja) quedaba invisible; el cwd durable es inmutable → fix: `resolveWorkspaceRootPath()` (entidad con sesión host-live → list()[0].path → repoRoot; espera acotada FIX 1b.1; nunca throw) + create de heads Y workers bajo el root + WARN de attach honesto + pin durable store-first verificado — 189/189 → **`666de7b`** (reviewer PASS; 1 flake transitorio no reproducible). (d) **Incidente de cuota + cambio de provider (owner aprobado)**: subagentes muriendo con 429 `GoUsageLimitError` (opencode-go, límite semanal) — y un `mv` de artifacts de sesión ROMPIÓ el catálogo (‘corrupt session log’: el scan valida ruta canónica del header; nada movible dentro de sessions/) → ‘subagent catalog read failed’ → recuperado (mv revert). Subagentes + coordinador del head → **`deepseek-official`/`deepseek-v4-flash-vision-exp` max** (preset base `.agent-presets/deepartments` editado — excepción directa del Asistente, autorizada, documentada, sin builders posibles con cuota agotada — + cordis.patch.yml → **`de13d5e`**; smoke PASS). (e) **Materialización**: el artifact legacy NO se puede ‘archivar’ con mv ni con archiveSession (hide-only, sin unarchive, y el scan mira TODO sessions/) → backup FUERA del árbol (`/opt/dsh/.dsh-dev/archive/heads-legacy-20260822/`, byte-idéntico) + rm de los dos dirs canónicos (posts.json intacto → resume-fail→create) → restart 23:15 → **verificación final**: `dept_room_who(‘research’)` = [research-head] + head live; artifact `--root--/head-research-head` con `cwd /root` y pin `session/title ‘Research Head’` (source user); workspace.json root sessionIds contiene head-research-head (fila nativa sidebar ‘Research Head’); boot sin corrupt/catalog. (f) **Avisos**: el head despierta con un mensaje (BE IDLE UNLESS ADDRESSED) y crea su journal al primer dept_sleep (el legacy se archivó); head-programming-head queda de reserva (artifacts en el backup). (g) **Open items**: fase 2 — comunicación entre agentes + permiso de dormir; el preset base (provider) vive en el perfil dev, NO versionado (nota); flake transitorio no reproducible (1 corrida) a vigilar.

- **2026-08-23** — **Fase 2 COMPLETA — Agent messaging: el board muere, llega send_message/agent_messages/dept_who (spec 003)**: (a) **Decisión owner**: eliminar write/read del board (who se queda → se funde con whereami en `dept_who`); `send_message` extendido UNIFICADO (el nativo del harness se desactiva); historial por cursor `before`; cada agente ve SOLO su historial; entrega SIEMPRE despierta (incl. host); análisis del Asistente: who+whereami=una tool (`dept_who` con `you:true`). (b) **B2 (com. `2649290`)**: `messages-store.ts` — `messages.jsonl` append-only (record `{id m-<n>, seq, ts, from, to[], text, kind agent|ack, threadId?, sensitive?}` — spec 003 §3.1), índice por-recipiente en boot (§3.3; paging O(log n)+slice O(1)), compactación boot (umbrales board-store; re-numeración con re-mapeo threadId; keep-rule durable members; `.bak`; keep-all defensivo), sidecar `deliveries.jsonl` (prepared→final; re-entrega idempotente); tools en `invoke.ts` — `send_message` unificado (ruta child→followup nativo; ruta catálogo→seam wakePost/materializePost; framing `[From X → a, b]: text` + source agent/send; persist-before-deliver; fan-out ≤20; self held; always-wake D4), `agent_messages` (pager propio, sin read marks — fase posterior), `dept_who` (catálogo, you:true); **override nativo**: el harness no tiene replace (NamedEntries.insert lanza duplicado) → registro scoped own-layer que shadowa el global (heads/workers restrict allow:[]; host vía agente/created; children transitorios conservan el nativo). 213/213, reviewer PASS. (c) **B3 (com. `28107cb`+`c3ace8f`+`e6381f3`)** — cutover: OUT `dept_room_read/write/who`, `dept_whereami`, `dept_witness_write`, relay/cursors/board-store.ts/applyOrg/proyección room; `org.rooms`+`members`+`roomId` fuera de config (departments = catálogo de agentes); ensureHead roomId inerte 'board'; wake pack `messageDelta`; `dept_post_create/retire` sin rooms; **gap cerrado**: busEnsureHostForCaller (auto-registro idempotente del host en dept_who/send_message, single-live guard respetado, subagentes excluidos); presets personas + skill → mensajería (canal = messaging; dormir pide permiso al Asistente por send_message y espera aprobación — protocolo fase 2 en la persona); docs README/AGENTS/specs banners. 190/190, reviewer PASS. (d) **Despliegue**: presets sincronizados al perfil, rooms/? → `rooms.bak-legacy-20260822`, plugin add, restart canary 03:44 → **VERIFICACIÓN LIVE end-to-end**: wake pack sin rooms con `messageDelta`; **m-0 recibido del head** — "[From research-head → host-session-db9680d0]: Acknowledged: research-head is up after the smart restart; no task in progress, staying idle." (bus + framing + sidecar prepared→resumed funcionando); `dept_who` = host (Asistente, YOU, live) + head (Head of Research, live); messages.jsonl/deliveries.jsonl shape correcta; skill de wake ya es la de mensajería (symlink propagado). (e) **Open items**: permiso de dormir = protocolo en persona (cumplimiento por comportamiento; tool-gate opcional futuro); nits P3 (test title 'dept_whereami' test/invoke.test.js:3200; comentario invoke.ts:2788-2790; comentarios stale board-store en messages-store.ts:212/215/271); preset base (provider) sigue sin versionar en repo; flake transitorio no reproducible; [EXTERNO] PR #2726; head-programming-head reserva.

- **2026-08-23** — **Ciclos de sueño del head verificados en vivo + 2 fixes**: (a) **Bug self-deadlock dept_sleep** (descubierto probando el permiso de dormir del research head): `await disposeHeadHandle` → `handle.dispose()` → dsh-agent-loop `whenIdle()` espera el PROPIO turno que ejecuta la tool → wedge irrompible (ni interrupt — root agent); evidencia `.dsh/reports/explore-deep/2026-08-23-head-sleep-hang.md` → **fix `0dbc66a`** — `void disposeHeadHandleOnce` (fire-and-forget) + promesa compartida por sesión (`disposingHeads`, finally-cleanup) + `materializePost` respawn joins el detach + `retirePost` paridad — 193/193, 3 tests con gate del dispose, reviewer PASS; verificado en vivo (tool/result + turn/end disposed + sleepEpoch durable, sin wedges). (b) **{{model}} en personas**: el primer turno post-restart fallaba 'deployment:persona' (la variable model no está ligada en el primer ensamblado del resume) → personas de presets sin `{{model}}` (literal deepseek-v4-flash-vision-exp, provider deepseek-official, reasoning max; `{{cwd}}` se conserva) — preset base del perfil + repos presets head/worker → **`3203b69`**; verificado: primer turno post-restart ensambla limpio. (c) **Health check del researcher** (`.dsh/reports/explore-deep/2026-08-23-head-health-check.md`): ciclo completo en vivo wake→reply(m-7)→memo→permiso(m-8)→aprobación(m-9)→dept_sleep limpio; 18/18 tool pairing, 0 deployment:persona, 0 finish errors, journal 1→4 contiguo; el `tool/result` "Error: tool call aborted" de dept_sleep es la firma esperada (cosmética). (d) **Notas**: el sleep es protocolo por comportamiento (head pide permiso → espera aprobación; sin tool-gate/expiración — posible mejora futura); el `end-seed` post-sueño de un preview GUI es benigno.

- **2026-08-23** — **Maratón — DEPARTAMENTO DE INVESTIGACIÓN (spec 004): catálogo de departamentos, ACL de mensajería, workers con rol/job, sidebar L1 pendiente + migración provider a OpenCode Go (key2)**: (a) **Decisión owner**: sesión maratón (diseño + implementación); el departamento se organiza como una empresa real (RH = jefe; trabajos y efímeros; roles researchers/reviewers/organizers; futuros deptos pueden añadir subroles; el Asistente NO coordina investigación — todo pasa por el RH); jobs versionados en el repo (sin scheduler/calendario en esta fase); mensajería scoped (workers solo su depto; heads entre sí; host todo); carpeta sidebar "Research Department" (L1 workspace real, confirmada — pendiente de implementar). (b) **Spec**: `docs/specs/004-research-department.md` (`720ac53`) — D1-D8 como requisitos, 27 ❓ abiertos para builder-verify/owner. (c) **Implementación**: F1 catálogo (`3e5b3fe`, 199/199) — `org.departments` con workspacePath/jobDir, `PostEntry` departmentId/managerId/jobId/retired, registro del creador, retire scope "solo mis workers", kind worker derivado en `dept_who`; F2 ACL (`9b5a67f`, 209/209) — `aclDenyGround` shared predicate (worker→su depto, worker→host PROHIBIDO, head→heads+depto, host→todos; record.to[] solo permitidos; ruta child fuera del ACL); F3+F4b tools (`57ece41`, 220/220) — `dept_worker_spawn`/`dept_worker_retire` (head-only, rol inyectado vía sistemaPrompt, pin de título, dedup de slugs incl. retirados, archive al retirar, historial conservado), `dept_who` con departmentId/role/jobId y retirados visibles (con flag), `dept_job_run`/`dept_job_list` (jobDir del depto, parse de frontmatter, idempotente no-dup, task=cuerpo del job, título humano); F4a jobs (`10f2cdf`) — monitor-dsh-updates / weekly-report-organize / fact-check-queue (role-tied, schedule reservado para la fase de calendario) + README layout; F6 personas (`90642b1`) — presets/departments/research/{researcher,reviewer,organizer}.md + head "manage your own department" (model literal, sin `{{model}}`); F7 agentOptions (`6b15709`) — `WORKER_AGENT_OPTIONS` único {opencode-zen/vision-exp/max} para los 3 spawn paths; `e2df499` rename provider id → opencode-zen; nits 7/7 (`47b711b`). (d) **MIGRACIÓN PROVEEDOR (decisión owner, key2)**: el catálogo opencode-go NO admite models (`NO_ADAPTER`/`UNKNOWN_MODEL`) → provider CUSTOM `opencode-zen` {api openai-completions, baseURL https://opencode.ai/zen/go/v1, models config} en settings.yaml de dev y stable; agent-default-model + presets (perfil y repo) → opencode-zen/deepseek-v4-flash-vision-exp/max; key2 en .credentials dev+stable (backups `.bak-20260823-key2`); VERIFICADO en vivo (smoke: subagente respondió "Model: deepseek-v4-flash-vision-exp (provider opencode-zen, reasoning max)"); contextWindow 1M / maxTokens 262144 (datos oficiales: modelo 1M entrada/384K salida, text+image, ago 2026; coste $0.22/1M in / $0.66/1M out; latencia ttfb 1.36s). (e) **INCIDENTE/diagnóstico de la migración (documentado)**: editar settings.yaml con python-yaml (YAML 1.1) convirtió la clave `off:` de reasoningEfforts en `false:` → la sección llm-pi-ai fallaba el schema → sin providers → `NO_ADAPTER`; 9 restarts antes de identificar (comentarios: la clave debe ser string `off` — js-yaml YAML 1.2; NO usar python/yaml para editar settings; usar sed/heredoc/ediciones manuales). Lección documentada. (f) **AUDITORÍA KEYS opencode-go (owner)**: key1 jedib53786@aganseo.com (válida, 0% weekly/50% monthly, requiere opt-in China para flash — 403), key2 newewic499@bora4d.com (LA ACTIVA — 0% weekly/20% monthly, 200 OK, usada en la migración), key3 haredan6762 (AGOTADA — 429 `GoUsageLimitError` "weekly", resets 2026-08-24T00:00; re-test mañana). (g) **PR #2726 EXTERNO** (awesome-dsh-plugin/awesome-dsh-plugin, autor edusrez): OPEN, CI verde, solo falta review/merge del mantenedor (cola activa 436 PRs); verificación post-merge: dsh-ai.org/api/plugins?q=smart-restart o catálogo npm (`.dsh/reports/researcher/2026-08-23-smart-restart-awesome-pr.md`). Sin acción urgente. (h) **OPEN ITEMS de la sesión**: F5 sidebar (carpeta "Research Department" vía workspace real por departamento + migración del research-head — L1 confirmada por el owner, A IMPLEMENTAR); "settings are unavailable in this browser" (GUI Providers — página models del harness; a investigar: autorización del navegador en el webServer; posible preexistente); 5 refs stale board-store en cabeceras de messages-store.ts (P3); 5 comentarios históricos "strict opencode-go API" en invoke.ts (P3); build del catálogo del harness desactualizado (vision-exp no embebido — por eso el custom; cuando el harness actualice, reconsiderar).

- **2026-08-23** — **INCIDENTE resuelto — GUI dev (8445) caído por race de boot en el
  workspace de depto (F5)**: crash loop del servicio dsh-deepartments-dev (~73
  reinicios) — `workspace domain is inconsistent: path
  '/root/.deepartments/departments/research' is claimed by both workspace ...` en
  validateStoredState del harness (@deepseek-ai/dsh-workspace); 502 en
  laagencia.taildb5a7a.ts.net:8445; Tailscale y stable (8444) intactos. (a)
  **Causa raíz**: `ensureDepartmentWorkspace` (F5, commit 00815a7) llamaba
  `registry.create` al boot sin guard de carga — create ES idempotente solo tras
  cargar el estado durable en memoria (createCanonical itera las entities
  en-memoria, dsh-workspace lib:457); los hooks de boot corren en la ventana de
  race de FIX 1b.1 → cada boot persistía un duplicado nuevo (7a9dbcbe→8be7833e;
  51ece1ad canónico intacto con sus sesiones) → el validate del boot siguiente
  abortaba el árbol de plugins. (b) **Fix `8dd2336`**: ensureDepartmentWorkspace
  con disciplina FIX 1b.1 — non-strict get, ausencia definitiva → WARN+return
  inmediato, retry acotado 250ms≤10s hasta `list()` resuelva (guard de carga),
  `resolveByPath` primero (no mutante) y create solo como fallback para path
  genuinamente sin dueño; nunca lanza; seam WorkspaceRegistryLike +resolveByPath
  opcional; 3 tests discriminantes (mid-init+pre-owned → create nunca llamado;
  ready+unowned → create 1 vez; sin registry → return inmediato); 232/232;
  reviewer PASS. (c) **Verificación en vivo**: limpieza del duplicado residual en
  /opt/dsh/.dsh-dev/storages/workspace.json (backup .bak-20260823-pre-fix-install),
  plugin link al repo con lib/ reconstruido, restart → active, 200 local en :3090
  y 200 en la URL Tailscale 8445, sin nuevos duplicados en el boot, 0 errores de
  workspace en logs. (d) **Lección**: `workspaceRegistry.create` requiere el guard
  "list() resuelto" antes de usarse en hooks de boot (misma ventana que FIX 1b.1);
  los comentarios F5 en invoke.ts:3989-4010 documentan la narrativa.

- **2026-08-23** — **Maratón (sesión 2) — Diseño agéntico orgánico + F10 y fixes de la suite Research + debug W0 (tool-fs) + Parallel search + RAG 3ª DB**:
  (a) **Diseño owner — flujo agéntico orgánico**: se abandona el esquema de "depth" rígido por un **modelo orgánico** — **efímeros** (subagentes de un solo turno) vs **job-workers** (jobs versionados en el repo, sin scheduler/calendario en esta fase), **ningún parámetro de profundidad fijo**; título de trabajo **"Rol: Misión"** (rol = identidad, misión = tarea única asignada); **presencia** decidida en sesión (presencia = gate de entrada al agente) y patrón **monitor → spawn + aviso RH** (un monitor dispara el spawn de un worker y avisa al Research Head; el Head NO coordina el flujo — el Asistente ya no coordina investigación, todo pasa por el RH); **suite Parallel SIN Responses API** (solo Search / Extract / Monitor — queda descartada la familia respuestas/chat por decisión del owner).
  (b) **F10 + fixes live** (commits del día): `cb2b4e9` (role→tools binding + inyección de archivo + fix ghost), `06bc41a` (personas orgánicas + analyst + ARCHITECTURE), `afb7ce7` (skill host delega research → RD, fallback excepcional), `e8639fc` (ids únicos `worker-<slug>-<uuid>`), `7acdb2d` (templating de persona + título "Rol: Misión"), `f3b81de` (allow-list scope-aware del agente), `7132855` (presets con tool-fs), `d4faeca` (await mount→probe), `174b663` (debug temporal).
  (c) **INCIDENTE / depuración W0 — root causes encadenados (5 rondas)**: (i) la allow-list se probó contra la **capa global del host vacía** (isolate realm) → se hizo **scope-aware**; (ii) presets worker/head **sin filas fs** → añadidas; (iii) orden del probe antes del mount → `await`; (iv) **raíz definitiva** — `dsh-web-app/cordis.patch.yml` **deshabilita por id** `tool-fs`/`tool-fs-search` (y `tool-jobs`, `str-replace-editor`) y ese disable **cascadea a los standing mounts de presets** → mount OK pero **filas inertes** → FIX: re-habilitar en `profiles/deepartments-dev{,-headless}/cordis.patch.yml` (`disabled:false`, la capa posterior gana; backups `.bak-20260823-fs-enable`). Evidencia: instrumentación `console.log` (vía journalctl; `ctx.logger` es invisible) mostró los probes `missing→FOUND`.
  (d) **Parallel — búsqueda integrada**: sección **search** en `dsh-tool-web-enhanced` (commit `c369a67`, **50/50**, reviewer PASS; `mode:fast` por defecto, `apiKeyEnv PARALLEL_API_KEY`, verificado live HTTP 200 + single-query OK); hace `POST /v1/search` con `{objective, search_queries, mode:fast}` y renderiza la lista "Parallel results" + snippets (≤10, sin paginación; **NO pide answer**). Investigación de la familia (`.dsh/reports/researcher/2026-08-23-parallel-api-eval.md` y `.dsh/reports/researcher/2026-08-23-parallel-api-family.md`): **search = LISTA** (sin answer; el answer sintetizado solo vive en la **Responses API** — descartada por el owner); **monitor** (activación push vía `event_stream`/webhook **Svix** o polling `GET /v1/monitors/{id}/events`; los **GET no consumen cuota**; frecuencia 1h–30d; `lite $3/1000 exec` / `base $10/1000`; 300/min) → **plan W3**.
  (e) **RAG**: 3ª DB **research** en el preset del Asistente (`.agent-presets/deepartments/agent.cordis.yml` — el runtime usa el **preset de sesión, no el profile patch** — + líneas de pregunta paralela restated; validado; `rag_index` **3 DBs OK**) + `sources/` + informe inicial archivado.
  (f) **Verificaciones**: **245/245** + reviewer PASS por unidad; smoke E2E m-27 con el RD escribiendo sus propios archivos; service HTTP 200 y canary PASS en todos los restarts.
  (g) **OPEN ITEMS (a cierre de sesión)**: **W1** calendario/scheduler (runtime + tools + UI panel); **W2** informe diario 09:00 GMT (dedup del ledger + entrega con "todo-pendientes" al volver presencia); **W3** suite Parallel (extract provider + monitor plugin); **W4** presencia (botón header + gate `ask_user` + inyección) — **diseño aprobado, implementación pendiente**; **key3** opencode-go re-test post-reset; **PR #2726** (externo, pendiente de merge del mantenedor); **P2** settings-unavailable; **P3** nits.

- **2026-08-23** — **ÉPICA COMPLETA — W1-W4 (Research Department como empresa real)**: (a) **W4 owner presence** (576952b) — toggle header `conversation.session.header.utilities` (order 15, junto a Session log parte derecha; localizada la util nativa session-log-download order 0), estado global runtime `<stateDir>/presence.json` (default present:true), RPC presence/get|set (rutas exactas slash), `ctx.tools.guard` niega `ask_user_question` solo host+absent (reversible), notificación por bus única por transición + estado `## Owner presence:` en el wake pack (dedup: eliminado el nodo pre-step de transición — fix 37c924b; probado en vivo 2x por el owner). (b) **W1 calendar & scheduler** (576952b) — `calendar.json` runtime + `dept_calendar_add/list/remove` (ACL creador/head) + panel Agenda (cliente `conversation.view`) + daemon 30s (cron 5-campos def., ledger minute-floored job-runs-state, motor compartido `runJobForDepartment`) + `agenda/list`. (c) **W2 daily AI news** (37c924b) — job `daily-ai-news` cron `0 9 * * *` (09:00 GMT), frescura ≤48h, ledger `daily-news/ledger.json` anti-repetición, reporte+archive sources/, protocolo entrega-presencia en SKILL.md; monitor-dsh-updates cronizado; D7 texts alineados. (d) **W3 suite Parallel SIN Responses API** — search section (`dsh-tool-web-enhanced` c369a67, mode fast, live 200) + **extract provider** (4afdc21 — `full_content:true`, 20-URL cap, opt-in, live 19KB) + **monitor module** (8f54f38, 09d5742 — event_stream polling GET events sin cuota, primer-cursor solo FRESH 48h, storm guard `maxConsecutiveSpawns=2` endurecido por página, spawn directo researcher `Researcher: Monitor: <query>` + aviso RH (bus, `[From deepartments]`), factory lazy resolve (boot race — byPost vacío en apply), 2 defaults base/1d ≈$0.02/día). Schema `parallel` first-class (org.ts). (e) **Verificación**: 275/275, reviewer PASS por batch, canary PASS todos los restarts, monitors creados+polling confirmados en vivo (state file + GET /v1/monitors), presence toggle y wake-pack estado probados en vivo por el owner. (f) **OPEN ITEMS**: key3 opencode-go re-test (reset 2026-08-24T00:00), PR #2726 merge, monitor huérfano `monitor_8b6b74b8...` limpieza P3 (DELETE requiere credencial admin), wake-12 flaky (passes isolated), P2 settings-unavailable, P3 nits (comentario stale invoke.ts:2290), backups storages limpieza opcional.

- **2026-08-23** — **Cierre de sesión + PRÓXIMA SESIÓN POTENTE — Departamento de Programación interna (W5)**: (a) **Entregado hoy**: fix de prompt/context-injection (regla *restart solo `smart_restart`*; directiva RESTART-INTERRUPTION en la fuente de presets), feature de dsh-smart-restart "sesiones interrumpidas" (verificada en vivo), directiva de presencia (wake pack + transición, en inglés), dshmarket 1.21.0, monitor huérfano cancelado, PR #2726 merged, bump web-fetch-http. (b) **PLAN PRÓXIMA SESIÓN**: crear el **Departamento de Programación interna** — equivalente al RD (head `programming-head` + workers builder/reviewer/explore-deep + personas + ARCHITECTURE), misión: programación interna de DSH/deepartments/plugins; plantilla: preset/departments de research (`presets/departments/research/*` + config `org.ts departments[]` + workspace `departments/programming`). **REVERSIÓN PARCIAL** del cleanup de `0859823` (revisar ese commit antes de re-crear; limpiar restos huérfanos de runtime `.agent-presets/deepartments-head-programming`). Microdecisiones pendientes con el owner (naming, workers efímeros vs standing, flujo Asistente→head→workers, alcance de permisos del depto).

- **2026-08-23** — **ÉPICA W5+W6 — Departamento de Programación interna (IPD) + monitorización de salud**:
  (a) **Departamento IPD creado y live** (decisiones owner 7 micro + naming "Internal
  Programming" para evitar colisión con el futuro "external programming"):
  `internal-programming` / `internal-programming-head` / "Internal Programming Head", preset
  `deepartments-head-internal-programming`, workspace
  `/root/.deepartments/departments/internal-programming`, personas
  builder/reviewer/explore-deep/organizer + ARCHITECTURE/README (spec
  `docs/specs/005`), **agenda común** (CalendarEntry.departmentId atribución + filtro
  opcional; ya era global).
  (b) **Tool `dept_exec`** (shell con ámbito): roots permitidos `{/home/esuarez/projects,
  /usr/lib/node_modules/@deepseek-ai/dsh, /opt/dsh/.dsh-dev, repo, workspace, stateDir}`,
  **stable `/opt/dsh/.dsh` protegido** (solo permiso explícito del owner), denylist de
  comandos, normalización/realpath de tokens + whitelist /dev + `org.execRoots` opcional;
  gated por allow-list de rol. 3 rondas de review (boundary stable-dev, tokens, redirects)
  — **281/281**.
  (c) **Jobs IPD**: `weekly-repo-health` (`0 8 * * 1`) y `version-watch` (`0 6 * * *` —
  evalúa seguridad/novedades/viabilidad, instala en dev, notifica Asistente para
  smart_restart; core-DSH solo propone).
  (d) **Rol del Asistente** (skill + preset host): interfaz/coordinador; programación
  interna SIEMPRE vía IPH (D2, PROGRAMMING REQUEST), fallback de emergencia anotado;
  presets base head/worker generalizados (orquestación agnóstica, ids `dept_worker_*`).
  (e) **E2E real del IPD**: m-79 → head ejecuta `weekly-repo-health` (job end-to-end vía
  `dept_job_run`, worker builder con `dept_exec` en-scope, reportes, 0 commits,
  0 out-of-scope); README actualizados en 3 repos (en/zh parity) — commits `24f701e`,
  `c12ef17`, `71733f6`.
  (f) **W6 system-health** (petición owner "monitorizar que todo va bien"):
  post-errors.jsonl (captura de fallos de materialización/wake) + daemon 60s (heartbeat,
  scan errores ≤2h dedupe 30min + fallos de entrega dedupe por messageId, ALERT por bus al
  Asistente con `[From deepartments]`, estado anti-repetición persistente) + job digest
  `system-health-report` (`0 7 * * *`) + spec 006; **tests 281→288**, 2 gates reviewer
  PASS; commit `5b1537a`.
  (g) **Deploy+drill**: ladder verde; probe E2E en vivo — heartbeat + **2 ALERT reales
  recibidos por el host** (drill + post-error REAL `agent/inbox/spliced non-JSON` en sesión
  host) → monitorización operativa.
  (h) **INCIDENTE (2026-08-23) — colisión de sesión IPH**: el smoke CLI del twin (comparte
  DSH home/sessions con el GUI) materializó al head nuevo y dejó log stale
  (`head-internal-programming-head`); el GUI al retomar la MISMA sesión determinística →
  guard `dsh-session-persistence` "id collision" → turno fallido. FIX: limpiar log stale +
  restart limpio; **patched twin** (`tool-subagent-control` disabled — colisión
  `send_message` pre-existente, carrera de registro; GUI/stable intactos). LECCIÓN: no
  correr smoke CLI del twin con heads configurados con artifacts durables en el mismo DSH
  home.
  (i) **PENDIENTES**: decisión OWNER — release-state de dsh-tool-web-enhanced (Parallel docs
  vs [Unreleased] npm 0.3.0-rc.1: ¿bump a 0.4.0-rc.1 o marcar unreleased en README?);
  W7 backlog IPD — (1) ruido de delivery-failures a recipientes muertos (re-intentos en cada
  boot), (2) error `agent/inbox/spliced non-JSON-serializable` en wake del host; key3
  re-test 00:00Z; informe diario 09:00 GMT 2026-08-24 (verificación); test script opcional
  dsh-tool-web-enhanced.

- **2026-08-24** — **W7 + PRIMERA OPERACIÓN REAL DE DEPARTAMENTOS (IPD)**:
  (a) **W7 backend IPD** (owner "adelante con todo"): A) terminaciones de entrega — estado
  `terminal` para entregas a recipientes muertos/retirados (se asientan una vez, sin reintentos
  en boot; el daemon solo alerta sobre `failed`) → adiós al ruido de alertas; B) fix
  `agent/inbox/spliced` — `toJsonSafe`/`jsonSafeMessageSource` en los 3 seams (busUserMessage,
  deliverBusRecord, buildWakePackMessage): la raíz era `senderSessionId: undefined` presente en
  el splice; C) **release dsh-tool-web-enhanced 0.4.0-rc.1** — CHANGELOG con Parallel +
  Parallel Extract (que faltaban), **publicado en npm (tag rc)**; `latest` sigue en 0.3.0-rc.1
  (decisión: el deploy usa el repo local symlink; rc es la línea del proyecto). Tests
  **288→296**, 2 gates reviewer PASS; commits `5224e15` + `fc6affe`.
  (b) **Sinergias inter-departamento** (petición owner): skill `Cross-department synergies` +
  specs 004/005 §7.6 + 8 personas con CROSS-DEPARTMENT + version-watch→RD: heads↔heads
  (RESEARCH REQUEST / PROGRAMMING REQUEST), workers siempre vía su head (D2), Asistente fuera
  del loop depto↔depto — commit `c92dfbc`.
  (c) **Primera operación en vivo**: `version-watch` (06:04Z) → **dshmarket 1.21.2 instalado**
  en dev (core y resto HOLD; 0 GHSA) y `system-health-report` (07:00Z) → primer digest:
  **SIN anomalías live**, 0 fail deliveries en 2h, spliced quieto >6h desde el boot W7 (= el
  fix B funciona); Asistente ejecutó el smart_restart para cargar 1.21.2 (verificado: servicio
  200 + heartbeat fresco).
  (d) **Lecciones/notas W8**: (1) AGENTS.md CLI pin `@deepseek-ai/dsh@0.1.0-rc.8` desfasado vs
  desplegado 0.1.1-rc.2 → actualizar; (2) peer ranges de dsh-deepartments y
  dsh-tool-web-enhanced (`^0.1.0-rc.7`) no matchean la línea 0.1.1-rc.2 (tolerado en boot;
  ensanchar en la próxima edición); (3) matiz m-115: entradas de sessión host ANTIGUA en
  hosts.json cuentan como "vivas" (1 failed tras boot; tratar hosts viejos como muertos en el
  fix A siguiente); (4) flaky test de timing de la suite (1 vez en 3 corridas; endurecer
  próximo pase); (5) key3 opencode-go re-test post-reset 2026-08-24 00:00Z (pendiente — el
  sistema opera con opencode-zen; verificar fallback); (6) informe diario 09:00 GMT 2026-08-24:
  primera entrega esperada por el protocolo de presencia (verificar).
- **2026-08-24→25** — **ÉPICA QD (Quality Department) + MARATÓN DE FIXES DE MONITORIZACIÓN + INCIDENTE REWIRE** (sesión owner-dedicated). (a) **Departamento de Calidad creado y live**: spec `docs/specs/007`, `quality-head` (Head of Quality) + `quality-inspector` efímero, hooks de archivo (worker retire con dado 10% — "control aleatorio" —, head sleep 100%, rotación de host 100%), análisis event-driven de post-errores + job `quality-daily` (`0 8 * * *`), QD report-only que auto-encola PROGRAMMING REQUEST al IPD — commits `1524a2e` (341/341). (b) **P2 system-health** (IPD): host retirado = terminal (scan-gate), stalled con skip de retirados/activos, dedupe por identidad de error, fix latente iterator host-list — `b357cd9` (348/348). (c) **First-wake assembly** (dueño: el error `{{model}}` / "no provider/model" tras restart): variante-1 persona literal + `agent-default-model` (`f695951`, 350/350), variante-2 `HOST_AGENT_OPTIONS` en el resume D4 (`28768e4`, 352/352) — **cerrado** (verificado vivo). (d) **Bug A row-writer**: los gates in-memory no bastaban — la raíz son los **daemons gemelos web/vanilla (3080/3081, boot 09:34, compartiendo `/.deepartments`)** con registros cacheados stale; fix = **relectura durable de `hosts.json` en disco** en el catch (`dd4f8ad`, 355/355) — el dev queda cubierto; los gemelos (stable protegido) requieren decisión del owner para re-iniciarlos. (e) **QD NO_ADAPTER hardening** (escalada auto del QD): error de turno NO_ADAPTER → cola post-error (daemon alerta) + check de registro proveedor↔adaptador en boot (`893ebfb`) con **tolerancia a la carrera** (ventana de polling: registro retardado ≠ alerta; nunca registrado sí — `2478335`, 361/361). (f) **W9-c**: normalización developer→system en dsh-key-pooler (`941187e`, 18/18) + docs head-sleep (directiva Asistente como vía primaria) (`bad12ef`). (g) **INCIDENTE (2026-08-24 18:27→23:00)**: rewire pooler (baseURL 4097) rompió el registro del adaptador `opencode-zen` (selector vacío + "no adapter registered", turnos 39-42 del IPH "silenciosos") — la verificación previa usó la API raw, no el adaptador DSH; **revert** (owner-sanctioned) a baseURL directo + restart 23:00:05 → adaptador + selector restaurados; el pooler queda servido pero **des-objetivado** hasta diagnóstico de compatibilidad del adaptador (cola IPH). (h) **PENDIENTES**: decisión OWNER — reiniciar/inactivar los gemelos 3080/3081 (única fuente restante de filas `session not found` del host retirado); diagnóstico adaptador↔pooler antes de un segundo rewire; P3 RAG ensureIndex; core-proposal `{{model}}`; flaky test de timing endurecer; informe diario 2026-08-24 pendiente de entregar (owner absent).

- **2026-08-25** — **MARATÓN DÍA 2 — FIXES MONITORIZACIÓN (M3/M4) + INCIDENTE 429 + RE-REWIRE POOLER EXITOSO + RENAME QUALITY HEAD + INSPECCIÓN CONSOLIDADA QD** (continuación de la sesión 08-24→25; dueño ausente → trabajo autónomo según workflow).
  (a) **M3 dshd-error-handler** (`bd9de7b`): safeInterrupt interrupt-loop guard + identity de alerta por clase estable + guard de cascade de materialización.
  (b) **M4 gobernanza** (`00207b8`, docs): workflow no-dispatch + consolidación QD + gobernanza autónoma de sueño (directiva del Asistente como vía primaria) + anti-bucle QH (D-Q7).
  (c) **Monitor/anti-loop batch** (`fdeca80`): QH sleep dice 10% (sampling anti-loop), dormant-exclusion (stalled+wait) en scans, dept_who retired flag, exclusión de workers huérfanos.
  (d) **Batch durabilidad** (`8b300a2`): durable reconcile + idempotencia de head-sleep + visibilidad head-session + dept_exec-guard.
  (e) **Fix health-alert recipient durable-source** (`2df0215`, 363/363): el daemon resuelve el host LIVE desde la cadena durable de rotación hosts.json en cada tick (durable-first, fallback in-memory) — las ALERTs de salud llegan al Asistente live, sin direccionar sesiones host retiradas.
  (f) **QD D-Q5 FIX-1 alert-text fidelity** (`cacca82`, 362/362): scanTurnErrorCaptures desciende al reason.error anidado → texto real de NO_ADAPTER en vez de 'error (turn N)'.
  (g) **INCIDENTE 429 (08-25)**: ráfaga de post-errors GoUsageLimit/429 mañana-mañana-tarde → cola de post-errors con 183 entradas históricas asentadas (sin entradas nuevas tras el restart 15:45; 2 interrupted-post = artefacto conocido cubierto por M3).
  (h) **P1 re-rewire pooler** (`ef6decb`): segundo intento del rewire tras el incidente 08-24 — LLMs del org ruteados por dsh-key-pooler (127.0.0.1:4097, rotación real de claves ante 429) + drift-exemption; host permanece en deepseek-official. CONFIRMADO esta vez (el fallo de 08-24 fue verificar con la API raw en vez del adaptador DSH): adaptador opencode-zen registrado + selector no vacío.
  (i) **Cierre m-301 — rename** "Head of Quality" → **"Quality Head"** (`a83516a`, docs-only 2 files): cordis.patch.yml title/sessionTitle + refs de naming en spec 007 (D-Q1, §3.1, §7.2); verificación directa del Asistente (docs-only, IPH ya verificó); el catálogo mostrará "Quality Head" en el próximo boot del QH (ciclo natural).
  (j) **Inspección consolidada QD** (m-319, 3 eventos de ciclo de vida — 2 rotaciones de host + sleep del IPH): (1) archivos OK, sin defecto de rotación (rotaciones deliberadas por directiva owner "dormir arquitectura / limpiar contextos"); (2) **PRIMARY fixable**: 135 post-errors `session "session-<uuid>" not found` a sesión host stale (host-session-606feebc 121, host-session-ecc50879 13, host-session-0b90801d 1) — la capa de entrega/workflow direcciona ALERTs host-destino a una sesión host retirada en vez del host LIVE; auto-amplificante (cada fallo re-emite alerta + directiva Quality inspect); alerta abierta post-error:host-session-ecc50879:aa7a7635; (3) **SECONDARY**: el IPH durmió con builder-59 en vuelo — su report m-310 quedó en "resumed" sin llegar a "delivered" (carrera con el dispose del head); auto-sanado (el head re-despertado lo recogió, verificó el rename, retiró builder-59) pero el patrón es inseguro → ambos findings con PROGRAMMING REQUEST auto-encolado al IPD (QD→IPD directo, fuera del loop del Asistente). Contexto sin request: NO_ADAPTER opencode-zen (18 filas, trackeado), 119 entregas fallidas a UUID anónimos (no host/rotación), session-store ~74MB sin podar (99 jsonl de sesión host, almacenamiento).
  (k) **Cola IPD durable (P3, esperando sesión con owner presente)**: M5 pane pooler Settings (research listo — reports/researcher/2026-08-25-dsh-settings-pane.md), P3 GAP-2, P3 RAG ensureIndex, P3 flaky teardown W9-b(d); P3 workerInspectProbability 0.25 — ya puesto y commiteado en `ef6decb`.
  (l) **Pendiente owner**: reiniciar/inactivar los gemelos web/vanilla 3080/3081 (comparten /.deepartments; fuente restante de filas session not found del host retirado — confirmado por el QD).
  (m) **TARDE 2 — ROTACIÓN 3 + DECISIONES OWNER (QUITA DE KEYS POOLER + FIX GEMELOS) + STALL IPD + ROLLOUT ISSUES 1-2-3**: (1) 3ª rotación de host (e9cd7897→e921ee10) inspeccionada por el QD: CLEAN — cadena hosts.json intacta, 1 fila live, 183 post-errors sin nuevos, defecto 135 `session not found` ESTABLE (no migra con rotaciones; informe .dsh/reports/quality/2026-08-25-host-rotation-e921ee10.md). (2) **Decisiones owner (presente)**: (a) pooler keys — sin acceso al billing de opencode.ai → claves no validables/operables se ELIMINAN de la pool; (b) gemelos web/vanilla — "Fixealo" → **dsh-vanilla.service (3081, twin vanilla rc.8 idle) stopped+disabled**; 3080 stable + 3090 dev intactos; sin restart de ningún servicio. (3) **Stall IPD detectado por el owner**: el reporte de builder-60 (Issue-2) nunca llegó al head (carrera del propio Issue-2 — notice perdido); árbol congelado 2h38m; desbloqueo vía `send_message` interrupt:true (m-352) → IPH verificó Issue-2 completo, retiró builder-60, builder-62 en pasada única (verificar Issue-2 + Issue-1 + Issue-3) + reviewer PASS; suite completa **405/405**. (4) **Invest. m-id** (explore read-only): el reuso de messageId por compactación boot-time es un riesgo LATENTE REAL (rebind → mis-entrega si un `prepared`/`failed` queda colgando) → Issue-3 (guard `record.to.includes(recipientId)` + 2 tests de regresión). (5) **POOLER oc-7 removido** (`c5f897e`, directiva owner): workspace wrk_01M0FFG5VJEW6K85S4J20PGFHG en límite semanal (429 GoUsageLimit) → pool con 7 claves (oc-1..6, oc-8); verificado build 0 / tests 22/22 / adapter real 200. (6) **ROLLOUT completo**: ladder (pnpm build ✓ · plugin add ✓ · dump-config capa `# == dsh-deepartments` ✓ · smoke `SMOKE-OK` ✓) + **smart_restart CANARY 19:28** (33s downtime) + verificación post-restart (servicio 3090=200 · pooler `/v1/models` 200 · **completación REAL por la pool 200 sin 429** · heads sin interrupted · boot log `[key-pooler] ... (7 key(s))`) + **commits `11dde84`** (bundle issues 1-2-3) y **`c5f897e`** (pooler oc-7).
  (n) **PENDIENTES tras el rollout**: QD verifique la caída del count 135 + desaparición del alert stale `post-error:host-session-ecc50879:aa7a7635`; verificar que el count no crezca (twin 3081 off + re-route activo); si dsh.service (3080 stable) sigue siendo fuente → backport durable (decisión owner); cola IPD durable P3 (M5 pane pooler Settings — research listo, GAP-2, RAG ensureIndex, flaky teardown W9-b(d)); opcional: retirar drop-in quality-inspect.conf cuando config 0.25 sea definitivo.

- **2026-08-26** — **SESIÓN AUDITORÍA ARQUITECTÓNICA + PLAN MODULAR CORDIS (owner-dedicated)**: (a) **MISIÓN owner**: buscar errores y optimizaciones de la arquitectura de cara a una versión modular siguiendo la filosofía/arquitectura de Cordis (`dshd-*` = deepseek harness departments), dividiendo el bundle en `dshd-core` + plugins hermanos; el owner añadió 2 requisitos: herramienta universal de feedback (cualquier agente notifica fallos/mejoras → worker QD → informe a QH → backlog durable de QH estudiado entre sesiones) y auditoría de prompts/personas para que no se repitan fallos de disciplina. (b) **AUDITORÍA (4 exploradores seed + verificación IPD)**: síntomas del owner CONFIRMADOS — `dept_who` devuelve 170 miembros con 162 retirados (bucle de posts no filtra retirados, invoke.ts:11022; sin vista activos-only; sin feedback post-deploy/post-retire; A1-A6); "no siempre se duermen todos" (no hay sleep_all atómico; send_message/materializePost SIEMPRE despiertan — sin sleep-fence; el ack CLOSED del QD re-despertó al IPH 8 min tras dormir, m-361; B1-B7); higiene/recursos (state home 591M; archive 121M sin cap; posts.json 162/169 retirados sin poda; messages.jsonl re-compacta cada boot; entregas fallidas esperan al boot; test flaky W9-b(d); C1-C16); arquitectura (monolito src/invoke.ts 11.998 LOC = 74,5% de src; sistema oculto de 3 plugins; roleRegistry module-global; ciclo type-only org.ts⇄invoke.ts; D1-D7). Informe consolidado: `.dsh/reports/ipd/2026-08-26-arch-audit-modular-plan.md` (reviewer-33 PASS). (c) **CORRECCIÓN DE DISCIPLINA — EXCEPCIÓN ANOTADA**: la auditoría la inició el Asistente con subagentes transitorios `explore` (análisis de código propiedad del IPD) => el owner lo señaló; se corrigió en la misma sesión: misión formal al IPH (m-364) que asume propiedad; los 4 seed reports quedan como insumo; la causa raíz (texto del workflow ambiguo: fila explore sin gate del IPD, subagent.ts descripción, fallback de emergencia indefinido) está en el AÑADIDO 2 F1/F2/F5 pendiente de aprobación del owner. (d) **PLAN MODULAR DAG (aprobación pendiente)**: `dshd-core` (kernel: catálogo hosts/posts + messages-store + motor de entrega + gate `deliverOrQueue({noWake})` + ACL + wake-pack + lifecycle) y hermanos `dshd-registry` (dept_who scope+activeMembers+retención), `dshd-sleep` (sleep_all+fence+rotación), `dshd-jobs`, `dshd-health`, `dshd-quality`, `dshd-feedback` (NUEVO, idea owner; spec QD m-371: tool universal severidad-gated, ACL-safe por bus daemon, feedback.jsonl 4 estados cap 200/archive, wake-pack del QH con backlog, QH única autoridad de cierre, fix → PROGRAMMING REQUEST estándar), `dshd-subagent`, `dshd-gui`, `dshd-pooler`, `dshd-webfetch` (hoja). ORDEN: webfetch (sonda byte-clean) → romper ciclo de tipos → core → sleep → registry → jobs → health+quality (final, máximo acoplamiento) → subagent/gui/pooler; feedback tras core, antes de cablear quality. (e) **PROMPTS/PERSONAS (AÑADIDO 2, F1-F10)**: corrección estructural propuesta — fila explore gateada al IPD, tool-desc de subagent (NON-CODE/emergency), fallback de emergencia definido estricto (confirmado down → ask_user_question), vocabulario de roles unívoco, persona del Asistente sin "coding agent", AGENTS.md regla 10 ownership, raíz explore-deep/ reservada. (f) **PENDIENTE OWNER (puerta de aprobación §7 del informe)**: 6 decisiones — target dshd-core+hermanos, orden de extracción, arranque Fase 1 (webfetch + ciclo tipos), política de cap archive (propuesta K=5+GC), retirada de quality-inspect.conf, autorización F1-F10. SIN implementación aún, sin restart, sin cambios de código; repo limpio salvo esta doc.

- **2026-08-26** — **TARDE-2 — IMPLEMENTACIÓN MODULAR POR FASES (vaciamiento del plan, owner delegó la ejecución en el Asistente — dec3 — y ausente el resto)**:
  (a) **Decisiones owner (presente al inicio de la tarde)**: (1) cada DEPARTAMENTO = su propio módulo dshd → diseño dshd-department genérico + módulos declarativos por-depto (research/IPD declarativos; quality/feedback = código real); (2) orden: decidido por el Asistente (webfetch → ciclo tipos → core → framework deptos → sleep → registry → feedback → jobs → health+quality al final → subagent/gui/pooler); (3) keys del pooler: verificadas 8/8 HTTP 200 contra opencode.ai/zen/go/v1 — pool sano, sin clave extra; (4) archive: política owner FINAL — NO borrar, COMPRIMIR (hot = 3 últimas rotaciones sin comprimir, cold zstd bajo demanda, sin GC destructivo); (5) quality-inspect.conf: ALINEAR (no retirar); (6) prompts/personas (AÑADIDO 2, F1-F10): AUTORIZADOS los edits estructurales para la fase de implementación.
  (b) **FASE 1 (`3ca2e04`)**: split `dshd-webfetch` a packages/ (405 LOC byte-identical, fetchProvider deepartments-fetch idéntico, cordis.patch.yml propio, peer ranges alineadas) + ruptura del ciclo type-only org.ts:22⇄invoke.ts:85 (tipos de config a org.ts) — ladder completa (build · plugin add ×2 · dump-config 2 capas · smoke SMOKE-OK), suite 405/405.
  (c) **FASE 2 — tallado dshd-core (a-f, commits `b220144` `fdd563a` `b6fa47b` `b65a16e` `153b800` `a263fa6`, suite 407/407 cada paso)**: (a) registry store durable hosts/posts (`src/core/registry.ts`, verbatim + bridge, migration-compatible R6); (b) messages-store + redelivery guard (`src/core/messages.ts`) + C8′ hardening terminal-una-vez para recipients no-catálogo (2 tests nuevos); (c) motor de entrega + **gate `deliverOrQueue(postId,msg,{noWake})` default noWake:false** (always-wake exacto; llave R7); (d) ACL como función pura (`src/core/acl.ts`, 12/12 grounds, worker→host PROHIBIDO); (e) wake-pack injector/roster (`src/core/wakepack.ts`, buildWakePack byte-identical, lista-activos fuente única en registry); (f) lifecycle carve (sueño+rotación+journal/archive tal cual, sin zstd — diferido). Prerequisitos anotados para dshd-sleep: estado `queued` (noWake no debe re-despertar en boot) y gate de los 4 hooks de daemon que bypassan el gate.
  (d) **FASE 2.5 (`09488e2` `d158732` — batch A y B)**: `packages/dshd-core` scaffolded (8 módulos verbatim + barrel + puentes drop-in lib/core supersets; roleRegistry único intacto vía DI; ROLE_CONTRACTS consolidado a una fuente; dshd-core a dependencies) + dshd-core como PLUGIN real (ctx.provide deepartments.catalog/acl/postState/org) y bundle consume vía ctx.get con fallback — **PARTIAL honesto documentado**: deliver/bus/lifecycle/wakepack quedaron closure-bound = D1 no completo.
  (e) **FASE 2.6-A (`f42f0c9`)**: config.org relocado al row dshd-core (dump byte-identical, sin overrides de perfil), ACL corregido del mirror (prepared, aún no consumido), dshd-core provee `deepartments.org`; bloqueador restante D1 = closure-into-core (poke: mover wake-relay closure maps con deps inyectadas) → **FASE 2.6-B/C en la próxima sesión** (+ fix del comment erróneo del header de dshd-core/cordis.patch.yml).
  (f) **CHECKPOINT DE CIERRE**: perfil live editado (dshd-core → dependencies link + dsh.profile.bundles ANTES de dsh-deepartments, backup .bak-20260826-closing) → dump-config capas `# == dshd-core`(513) ANTES de `# == dsh-deepartments`(560) + webfetch(712) + fetchProvider ok; **smart_restart CANARY 06:46Z** (canary PASS) → verificación post: servicio 3090=200 · pooler /v1/models 200 · boot log `[deepartments] online` + `[key-pooler] ... (7 key(s))`; dshmarket 1.30.0 (version-watch 08-26, UI/diagnostics, sin breaking) cargado en el mismo restart.
  (g) **Sin pendientes owner-facing**: próximos pasos anotados (2.6-B/C + ladder; luego dshd-sleep/registry/feedback por fases; F1-F10 authorized).
  (h) **EXCEPCIÓN ANOTADA (ya en la entrada de la mañana)**: la auditoría inicial la hizo el Asistente con explore transitorio → corregido a IPD; causa raíz F1/F2/F5 pendiente de los edits F1-F10 autorizados.

- **2026-08-26** — **TARDE-3 — FASE 2.6-B/C closure-into-core (D1 FULL) DEPLOYED + CHECKPOINT** (continuación; dueño ausente → trabajo autónomo): (a) **Fase 2.6-B/C** (`7e8d727`): dshd-core = el kernel que posee catalog+bus+deliver(gate)+wakepack+lifecycle+acl+postState+org+binder como servicios — B-1 (`deepartments.binder` + lifecycle/wakepack lazy on-first-use) · B-2 (`deepartments.bus` + `deepartments.deliver` lazy; buckets a/b internos, bucket-c vía binder, dep ausente FAIL-LOUD R1) · C (re-cable `applyInvoke` del bundle para consumir todo vía `ctx.get` con fallback in-bundle behavior-neutral, seam `deepartments.bus`, `deepartments.acl` vía ctx.get, sin ciclo org.ts⇄invoke.ts, tools+daemons intactos); fix del comment erróneo del header de `dshd-core/cordis.patch.yml`. (b) **Verificación**: pnpm build 0 · pnpm -C packages/dshd-core build 0 · suite 407/407 (fail 0) · git diff --check limpio · reviewer PASS por batch (44/45/46/77) · sin edits de tests (fallback bundle-alone behavior-neutral) · R6 intacto · daemons/roleRegistry/src-core sin tocar. (c) **CHECKPOINT deploy**: `--dump-config` muestra `# == dshd-core`(513) ANTES de `# == dsh-deepartments`(560) · plugin add · **smart_restart CANARY 09:04Z PASS** → verificación post-restart: 3090=200 · pooler /v1/models 200 · boot log `[deepartments] online` + `[key-pooler] listening ... (7 key(s))` + `/deepartments channel mounted` (Tailscale 8445) · sin errores de adaptador. (d) **D1 FULL alcanzado**: deepartments ya NO bundle-construía deliver/bus/lifecycle/wakepack — consume los servicios del kernel. (e) **Corrección de disciplina (verificada con el IPD)**: explore-deep-22 (audit provider-fallback) SÍ fue interrumpido de verdad por el smart_restart 09:04Z — el audit quedó sin entregable y se re-lanzó fresh por el IPH (worker retirado/re-spawn); research-head interrupted-post en verificación. No es un artefacto benigno: fue una interrupción real, clasificada erróneamente al inicio por el Asistente y corregida tras contactar al departamento. (f) Tras el checkpoint, el IPH re-toma el audit de provider-fallback (LOW) para el cuota opencode-zen.

- **2026-08-26** — **TARDE-4 — CHECKPOINT 2 (deploy 10:00Z): version-watch + pooler key-env cleanup + stalled-post heuristic**: (a) **version-watch** (IPD): dshmarket 1.30.0 → **1.31.1** + dsh-smooth-stream → **0.4.1** (0.4.0 se actualizó a 0.4.1 — el verdadero latest, non-breaking; hard dep `@deepseek-ai/dsh-settings@0.1.0-rc.6` resuelta limpia, sin ERESOLVE), instalados solo en deepartments-dev (backup `backup-20260826-095512-versionwatch/`), nunca stable. (b) **Pooler key-env cleanup** (directiva owner — "borra las totalmente inválidas"; smoke directo verificado): oc-1..oc-4 = 401 "Insufficient balance" (muertas), oc-7 = weekly-limit (ya retirada del pool 08-25) → eliminadas del env `key-pooler.conf` (backup `.bak-20260826-keys-cleanup`); se mantienen oc-5 (monthly-limit, se recupera ~4d), oc-6 y oc-8 (**verificadas WORKING** con el modelo del org — smoke real 200). **Daemon-reload hecho** → unidad ahora configura solo OPENCODE_GO_KEY_5/6/8 (efecto completo en el próximo start del servicio; el pooler ya excluye las inválidas funcionalmente). Pool efectivo: **2 claves** (oc-6/oc-8). (c) **stalled-post heuristic** (`0d4e7e1`): heads CONFIGURADOS con turn/end MÁS-RECIENTE benigno (end limpio O error transitorio de quota/rate-limit) exentos del flag stalled-post (idle-hold deliberado — cubre los falsos positivos QH quota-hold y RH post-delivery; genuine stalls siguen señalados; tradeoff D2 documentado: benign-ended-then-zombied ya no lo flag; residual D3: boot-quiet sin turno benigno sigue flag). (d) **smart_restart CANARY 10:00Z PASS** → verificación post: 3090=200 · pooler /v1/models 200 · boot log `[dsh-smooth-stream] plugin loaded!` + `[deepartments] online` + `/deepartments channel mounted` · sin errores de adaptador · heads live. (e) **Cierre**: sesión al límite de la jornada — D1 FULL + auditoría provider (veredicto honesto: sin fallback confirmado hoy; deepseek-official es la única salida estructural y necesita apiKey del owner — PENDIENTE-OWNER), disciplina corregida/verificada con departamentos, y DAG modular en punto de retoma (siguiente fase: dshd-registry/sleep/feedback).

- **2026-08-26** — **TARDE-5 — FASE dshd-registry (A1-A5 + C2) DEPLOYED + CHECKPOINT 3 (deploy 11:38Z)**: (a) **Commit `cc896f5` (4 files, +424/-40)**: A1/A2 `dept_who` con `scope: 'active'|'includeRetired'` default `active` en AMBOS loops (hosts + posts) — el fix de la "lista infinita" (170 miembros/8 activos → ahora 5 activos); A5 header honesto `(N member(s), M retired)` + `retiredCount`; A4 `activeMembers` post-mutación en dept_post_create/retire + dept_worker_spawn/retire (schema+render); A3/C2 poda/archivo NO destructivo de posts.json retirados (`reconcileDurablePostsRegistry`: backup `posts.json.bak-<ts>-prune` + archive `posts-retired-archive.jsonl`, shape R6 intacto) con gate `enableRetiredPrune === true` — **default conservatorio OFF** tras follow-up del Asistente (m-577: absent = NO poda; el registry vivo intacto) + config `org.postsRetention` (absent = code defaults; PRODUCTION GATE anotado en doc); doc alineada (org.ts unifiada); 2 reviewers PASS (7/7 + parity final). (b) **Ladder**: builds green (root + dshd-core); suite **412/412** (407+5: 4 nuevos A-series + 1 default-off); plugin add; dump-config `# == dshd-core`(513) antes de `# == dsh-deepartments`(560) y `postsRetention` AUSENTE (poda OFF); smoke headless twin (ambos scopes ejecutan limpio); **smart_restart CANARY 11:38Z PASS** → post: 3090=200, pooler 127.0.0.1:4097 /v1/models=200, boot log `[deepartments] online` + `[dsh-smooth-stream] plugin loaded!` + **pooler `(3 key(s))`** con `oc-1..4 skipped (no key resolved)` — el env cleanup TARDE-4 completamente efectivo (residual 7 key(s) resuelto en este boot); `dept_who` LIVE verificado: **"Deepartments catalog (5 member(s), 238 retired)"** (default active). (c) **Triage**: m-570 delivery-failed = benigno (recipient builder-82 retirado; IPH re-despachó a builder-83; review final PASS) — NO perdido; ANOTADO: el daemon re-alertea delivery-failed permanentes cada ~30 min (patrón m-380/m-570) → m-590 en curso (IPD, recipients retirados → terminal). (d) **Monitores**: post-error IPH interrupted-turn-12 por el restart 11:38 — interrupción REAL (IPH live 1s antes), verificación de impacto en curso con el departamento (m-13); stall-post heuristic: sin flags QH/RH desde la exención (0d4e7e1). (e) **PENDIENTE-OWNER**: (a) política retención C2 — N=50 + archive `posts-retired-archive.jsonl` + gating `org.postsRetention.enabled: true` EXPLÍCITO — pendiente de confirmación del owner (default OFF ya desplegado; poda NO corre); (b) apiKey DeepSeek para fallback deepseek-official (diferido sin fecha). (f) **Siguiente DAG**: dshd-sleep (B1-B7 con prereqs: estado queued settled/no-wake + gate de los 4 hooks) → dshd-feedback (spec m-371).

- **2026-08-26** — **TARDE-6 — FASE dshd-sleep (B1–B7) DEPLOYED + CHECKPOINT 4 (deploy 13:32Z)**: (a) **Commit `857494c` (6 files, +708/-40)**: B1 `dept_sleep_all` (lifecycle.sleepAll — heads configurados no-QH, una escritura durable, sin directives head-slept por batch, idempotente, excluye quality-head anti-loop D-Q7); B2 `noWake` cableado en `send_message` (default always-wake inalterado; gate existing delivery.ts:296); B3 fix m-361 — ack a head DORMIDA no-wake ('prepared', drena en su próximo wake), directive de inspección a QH siempre-wake; B4 `deliverDaemonNotice` gatea los notices rutinarios de los 2 daemons (agenda + parallel-monitor: target dormido → 'queued'; health ALERT/system-wait/interrupt NO gateados); B5 reconcile de catalog-live-sin-sesión-usable (marker `unusable-agent-options.json` — VARIANT-2/builder-87 cubierto; WORKER retire opt-in, HEAD warn-ONLY nunca auto-retirada; gate `sleepEpoch` para no-STALE-falso en boot); B6 (enum postState) y B7 (superficiar backlog en wake) **DEFERIDOS documentados** (deuda/línea de bajo valor). (b) **Ladder**: builds green (root + dshd-core); suite **422/422** (407+15); 2 reviews PASS (Batch1 B2+B3 5/5, Batch2 B1+B4+B5 6/6) + refinamiento slept-head-no-stale; plugin add; dump-config capas `# == dshd-core`(513) antes `# == dsh-deepartments`(560); smoke del twin CLI >60s (timeout executor) → se sustituyó por **verificación en vivo post-canary** (más fuerte, plan del IPH); **smart_restart CANARY 13:32Z PASS** → post: 3090=200, pooler 4097=200 (3 keys), boot `[deepartments] online` + smooth-stream + channel mounted, **SIN warns STALE/unusable (gate B5 ✓)**; **verificación B1 en vivo**: `dept_sleep_all` → 1 slept (internal-programming-head) + 1 skipped (research-head ya slept) + quality-head excluido sin directives; roster: RH+IPH sleeping durables. (c) **Cierres**: fix m-590 (`18ef2f0`, delivery-failed a recipients retirados ya NO alerta — recogido en ESTE canary: el ruido m-570/m-34 cesa); m-34 triaged (retirado builder-90, sucesor builder-91, clase misma que m-570); post-error IPH turn-12 + builder-87 = artefacto B5 real verificado con el IPD (veredicto: VARIANT-2 spawn interrumpido, re-spawn builder-88 confirmó, sin regresión); backlog aprobado: "settle-to-terminal" (W7-A asentar failed de retirados → terminal, no solo el próximo boot). (d) **PENDIENTE-OWNER (sin cambio)**: política retención C2 (N=50 + archive + `org.postsRetention.enabled:true` explícito — default OFF desplegado, la poda NO corre); apiKey DeepSeek para fallback deepseek-official (diferido sin fecha). (e) **Siguiente DAG**: **dshd-feedback** (AÑADIDO 1 — tools dept_feedback/list/update + store feedback.jsonl + archive; requiere deliverables ya wired) → luego jobs, health+quality, subagent/gui/pooler, F1-F10.

- **2026-08-26** — **TARDE-7 — FASE dshd-feedback (AÑADIDO 1) DEPLOYED + CHECKPOINT 5 (deploy 14:21Z)**: (a) **Commit `2f73609`** (+ package nuevo `packages/dshd-feedback` — librería pura 490 LOC `FeedbackStore` + estado + máquina de estados, bridges src/core/feedback.ts + src/feedback.ts, consumida por el bundle; aún NO plugin Cordis compuesto — decisión pendiente owner, split con ctx.get('deepartments.feedback') en fase posterior); 3 tools: `dept_feedback` (UNIVERSAL — cualquier agente: store append-only, estado 'abierto', notif a quality-head severity-gated: critico {wake,interrupt:true}, alto {wake}, medio/baja/mejora {noWake}), `dept_feedback_list` (heads/host: paged+cursor, filtros estado/severidad/tipo/emisor, sort severidad desc+createdAt asc; worker DENIED), `dept_feedback_update` (transición append-only; QH terminal resuelto|descartado con stamp cerró; head en-estudio; worker DENIED; reopen en-estudio→abierto, nunca desde terminal); store `<stateDir>/feedback.jsonl` prune-to-cap (live N=200, SOLO records terminales evictados, backup `.bak-<ts>-prune` + appendFile a `feedback-archive.jsonl`, rewrite atómico — NINGÚN borrado destructivo R6/dec4). (b) **ACL/ruta (PENDIENTE-OWNER)**: worker→QH directo PROHIBIDO (12/12) → Opción A: la notificación usa `deliverOrQueue('quality-head', notif, ...)` con `record.from` = FORWARDER ACL-legal (worker → su managerId/coordinator; head/host → sí mismo); emisor real en store+body; aclDenyGround NO tocado; fallback `record.from='deepartments'` si forwarder no resuelve; tradeoff cosmetico (el bus muestra el forwarder) — propuesta a confirmar por el owner. (c) **Ladder**: builds green (root + package); suite **443/443** (422+21: 17 unit FeedbackStore + 4 integración tools); reviewer PASS 7/7; plugin add; dump-config capas core(513)<bundle(560), sin capa nueva (librería — correcto); **smart_restart CANARY 14:21Z PASS** → post: 3090=200, pooler 200, boot limpio; **verificación EN VIVO**: `dept_feedback_list` → "feedback backlog (0 total)" (tool live, store sano). (d) **Obs no bloqueantes del reviewer** (documentadas, sin cambios): reopen sin validar 'evidencia nueva'; abierto→terminal directo solo QH; fallback pierde gate severity en worker sin manager; notas_qh/escalado no restringidos a QH (conservador). (e) **PENDIENTE-OWNER (acumulado)**: (i) política retención C2 N=50 + archive + `org.postsRetention.enabled:true` explícito (default OFF desplegado; poda NO corre); (ii) apiKey DeepSeek fallback deepseek-official (diferida sin fecha); (iii) confirmar ACL Opción A del feedback; (iv) librería vs plugin compuesto dshd-feedback (fase posterior). (f) **DAG siguiente**: jobs (weekly-repo-health / version-watch / daily) → health+quality → subagent/gui/pooler → F1-F10 (edits estructurales prompts/personas AUTORIZADOS). D1-FULL + módulos: core, webfetch, registry (A-series), sleep (B-series), feedback (AÑADIDO 1) — quedan por tallar jobs/health/quality/subagent/gui.

- **2026-08-26** — **TARDE-8 — CHECKPOINT 8 (C2 COMPLETO): poda posts.json efectiva + fixes shared-source/race/divergencia (deploys 18:47Z y 19:10Z)**: (a) **Checkpoint 8 (18:47Z, `5ad798b`)** — fix C2 shared-source (postsRetention leído SHARED-first del binding org resuelto — `coreOrg?.org ?? cfg.org` — NO del mirror del bundle; la poda no corría porque el knob vivía en el row dshd-core y el wiring leía config.org del bundle) + fix race loadPosts (puebla byPost/byChild directo + UN await persistPosts() gated sweptLegacy>0; posts.json estable ANTES de registryLoaded → boot-gate reconcile completo → poda determinista); revert de atomic-write tmp+rename (tmp huérfanos); ARCHITECTURE.md research restaurado HEAD; dept_who stub erróneo dropeado. (b) **Checkpoint 8 FINAL (19:10Z, `3d6563a`)** — fix PODA-PARTIAL — reconcileDurablePostsRegistry devuelve prunedPostIds + RegistryStore.removePosts sincroniza byPost/byChild → el persistPosts posterior escribe el set PRUNED (54) no el completo (268) — cierra la poda que se deshacía en 4s (la corrida 18:47 hizo backup+archive pero un writer posterior la sobrescribió; diagnóstico del IPD: prune file-based no sincronizaba in-memory). Test nuevo (byPost 61→11). Suite **475/475**, reviewer 7/7 PASS. (c) **Verificación LIVE**: posts.json 268 → **54 (50 retirados + 4 vivos: 3 heads + builder-103)**; backups posts.json.bak-*-prune (2) + posts-retired-archive.jsonl (430 líneas, dos corridas) — archivo/backup preservan todo (R6/política NO-borrar); pool honesto (4 key(s), 200) sin rotación errónea de oc-8; 0 errores de boot. (d) **Contexto del día (para el registro)**: incidente de capacidad 17:16-18:20Z (pooler pre-fix agotó 3 keys por under-block — fb-0 QD; el fix del pooler (repo dsh-key-pooler, 45/45) + oc-9 (key owner, valida) activos tras RESTART DE EMERGENCIA con HEAD (el WIP del IPD no-green en stash → recuperado → aterrizado en 5ad798b/3d6563a); error propio del Asistente (append del conf sin prefijo Environment= — ignorado por systemd) corregido; conf 600 root-only. OWNER presente; decisiones: crecimiento SIN recortes, flash 0731 para agentes (host vision — m-140 en cola IPD), OpenRouter documentado (verificación RD: NO :free DeepSeek; la palanca real = flash en la misma key = $30/mes 2x). (e) **PENDIENTE-OWNER (actualizado)**: (1) 1-2 keys opencode más ($10 c/u — pool 4 keys, 1 sana oc-9; flujo validado: pegarla → smoke → conf `Environment=` → daemon-reload → [próxima arrancada o envFile hot-reload]); (2) apiKey DeepSeek (diferida sin fecha); (3) [cerrado] C2 política N=50 + archive + gating — APROBADO y ACTIVO (36da84a + 5ad798b + 3d6563a — la poda corre en cada boot). (f) **Cola IPD**: dept_who running|idle (m-64 + evidencia QD: seam agents.get(sid).status==='running', precedencia sleeping↔live, test, preservar m-228) → model-flash (m-140, todos los agentes salvo host vision) → higiene sesiones muertas (m-162) → [siguiente DAG: health+quality → subagent/gui/pooler → F1-F10].
- **2026-08-26** — **TARDE-9 — MODEL-FLASH desplegado (CHECKPOINT 10, deploy 19:54Z) — org en deepseek-v4-flash**: (a) **`8b5a63a`** (6 files, 479/479 ×2, reviewer 7/7): WORKER_AGENT_OPTIONS → deepseek-v4-flash (3 spawn paths :4712/:5634/:5703/:6342, provider+reasoningEffort max intactos); CABEZAS coordinator.agentOptions.model → flash en AMBAS fuentes (dshd-core + mirror bundle) + reasoningEffort añadido al schema (org.ts:297-307 + dshd-core/index.ts:84 — preserva max, aditivo); HOST vision-exp INTACTO (invoke.ts:4733 + agent-default-model + persona — grep: ninguna ruta worker/head lee HOST_AGENT_OPTIONS); SUBAGENTES transitorios del host → **HEREDAN host (vision-exp), DOCUMENTADO, no-tocado** (decisión owner: no re-habilitar nativo ni insertar deepartments-subagent; seam real dsh-subagent resolveChildAgentOptions + fila nativa tool-subagent disabled — reports/explore-deep/2026-08-26-subagent-model-seam.md); presets re-generados del repo. (b) **Ladder**: builds green; suite 479/479; dump-config: deepseek-v4-flash ×8 en agentes/cabezas; deepseek-v4-flash-vision-exp SOLO host (:45 + persona :359 narrativa); plugin add; **smart_restart CANARY 19:54Z PASS** → post: 3090=200, pooler 200 (4 keys), boot limpio, dept_who coherente (heads idle, host sleeping), **VERIFICACIÓN EN VIVO: turno real del IPH corre en deepseek-v4-flash sin errores (razonamiento max)**. (c) **BENEFICIO** (decisión owner crecimiento sin recortes): cada key de $10 pasa de ~$15/mes (vision) a **$30/mes** (V4-Flash en el mismo pooler) = 2× capacidad a precio igual; vision-exp reservado al host. La palanca del RD (verificación: NO :free DeepSeek; cache-hit/off-peak; OpenRouter $0.03/$0.075 con tarjeta documentado-no-integrado). (d) **Cola IPD**: hygiene sesiones muertas m-162 (LOW — script pendiente del IPH/paso) → siguiente DAG: **health+quality** (espera PROGRAMMING REQUEST) → subagent/gui/pooler → F1-F10. (e) **PENDIENTE-OWNER (sin cambio)**: (1) 1-2 keys opencode más ($10 — pool 4 keys/1 sana oc-9); (2) apiKey DeepSeek (diferida); (3) [cerrado] C2 N=50 + gating (activo).
- **2026-08-26** — **NOCHE-1 — TOOLS-OPT P1-P5 desplegado (auditoría QD completa + decisiones owner)**: (a) **QD auditoría de TODAS las tools** (m-264) → reporte consolidado `.dsh/reports/quality/2026-08-26-tools-audit.md` (verdict: 2 issues C1/C3 → PR-A; 6 optimizaciones → PR-B/PR-C + fb-2/fb-3 agendadas; resto todo bien; inventario 21 repo + 27 harness tools, allow-lists por rol verificadas; ruido bus 11.6% = rutas diseñadas; superficie muerta host workflow/ralph/goal → decisión owner). (b) **Decisiones owner**: dept_who default SOLO activos idle|running (+ scope:all); send_message auto-creación SOLO cabezas (verificado PASS: wake-on-send ya rematerializa dormant heads con su config; typos fallan per-recipient) → confirmado SIN CAMBIOS; eco del plantel en spawn/retire; **DESHABILITAR workflow/ralph/goal del toolset del host** (decisión owner; goal además cancelado por el owner, no usar). (c) **Implementación IPD P1-P5** (reporte consolidado): P1 dept_who default activos (scope enum active|all|includeRetired-alias, header `(N member(s), M retired, K sleeping/offline hidden)`, inactiveHiddenCount excluye you, fila caller siempre); P2 activeMembers con state + roster compacto en 5 executes (row-builder único buildCatalogRows, fuente única); P3 preset dev host tool-goal/tool-workflow/tool-ralph `disabled: true` (seam tool-subagent; subagent/subagent_fork intactos; host-only por construcción; stable NO tocado); P4 R1 definición única memo/sleep/post_retire (neto −44/−45 LOC, comportamiento intacto) + R2 dept_post_create deprecada en description (sigue registrada y funcional, R6); P5 docs dept_exec → preferir read/grep/glob nativos (4 presets). Suite **482/482**, reviewer PASS por bloque ×3, `pnpm build` 0, git diff --check limpio. (d) **Commit `ca0b482`** + **smart_restart CANARY 23:19Z PASS** → post: 3090=200, suite desplegada; **verificación EN VIVO dept_who** con el nuevo layout: `Deepartments catalog (3 member(s), 70 retired, 2 sleeping/offline hidden):` (host YOU + IPH idle + QH idle; research-head y otros ocultos como sleeping/offline hidden) — P1 confirmado. (e) **Cola noche**: health+quality (GO owner, PROGRAMMING REQUEST m-289 al IPH) → subagent/gui/pooler → F1-F10; deudas menores (B6/B7 dshd-sleep, settle-to-terminal W7-A, C12 flaky, higiene m-162, [RD] NIM prototipo, monitor-dsh-updates-2) según capacidad pool 1 key; QD verificando tools (m-290). (f) **PENDIENTE-OWNER**: (1) apiKey DeepSeek (diferida sin fecha); (2) NO más keys opencode (pool 1 sana oc-9; mitigación flash 2x; futura vía OpenRouter/DeepInfra tarjeta o DeepSeek si decide); (3) goal cancelado — herramienta a retirar (owner). (g) **Cierre en pausa** mientras la optimización de tools trabaja en autonomía nocturna.
- **2026-08-26** — **NOCHE-2 — FASE health+quality: lote endurecimiento + alineación dec5 desplegado (deploy 23:47Z)**: (a) **Commit `00a9053` (5 files, +346/-42; suite 485/485, reviewers PASS x2)**: **LOTE A** (C4 health-alerts.jsonl cap 500; C9 post-errors descarta filas >2h en append; C6 scanDeliveryFindings tail-read por cursor byte-offset con sidecar ausente→[]; C8 invariante verif — la ALERT va store.append + busDeliverToHost directo, NUNCA escribe fila 'failed', el bucle alerta→error→alerta es estructuralmente imposible) + **LOTE B alineación dec5** (C10/C11/C16: cordis.patch.yml párrafo ENV-VAR ALIGNMENT `DEEPARTMENTS_QUALITY_INSPECT` env>config>default; AGENTS.md sección «Environment variables»; spec 007 §4.1/§5.2 re-enmarcados + coherencia drop-in 0.25). (b) **Ladder**: pnpm build 0 · plugin add · dump-config sin capas nuevas (comportamiento del perfil intacto) · **smart_restart CANARY 23:47Z PASS** → post: 3090=200, dept_who coherente (IPH/QH idle). (c) **FLAG no-bloqueante** (decisión del IPH, revisión PASS): C6 cambia la cadencia de re-recordatorio intra-boot para una fila 'failed' persistente (antes: cada 30 min mientras <2h; ahora: primer alert inmediato + re-alerta solo en el pase de boot si aún <2h) — el primer alert nunca se pierde; si el owner quiere la cadencia 30-min de vuelta es un follow-up pequeño (backlog). (d) **PENDIENTE-OWNER**: EXTRACCIÓN **dshd-health + dshd-quality** (paso 9 DAG, dec2, riesgo ALTO; mapa `/root/.deepartments/departments/internal-programming/reports/explore-deep/2026-08-26-health-quality-map.md`) — decisión: (1) extraer AHORA vs tras subagent/gui/pooler; (2) forma **LIB** (recomendada, precedente dshd-jobs/feedback) vs PLUGIN compuesto; esfuerzo ~1.5-2.5 días-builder; riesgos: re-apuntar la suite (importa lib/*.js), no doble-registrar los 3 plugins ocultos, C6 twin-safe. (e) **Cola**: subagent/gui/pooler → F1-F10; deudas menores (settle-to-terminal W7-A, C12 flaky, m-162 hygiene) → IPH; fb-2/fb-3 QD. (f) **Cierre en pausa** (optimización + health+quality en autonomía nocturna).

- **2026-08-27** — **DAY-1 — PASO 9 DAG COMPLETO (EXTRACCIÓN dshd-health + dshd-quality LIB) + fb-6 + key oc-10 desplegados (deploy 10:14Z, canary PASS)**: (a) **fb-6 `32d6314`** (collab QD m-367): fallback AgentOptions en materializePost (`isUsableAgentOptions` + `resolveMaterializeAgentOptions` → WORKER_AGENT_OPTIONS cuando el candidato no tiene provider/model; host D4 intacto) + forensia B5 (`withAgentOptionsContext` anexa JSON preservando texto/clasificación); suite 491/491. (b) **PASO 9 `c5131af` (+2504/-2094, 11 files)**: EXTRACCIÓN dshd-health (LIB, 1964 LOC, 92 exports) + dshd-quality (LIB hoja, 0 deps) como **LIBs puras** (patrón dshd-jobs/pooler), bridges src/core/health.ts + src/core/quality.ts same-module, bundle conserva SOLO efectos/closures, C6 twin-safe (1 call-site createDeliveryRowsTailReader), **no doble-registro de los 3 plugins ocultos**; suite 491/491 EXACTA (489 baseline + 2 fb-6), builds ×4 exit 0, dump-config capa `# == dsh-deepartments` SIN capas nuevas, drop-in intacto, stable 3080 + dsh-vanilla 3081 NO tocados, R6 intacto, reviewers PASS por lote (H 5/5, Q+FINAL 5/5). (c) **doc digest job `f59f07a`** (re-base del delta post-error a health-alerts.jsonl — post-errors no diffeable por log-rotation 26ac649). (d) **Key pool oc-10/ws10** (owner, 2ª key → workspace NUEVO = cuota independiente para la familia 429 per-workspace Open Code Go): registrada en drop-in `/etc/systemd/system/dsh-deepartments-dev.service.d/key-pooler.conf` (OPENCODE_GO_KEY_10 + OPENCODE_GO_WORKSPACE_10=ws10) vía pattern-discovery (oc-10 no declarado en config del pooler — declara oc-1..8); **pooler boot 10:14 = 5 key(s)** (antes 4 → oc-10 cargada); credencial nunca eco/log. (e) **Ladder**: builds ×4 exit 0, suite 491/491, plugin add OK, dump-config capa `# == dsh-deepartments` (línea 564) sin capas nuevas, **smart_restart CANARY PASS** → post: **3090=200**, org coherente (host + 3 heads idle, sin workers en vuelo), pooler vivo (`/usage` HTTP 200; monthly 98% resets 16:57Z — la cuota exhausta que la nueva key aborda), tree limpio 64 ahead. (f) **PENDIENTE-OWNER (sin cambio de fondo)**: EXTRACCIÓN health+quality YA desplegada (cerrado); F-HIGH (F1/F2/F3/F4/F5/F10 — skill/persona/política host, alta radiación, propuesta en m-355); D5 (3 plugins ocultos / re-exposición deepartments-subagent); 429 quota (2ª key registrada oc-10 pendiente de ver consumo; gate quota-aware como alternativa si no basta); apiKey DeepSeek (diferida); NO más keys opencode; tool goal a retirar; dsh-deepartments publish vs link-only; stable 3080 upgrade (dshmarket 1.21.2→1.33.0 — research monitor 08-27; NO ejecutado, constraint no-tocar-stable). (g) **DAG siguiente**: solo PENDIENTE-OWNER (F-HIGH + D5); nada técnico nuevo del roadmap. Versión-watch: dshmarket 1.31.2→1.33.0 (dev) candidato a bundlear en un futuro deploy; DSH core 0.1.1-rc.2 sin cambio.

- **2026-08-27** — **M1.1 CERRADO + M3 SYNERGY-DOCS (decisión owner, última de la cola)**: (a) **M1.1 qi-silence priming CERRADO y COMMITEADO** (`7172b19`, fix del falso positivo del primer ejercicio en vivo — commit del Asistente). (b) **M3 SYNERGY-DOCS** (este registro, docs-only): **WORK-REGISTER creado** (`docs/WORK-REGISTER.md` — registro TOTAL de trabajo pendiente: cola IPD activa, DAG técnico cerrado, PENDIENTE-OWNER, capacidad, backlog, sinergias; mantenido por IPD + Asistente); **flujos head↔head institucionalizados como hábito operativo (directiva m-422)** — sección "Head↔head as operational habit" en el skill deepartments-workflow + notas breves de sinergia en docs/specs 004/005/007 (IPD→RD research-on-demand = la NORMA; QD→RD análisis/contexto de fallos; RD proactivo tech-watch biweekly para IPD + análisis de fallos con QD; regla "resultados, no intercambios, hacia el Asistente"); **follow-ups alineados** — modelos `vision-exp` → `deepseek-v4-flash` en los 12 templates/READMEs de presets/departments + afirmaciones PRESENTES de docs/specs 004/005/007 (entradas históricas del ROADMAP intactas; host vision-exp intacto), y AGENTS.md línea 99 `0.10` → `0.25` (default del dado de inspección, coherente con la config 0.25). (c) **Cola técnica de DAG CERRADA** (referencia en el WORK-REGISTER §2): PASO 9 (c5131af) · fb-6 (32d6314) · F-HIGH (630a59c) · fb-7 pooler (3d55bbf) · A+B (408f1c6) · M1 (6f638d4) · M2 (274d550) · E2-IMPL (e09e687) — solo queda PENDIENTE-OWNER + BACKLOG. (d) **Verificación**: git diff --check limpio; docs-only (0 cambios en tests); suite completa y builds sin cambios esperados salvo el worktree M1.1 ya commiteado.

- **2026-08-27** — **M4 WATCHDOG DE INACTIVIDAD DEL SISTEMA implementado (builder-4, owner request directa — pendiente commit del Asistente)**: nuevo watchdog GLOBAL `system-idle` en `dshd-health` — ALERTA al host (flujo findings→dedupe→notifyHost existente, interrupt:true ya cableado; NUNCA deliverDaemonNotice) iff [cero agentes running (deps.posts[].running + NUEVA dep `hostRunning?` — ausente → no-op conservador)] AND [pendientes>0 (pendingCount de post non-retired OR turno interrumpido W8-h; q-i cae en pendingCount; agenda/calendar excluida)] AND [quietud ≥ `idleWindowMs` (default 900000 = 15 min, knob `health.idleWindowMs`; gate `health.systemIdleEnabled` default on)]. Ledger PROPIO `system-idle-state.json` con `firstQuietTs` (el prune 2h del shared borraría la ventana — precedente qi-silence-state); dedupe key `system-idle` en el ledger compartido → re-alerta cada 30 min mientras la quietud persista (nunca one-shot). Sin pendientes → warn-only (sin finding). Frame propio en buildHealthAlertFrame («<n> pendiente(s) sin agente running durante <ms> — posible espera que nunca llegó»); canal = el host (no existe canal directo al owner). Ficheros: packages/dshd-health/src/index.ts (+scan/ledger/knobs/frame), src/invoke.ts (buildHostRunning — misma expresión que buildCatalogRows), src/org.ts (2 knobs), test/invoke.test.js (+8 tests estilo M1: quietud+pendientes→ALERTA, sin pendientes→warn, running→nada, umbral, dedupe/re-alerta, ledgers separados, hostRunning ausente→no-op, SMOKE bootPlugin intervalMs:50+idleWindowMs:50).

- **2026-08-28** — **M-A MONITOR DE CONTEXTO implementado (builder-27, misión M-A owner — pendiente commit del Asistente)**: nuevo watchdog `context-threshold` en `dshd-health` (el 2º bloque M-A; la rotación-por-umbral dept_head_rotate aterrizó en el worktree pendiente de commit) — ALERTA al host (flujo findings→dedupe→notifyHost existente, interrupt:true ya cableado) iff el uso de ventana de contexto de un post O del host supera `contextThreshold` (default 0.5 = 50%, knob `health.contextThreshold`; gate `health.contextThresholdEnabled` default on). El % viene de la proyección `contextPressure` del token-meter (`projectedTokens / contextWindow`), leída por el bundle en-proceso vía `ctx.sessionProjections.snapshot(session).values.contextPressure` (la VISTA WIRE — superficie común de dsh-session-projection RC.7/RC.2) y pasada como dep estructural opcional `deps.sessionContexts` (ausente → no-op conservador, patrón hostRunning). **Dedupe por BANDA** sin ledger propio: key `context-threshold:<agentId>:b<floor(pct*10)>` en el shared health-alerts-state.json → cruce de banda (52%→61%) = key nueva = ALERTA inmediata; banda persistente → re-alerta cada 30 min por la dedupe shared; el prune 2 h del shared es seguro con keys de banda. `contextThresholdPollMs` (default 60000; patrón per-minute WAIT generalizado a cadencia arbitraria — el scan corre en el primer tick de cada bucket) + knobs en org.ts. Ficheros: packages/dshd-health/src/index.ts (+kind `context-threshold` + `hostId` en el finding + scan puro `scanContextThreshold` + frame + tick), src/invoke.ts (+`SessionProjectionsLike` snapshot surface + `buildSessionContexts` — fila por post non-retired + fila del host), src/org.ts (3 knobs), test/invoke.test.js (+9 tests: scan puro, tick ALERTA, cruce de banda, banda persistente 30 min, knob 0.8, poll gate, dep ausente/no-op, no-colisión con M1/M4 en el mismo tick, SMOKE bootPlugin real con fixture del unit contextPressure → frame al host), test/fixtures/ma-contextpressure-unit.js (fixture del unit), docs/WORK-REGISTER.md §1. Suite raíz 576/554/0/22 (base 566/544/0/22 + 9 míos + 1 del worktree M-A-rotate), builds root + dshd-health exit 0, git diff --check limpio.

- **2026-08-28** — **JORNADA DE ARQUITECTURA + INCIDENTES (host Asistente; el "día de la transformación")**: (a) **CRISIS DE CAPACIDAD** (12:42-13:10Z): oc-11 5h-limit → pool agotado → 503 KeyPoolerExhausted en cabezas/turnos (incl. turno host) — resuelto con **oc-12** (key owner, cuenta nueva/otra opencode — cuota independiente; drop-in + `daemon-reload` [causa raíz: drop-in systemd sin re-leer] + reset del state del pooler para re-descubrimiento) + **DS-fallback** (pooler c4a04bc: relay a api.deepseek.com cuando Go agotado — activación solo en agotamiento, salud cooldown 30s, no-echo; **live desde 06:25Z 08-29**). Aprendizajes: FB-13 (pooler no re-descubre keys env nuevas sin reset-state) + lección `daemon-reload` antes de restart. (b) **CADENA RAG — INCIDENTE DE SEGURIDAD CRÍTICO** (fb-12/13/14/15): el índice RAG del profile contenía **33 chunks con claves reales en claro** (sk-* pool OpenCode oc-1..8 + DEEPINFRA + DEEPSEEK + PARALLEL, vía toolresults verbatim en journals/sessions — causa: dumps de credenciales) → **purge** (rag.db → bak R6, ambas instancias — dev + stable 3080, 46 claves) + **denylist de contenido** (dsh-tool-web-enhanced 953c718/3f9a536) + **mini-fix dedup** (serialización engine + clear-before-insert — causa UNIQUE: PK posicional + ensureIndex no concurrency-safe) + **rebuild limpio** (0 valores; 14.342 chunks; verificación QD independiente PASS) — **fb-15/14 CERRADOS**. Owner: **NO rotación** (riesgo residual aceptado; tailnet-trusted). Disciplina FB-16 anotada (nunca dumps de credenciales en journals/reports). (c) **VENTANA ÚNICA (7f634ef)** — todo el DAG: Dx1 + fb-11 guard + fb-10 auto-wake + dice-logging (16938bf) · PR-1 + M-C (docs) · fb-9 realineación (config `compat.` — la clave m-603 era una fila muerta — + preflight ruta compat + wire) · ALTO-1/2 (delivery id-safe + host-session validation) · M-A (dept_head_rotate + monitor context-threshold) · Pacing (pacing.ts + franja wake-pack + avisos, valle/peak UTC) · dispatch-hardening (pre-check + b5-ghost) · dept_zstd_read · post-errors archive. Suite 610/588/0/22 → 615/593/0/22 (fb-18/19 + fb-25). (d) **ROTACIÓN DE CABEZAS (3 QH + 2 IPH; M-A, policy owner por umbral 50%)**: mecanismo probado; la #2/#3 del QH + la del IPH fueron **falsos-triggers por re-emisión de 400 antiguos** (causa: host rotó sobre cifra re-emitida del monitor post-error, no el cruce real — el bug de la jornada) → **fb-25 (ALTO)** — verifyRotateReason (cross-check reason vs projcache, stamp reasonVerified, nunca bloquea) + proveniencia sessionId+turn en post-error (commit 2b5a370; revisado live: mirror m-1194 → `unverified` vs 57bed534 184,914/17,6%) — **el fix que elimina la clase de raíz**. fb-18/19 resueltos (toolset worker sin dept_feedback_list/update + zstd >4MB con cap — 8e316c1). (e) **VERSION-WATCH**: dshmarket 1.36.0 (sin breaking) — instalado + live (boot 06:25Z 08-29). Core 0.1.2 NO instalado (peers → ERESOLVE trap; watch vigente). (f) **PENDIENTE-OWNER (a decisión del owner)**: validación DS-fallback (GET /user/balance + /v1/models — cuenta oficial), capacidad (enable-balance / keys extra), decisión D-Q2 (cadencia vs cap de trabajo para cabezas — 2 clases cuantificadas), publish 0.1.x, stable 3080 upgrade (dshmarket 1.21.2→1.36.0 + smooth-stream 0.3.4→0.4.1 — constraint no-tocar), hygiene .tmp, RAG-stable sentinel-pendiente (variante (b) aceptada). (g) **8-29**: audit de modularización en curso (explore-deep m-1295) — ¿totally modularizada? D5 en backlog 0.2.x.

- **2026-09-01** — **BLOQUE VALLE (Fase 4 auto-observación avanzada) — M-7 + fb-43 + fb-30 + pulse-digest + materializePost cold re-spawn (host Asistente; 3 lanes IPH; VALLE reapertura del pipeline)**: (a) **LANE A (`0dbf645`)** — **M-7 cola de misiones por head** (kind `mission-queue`, 12º watchdog Fase 4: `pendingCount` de posts HEAD non-retired ≥ `missionQueueLimit` (default 5) SUSTENIDO ≥ `missionQueuePersistMs` (60s; ledger propio `mission-queue-state.json` anti-transitorio — primer cruce solo registra, emite al sostener) → key `mission-queue:<postId>` en el shared ledger re-alerta 30 min; dep eficiente `buildHealthPosts` materializada una vez/tick (0 I-O extra); 6 tests incl. SMOKE real 2 heads) + **fb-43 restart-registry** (`restart-registry.jsonl` append-only con seed histórico idempotente de 4 filas: 22:31Z switch glm · 22:50Z reversión flash · **05:23:31Z DESCONOCIDA** · 06:08:57Z reload vw; reconcile via `bootId` del heartbeat; `buildRestartDigest`) + **fb-30 quiet-bands INVESTIGADO** (veredicto: DISEÑO/limitación, **no bug** — 8 mecanismos citados; recomendación catch-up durable = follow-up; denominador L332 = fixture de test, 0 cambios). (b) **LANE B (`b2ecb45`)** — **materializePost COLD RE-SPAWN (fb-29 estructural)**: al re-materializar un worker tras restart el toolset ya **NO** queda vacío — A primario re-resolución `resolveRoleTemplate` en el seam (entradas legacy sin campo `tools` re-resuelven; SpawnSurface +1 TYPE-only, exports 287 **sin** re-freeze) + B fallback `PostEntry.tools` durable (persistPosts/loadPosts) + guard fb-29 (template existente con scope vacío → **FAIL ruidoso**, nunca messaging-only silencioso). Locks 12/12 sin re-stamp. (c) **LANE C (`c59e1ab`)** — **pulse-digest job def** (07:30 diario, digest conciso de métricas de job al Asistente vía IPH; fuentes solo-existentes; missing-primitives→recommendations only) + **F3 MEMO NORM** en jobs recurrentes (dept_memo_write al final de cada round; anti-patrón journals stale 08-24) + coordinación RD. (d) **Suite `678/656/0/22`** (base `666/644` +12; build/add/dump-config verdes). **smooth-stream 0.4.3** aplicada a disco (forward-prep; live-load al próximo arranque — la línea core se mantiene en rc.2 HOLD, trap de peers 0.1.2 vigente). Restos: de-flake W6/BugA, catch-up durable fb-30, restart 05:23:31Z (restart-registry lo hará visible), ronda version-watch 09-02.

- **2026-09-01** — **BACKLOG AUTORIZADO (owner) — hardening 401/fb-39**: gate en pacing/pooler — cuando el pool detecta capacidad crítica (todas las keys con saldo bajo/límite), PAUSAR los despachos nuevos en vez de dejar que los turnos/jobs mueran con 401 (lección del outage 08-31 que mató todos los jobs del día; el fall-through fb-39 sostuvo 0 503 pero no salva el caso todas-secas). Candidato al DAG del IPD; pendiente de despacho cuando la capacidad de flota lo permita (owner: top-up no necesario por ahora — varias keys con buena capacidad; decisión D-Q2: mantener cadencia event-driven actual del QD, sin cap por ahora).

- **2026-09-01** — **BACKLOG AUTORIZADO (owner) — optimización de prompts/inyecciones de contexto + refuerzo del secretary (fb-47)**: (1) **secretary**: secretary-first para lecturas bulk/grounding en la wake routine · línea `tool-secretary` en wake packs de cabezas · templates de despacho con «busca reportes previos vía tu secretary» · patrón **memory-steward** (secretary lee journal+reportes → briefing compacto → el padre escribe el memo; refuerza la MEMO NORM). (2) **EN GENERAL**: condensar el skill `deepartments-workflow` (retirar secciones R6-deprecadas verbatim — templates builder/reviewer/scribe «for the record» — y deduplicar los espejos E2: departments directory + pacing, que aparecen en pack y skill) · acotar el tail del ROADMAP en el wake pack (resumen condensado por día en vez de entradas completas) · deduplicar la wake routine (aparece 3×: guidance del pack, skill, texto canónico) · revisar redundancias pack↔skill (M-A ya rota por umbral 50%, la inyección es parte de la presión de contexto). **DISCIPLINA de despacho: NO saturar el IPD** — el DAG actual (hardening 401/fb-39 → fb-27 → fb-28 → de-flake) sigue seriado; estas mejoras se agrupan en UN lane coherente de «mejoras de sistema» al terminar el DAG o en la siguiente ventana.

- **2026-09-01** — **BACKLOG AUTORIZADO (owner) — GUI "modo monitoreo" (composer oculto)**: ocultar el cuadro de texto de la interfaz web (y el botón enviar/detener) en: (1) TODAS las sesiones que NO son del host (cabezas/workers — el owner nunca les habla directamente; esto hace MOOT el display del selector en cabezas: el effort max vive en agentOptions por-agente y el selector lee el modelo declarado de la sesión — con el composer oculto el tema desaparece; en el host el selector sí muestra `Max`, verificado por el owner), y (2) la sesión del host asociado al TOGGLE Presente/Ausente (composer oculto en AUSENTE, visible en PRESENTE — aparece/desaparece con el toggle). Interfaz limpia tipo monitoring; imposibilita escribir estando supuestamente ausente. El estado de presencia debe exponerse al cliente GUI para que el toggle lo maneje. Nota técnica: el composer es del shell web del harness (dsh-web-app); el plugin dshd-gui (client-inject deepartments-client) es el punto probable del cambio (esconder por tipo de sesión/superficie + estado de presencia) — verificar viabilidad con el IPD al implementar.

- **2026-09-01** — **MISIÓN DE FONDO (owner) — TOTAL MODULARITY per Cordis (crecimiento/mantenimiento escalables)**: visión: todo el sistema extremadamente modular siguiendo la Filosofía/Arquitectura Cordis. **Status**: HITO 3 (decoupling, 5/5 fábricas delivery/spawn/tools/presets/boot) CERRADO 09-01; fase 0.2.x (applyInvoke residual, bridges, provide hito 4) en backlog. **DEFINICIÓN NECESARIA (owner declara ambigüedad mutua: ¿entiende él la Cordis philosophy? ¿entiende la arquitectura qué se busca?) → RESEARCH REQUEST al RD (m-1847)**: qué es Cordis (DI/plugins/composición, fuentes primarias), cómo lo usa DeepSeek Harness (bundle/plugin layers, service-first), qué propiedades definen un sistema "totalmente modular per Cordis" (end-state medible), y una **DEFINICIÓN DE DONE accionable + gap assessment** de nuestro estado (5 fábricas + 8 packages plugin-izados vs end-state). La definición del RD será el **EXIT-CRITERION de la misión** antes de seguir modularizando (2026-09-01: bloque VALLE + DAG en vuelo no la bloquean; la prioridad sigue el orden orgánico ya presentado).


- **2026-09-01** — **MISIÓN RATIFICADA (owner) — TOTAL MODULARITY per Cordis**: north-star **P1-P8 ACEPTADO** (definición del RD: reports/researcher/2026-09-01-cordis-total-modularity.md — 0 módulo monolítico · plugin-en-Context · capacidades=ctx.<key> services · swap sin tocar core · test por pieza · reversibilidad sin fugas · crecer añadiendo plugins · cultura service-as-seam) **+ AMPLIACIÓN owner**: **CADA DEPARTAMENTO = SU PROPIO PLUGIN (dshd-<dept>)** — la arquitectura de departamentos debe autoescalar y mejorarse añadiendo/quittando plugins-departamento como unidades composables/desechables (P7 elevado a unidad-departamento; la filosofía de reversibilidad aplicada a los departamentos). **Gap hoy ~2-3/8** (binder-a-closures, applyInvoke+políticas no-services, doble-mirror+híbridos). **Receta 0.2.x → 6-7/8** (binder→Service · applyInvoke→fábricas+políticas · colapsar doble-mirror/normalizar híbridos). **EXIT-CRITERION de la misión**: P1-P8 medibles (6/8 por inspección/test; P6/P8 cualitativas) + dept-as-plugin (P7 a nivel departamento). Implementación → IPD vía programming request tras el DAG actual/bloque B (disciplina no saturar).

- **2026-09-01** — **BACKLOG AUTORIZADO (owner) — feedback-nudge en errores de tools**: cuando un agente ve un error de tool (o un "unknown tool"), añadir un mensaje breve que le invite a valorar usar **dept_feedback** para reportar el error o solicitar un cambio/mejora al QD. DISEÑO: (1) NOSOTROS controlamos el texto de error de nuestras tools (dept_*, guard denials — tools.ts) → append de la línea-nudge en errores/denials; (2) el texto "unknown tool: <name>" es del HARNESS (no lo controlamos — las restricciones toolFilter del preset las valida el core) → cobertura vía **preset personas** (agente.cordis.yml worker/head): línea de guía «si encuentras un tool desconocido o un error, considera dept_feedback para reportarlo/proponer una mejora al QD». Ideal: ambos (errores nuestros + guía de persona para el texto del harness). Lane pequeño, disjunto (tools.ts + presets .md persona) — candidato post-DAG/junto a mejoras de sistema (alineado con fb-45/48: la fricción → feedback durable). Verificar con el IPD si el harness tiene algún punto de inyección para el texto unknown-tool (preferente si existe).

- **2026-09-01** — **VEREDICTO RD dsh-v0.1.2-alpha.4 (consolidado reports/researcher/2026-09-01-consolidated-alpha4-impact.md, 3 fronts + reviewer PASS)**: (1) **colisión de nombre send_message NO es nueva** — el nativo global (dsh-tool-subagent-control) existe desde rc.2 y ya coexiste con nuestro registro (nombres planos por capa; own-layer shadowa global; test probado); alpha.4 solo elimina la tool unidireccional `report` (nuestro registro NO cambia). (2) **HOLD 0.1.1-rc.2 confirmado (confianza ALTA)** — 3 bloqueantes: alpha≠rc (4 alphas/5 días) · **migración Session** (alpha.4 elimina `session.events`; lo usamos en 12+ sitios — 1 crash seguro invoke.ts:2243 pinSessionTitle + 11 degradaciones silenciosas + tests; migrar a snapshotEvents()/seq/eventAt() = PR previo a CUALQUIER host 0.1.2, mapeo con IPD) · ERESOLVE persiste (smart-restart/tool-web-enhanced/deepartments sin widening; dshmarket 1.39.0 + smooth-stream 0.4.3 sí admiten alpha.4). web_fetch default + workflow-PTC = impacto nulo. Ruta del move futuro: widen peers → migración Session → rc → aplicar. (3) **feedback-nudge VIABLE como plugin**: "unknown tool" hardcodeado en dsh-tools (ToolNotFoundError — NO servicio substituble, provide duplicado LANZA) PERO el seam existe = waterfall `tools/post-execute` (thrown tools llegan como errors) → opción A: accept{content} (preserva contrato del error) · opción B: anexar additionalContexts (no invasivo). Default propuesto: **B** (no invasivo; el nudge como contexto adicional). Conexión misión: NO es demo P4 — SÍ es demo P2/P6 (primera aplicación práctica de eventos-Cordis en nuestro stack). Implementación → IPD (post-B, con la cola).

- **2026-09-02** — **MISIÓN TOTAL MODULARITY (0.2.x) — TERMÓMETRO 7/8 + CIERRE DEL DAG 09-02** (IPD + host; detalle: docs/WORK-REGISTER.md LANDING 09-02): lanes 0.2.1/0.2.2/0.2.3a (`e8222af`)/0.2.3b (`dc9f79a`)/0.2.3c (`48cea9f`) + fix fb-55 (`5210682`) CERRADOS y desplegados (canary 06:14Z); suite 720/698/0/22 EXACTA; re-freeze CUT-4 `5b548545`; dshmarket 1.40.0 + dsh-smooth-stream 0.4.3 live. Post-DAG: DI-by-services CERRADO (`73f60d9`, MISIÓN 8/8, canary 11:17Z) · F6 (`006919c`, ancla recursión D-Q2) · fb-60 (`549fdfb`+`6b33664`, infra-veracidad) · fb-50/51/52 (`5ada8ac`, calibración M-A + glob + aritmética) · feedback-nudge (`77ca2de`, LIVE) · fb-57/59 (dsh-key-pooler) · fb-61 (`2f35cab`, source gate) · fb-62+53 (`5c4153a`, token-guard) · fb-63/66/67 (`6feff49`, familia toolset docs) · fb-64 scoping (opción C — ARCHITECTURE.md, `b05d75a`) · fb-65 (`35a267e`, opciones A+C) · fb-68 (`a00e8de`, atomicidad — suite 738/716/0/22). GUI modo monitoreo: scoping CERRADO (explore-deep 09-02), **OWNER-GATED (parqueado)**.

- **2026-09-03** — **DÍA DE INFARTO RESUELTO — apagón de coordinación ~2h + recuperación (owner presente; host rotado spec 002)**: crisis: crash-loop build-0.1.2-vs-kernel (parche dual Stable) · keys Go inválidas/sin-crédito (**usage 200 ≠ chat-auth** — lección del día) · **pooler-gate branch 3 re-armado por lastRotation stale → materializePost bloqueado → cabezas sin despertar → apagón de coordinación**. SOLUCIÓN (workarounds con backup): **key opencode-go del OWNER (chat-auth 200 — la única real) en oc-6** + gate-clean (lastRotation stale) + restos en cooldown (monthly resets 23-28/09) → pool OK → **delivery vivo (m-416/417)**; fix formal en la delivery lane. Commits del día 16+ (a00e8de último). Cadena IPH (secuencial): ① **clasificador 401-invalid** (en vuelo — prioridad) → ② **delivery lane commits 1-4** (fix formal del gate + re-drive) → ③ **stable :3080 (AUTORIZADO owner 09-03)**. QD: inspección del incidente en curso (2 quality-inspectors: delivery + fix-verify; datapoints fb-79/fb-58/fb-83 — familia de aborts AMPLIADA de mensajería a tools de operación bash). En cola: settings revert (coordinar IPH) · job-runs primitiva · transporte/fb-58 · familia fb-81/83 · **fb-78 A1 CERRADO (smokes re-aplicados vía API canónica fb-82)** + A2/A3. Bandeja owner: **0.1.2 DECIDIDO (owner 09-03: opción A — WIP+main hasta verde; el IPD aterriza la migración session-surface)** · **vanilla** (tras el aterrizaje). Registro completo: este entry + WORK-REGISTER LANDING 09-03. **VEREDICTO QD
consolidado (09-03, 3 inspectores — .dsh/reports/quality/2026-09-03-incident-
delivery-consolidated.md)**: cadena confirmada con artefactos (pooler stale →
gate branch 3 sin age-check dshd-health:3069-3077 → materializePost; 295 filas
failed; gate-clean 17:02:34Z re-entrega 100% en 71s, 0 pérdida); **fix formal
AUSENTE (lane ② = 0 commits)**; cierre de clase = lane ② + 48h de 0 failed;
**R1: branch 3 re-armable → prioridad ALTA de la lane ②**; fb-58 recidiva doble
(F-3 prepared x2 post-retirement, settle sidecar sin implementar); rotación
host m-423 PARTIAL (archive corto ~85 líneas, m-426 delivered post-retirement).
