# Local agent memory (never committed)
.dsh/reports/

# Node / build artifacts
node_modules/
dist/
lib/
/client/
packages/dshd-gui/client/
*.tgz

# OS
.DS_Store
.deepartments/
.deepartments.bak-*/

# Local convenience: pnpm release-age exclusion (never committed)
pnpm-workspace.yaml

# test-run artifacts (git hygiene, 09-01)
.run-*.log
.run-*.summary
/reports/

# Local scratch dumps (never committed; 23 MB of agent/reviewer dumps measured 09-15)
scratch/

# Atomic-write staging dirs of the harness fs writer (dsh-fs-local writeFileAtomic: `.<basename>.<pid>.<uuid>.tmpdir`, created INSIDE the target's dir; transient + untracked, never committed; 09-15)
*.tmpdir/
