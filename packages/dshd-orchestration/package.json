{
  "name": "dshd-orchestration",
  "version": "0.1.0-rc.1",
  "description": "Deepartments apply-orchestration layer: the 5 orchestration FACTORIES (boot/presets/spawn/tools/delivery — the closure zones of applyInvoke that coordinate the dshd-core kernel services + the live apply-fiber registries) extracted from the dsh-deepartments bundle into a standalone package, exposed as the 5 LAZY SERVICES deepartments.boot/.presets/.spawn/.tools/.delivery + the 5 deps holders deepartments.*Deps (pattern 1B of 0.2.1: the bundle WRITES the holders via register after its apply state is ready — P1 '0 ctx.provide in src' intact) + the substitutable POLICY services deepartments.pacing / deepartments.execRoots (P4).",
  "type": "module",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "exports": {
    ".": "./lib/index.js",
    "./package.json": "./package.json"
  },
  "files": [
    "lib",
    "cordis.patch.yml"
  ],
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    }
  },
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "dshd-core": "workspace:*",
    "dshd-feedback": "workspace:*",
    "dshd-gui": "workspace:*",
    "dshd-health": "workspace:*",
    "dshd-jobs": "workspace:*",
    "dshd-pooler": "workspace:*",
    "dshd-quality": "workspace:*"
  },
  "peerDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-llm": "^0.1.0-rc.7 || ^0.1.1-rc.0",
    "@deepseek-ai/dsh-session": "^0.1.0-rc.7 || ^0.1.1-rc.0",
    "@deepseek-ai/dsh-subagent": "^0.1.0-rc.7 || ^0.1.1-rc.0",
    "@deepseek-ai/dsh-tools": "^0.1.0-rc.7 || ^0.1.1-rc.0"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "^4.0.1",
    "@deepseek-ai/dsh-llm": "0.1.0-rc.7",
    "@deepseek-ai/dsh-session": "0.1.0-rc.7",
    "@deepseek-ai/dsh-subagent": "0.1.0-rc.7",
    "@deepseek-ai/dsh-tools": "0.1.0-rc.7",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "license": "MIT"
}