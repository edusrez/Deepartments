# dshd-core configuration layer — top-level YAML array of patch entries.
# FASE 2.5 BATCH A (scaffold): dshd-core is a LIBRARY — a barrel that re-exports
# the core surface (registry/messages/delivery/acl/wakepack/lifecycle/rotation/
# cleanup). It is NOT yet a Cordis plugin: the plugin row below exists so the
# package ships a patch-complete configuration layer, but it is INERT until the
# bundle wires dshd-core as a plugin with apply/inject/services (Batch B). The
# bundle's own cordis.patch.yml does NOT reference this row in Batch A, so it is
# never composed into a live configuration (it does not affect dump-config).
# Plain scalars only — no !!js outside the runtime config engine.
- insert:
    - id: dshd-core
      name: dshd-core
